load("@io_bazel_rules_go//go:def.bzl", "go_test")

go_test(
    name = "executor_test",
    timeout = "short",
    srcs = ["executor_test.go"],
    flaky = True,
    shard_count = 48,
    deps = ["//testkit"],
)
