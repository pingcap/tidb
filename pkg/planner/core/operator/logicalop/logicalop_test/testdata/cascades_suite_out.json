[
  {
    "Name": "TestLogicalExpandBuildKeyInfo",
    "Cases": [
      {
        "SQL": "SELECT * FROM (   SELECT     SUM(IFNULL(pay.payamt, 0)) AS payamt,     SUM(IFNULL(ret.retamt, 0)) AS retamt,     GROUPING(org.org_type) AS grouptype,     org.org_type,     GROUPING(org.org_id) AS groupid,     org.org_id,     org.org_name   FROM testorg org   LEFT JOIN (     SELECT       SUM(IFNULL(amt, 0)) AS payamt,       org_id     FROM testpay tp     WHERE tp.pay_date BETWEEN '2024-06-01' AND '2024-07-31'     GROUP BY org_id   ) pay ON pay.org_id = org.org_id   LEFT JOIN (     SELECT       SUM(IFNULL(amt, 0)) AS retamt,       org_id     FROM testreturn tr     WHERE tr.ret_date BETWEEN '2024-06-01' AND '2024-07-31'     GROUP BY org_id   ) ret ON ret.org_id = org.org_id   GROUP BY org.org_type, org.org_id WITH ROLLUP ) t WHERE groupid = 1 AND grouptype = 1;",
        "Plan": [
          "Projection root  Column, Column, grouping(gid)->Column, Column, grouping(gid)->Column, Column, test.testorg.org_name",
          "└─HashAgg root  group by:Column, Column, Column, funcs:sum(Column)->Column, funcs:sum(Column)->Column, funcs:firstrow(Column)->test.testorg.org_name, funcs:firstrow(Column)->Column, funcs:firstrow(Column)->Column, funcs:firstrow(Column)->gid",
          "  └─Projection root  ifnull(Column, 0.00)->Column, ifnull(Column, 0.00)->Column, test.testorg.org_name->Column, Column, Column, gid->Column",
          "    └─Selection root  eq(grouping(gid), 1), eq(grouping(gid), 1)",
          "      └─Expand root  level-projection:[test.testorg.org_name, Column, Column, <nil>->Column, <nil>->Column, 0->gid],[test.testorg.org_name, Column, Column, Column, <nil>->Column, 1->gid],[test.testorg.org_name, Column, Column, Column, Column, 3->gid]; schema: [test.testorg.org_name,Column,Column,Column,Column,gid]",
          "        └─Projection root  test.testorg.org_name, Column, Column, test.testorg.org_type->Column, test.testorg.org_id->Column",
          "          └─HashJoin root  left outer join, left side:HashJoin, equal:[eq(test.testorg.org_id, test.testreturn.org_id)]",
          "            ├─HashAgg(Build) root  group by:test.testreturn.org_id, funcs:sum(Column)->Column, funcs:firstrow(test.testreturn.org_id)->test.testreturn.org_id",
          "            │ └─TableReader root  data:HashAgg",
          "            │   └─HashAgg cop[tikv]  group by:test.testreturn.org_id, funcs:sum(ifnull(test.testreturn.amt, 0.00))->Column",
          "            │     └─Selection cop[tikv]  ge(test.testreturn.ret_date, \"2024-06-01\"), le(test.testreturn.ret_date, \"2024-07-31\"), not(isnull(test.testreturn.org_id))",
          "            │       └─TableFullScan cop[tikv] table:tr keep order:false, stats:pseudo",
          "            └─HashJoin(Probe) root  left outer join, left side:TableReader, equal:[eq(test.testorg.org_id, test.testpay.org_id)]",
          "              ├─HashAgg(Build) root  group by:test.testpay.org_id, funcs:sum(Column)->Column, funcs:firstrow(test.testpay.org_id)->test.testpay.org_id",
          "              │ └─TableReader root  data:HashAgg",
          "              │   └─HashAgg cop[tikv]  group by:test.testpay.org_id, funcs:sum(ifnull(test.testpay.amt, 0.00))->Column",
          "              │     └─Selection cop[tikv]  ge(test.testpay.pay_date, \"2024-06-01\"), le(test.testpay.pay_date, \"2024-07-31\"), not(isnull(test.testpay.org_id))",
          "              │       └─TableFullScan cop[tikv] table:tp keep order:false, stats:pseudo",
          "              └─TableReader(Probe) root  data:TableFullScan",
          "                └─TableFullScan cop[tikv] table:org keep order:false, stats:pseudo"
        ]
      },
      {
        "SQL": "SELECT SUM(IFNULL(pay.payamt, 0)) AS payamt,     SUM(IFNULL(ret.retamt, 0)) AS retamt,     GROUPING(org.org_type) AS grouptype,     org.org_type,     GROUPING(org.org_id) AS groupid,     org.org_id,     org.org_name   FROM testorg org   LEFT JOIN (     SELECT       SUM(IFNULL(amt, 0)) AS payamt,       org_id     FROM testpay tp     WHERE tp.pay_date BETWEEN '2024-06-01' AND '2024-07-31'     GROUP BY org_id   ) pay ON pay.org_id = org.org_id   LEFT JOIN (     SELECT       SUM(IFNULL(amt, 0)) AS retamt,       org_id     FROM testreturn tr     WHERE tr.ret_date BETWEEN '2024-06-01' AND '2024-07-31'     GROUP BY org_id   ) ret ON ret.org_id = org.org_id   GROUP BY org.org_type, org.org_id WITH ROLLUP having  groupid = 1 AND grouptype = 1;",
        "Plan": [
          "Projection root  Column, Column, grouping(gid)->Column, Column, grouping(gid)->Column, Column, test.testorg.org_name",
          "└─HashAgg root  group by:Column, Column, Column, funcs:sum(Column)->Column, funcs:sum(Column)->Column, funcs:firstrow(Column)->test.testorg.org_name, funcs:firstrow(Column)->Column, funcs:firstrow(Column)->Column, funcs:firstrow(Column)->gid",
          "  └─Projection root  ifnull(Column, 0.00)->Column, ifnull(Column, 0.00)->Column, test.testorg.org_name->Column, Column, Column, gid->Column",
          "    └─Selection root  eq(grouping(gid), 1), eq(grouping(gid), 1)",
          "      └─Expand root  level-projection:[test.testorg.org_name, Column, Column, <nil>->Column, <nil>->Column, 0->gid],[test.testorg.org_name, Column, Column, Column, <nil>->Column, 1->gid],[test.testorg.org_name, Column, Column, Column, Column, 3->gid]; schema: [test.testorg.org_name,Column,Column,Column,Column,gid]",
          "        └─Projection root  test.testorg.org_name, Column, Column, test.testorg.org_type->Column, test.testorg.org_id->Column",
          "          └─HashJoin root  left outer join, left side:HashJoin, equal:[eq(test.testorg.org_id, test.testreturn.org_id)]",
          "            ├─HashAgg(Build) root  group by:test.testreturn.org_id, funcs:sum(Column)->Column, funcs:firstrow(test.testreturn.org_id)->test.testreturn.org_id",
          "            │ └─TableReader root  data:HashAgg",
          "            │   └─HashAgg cop[tikv]  group by:test.testreturn.org_id, funcs:sum(ifnull(test.testreturn.amt, 0.00))->Column",
          "            │     └─Selection cop[tikv]  ge(test.testreturn.ret_date, \"2024-06-01\"), le(test.testreturn.ret_date, \"2024-07-31\"), not(isnull(test.testreturn.org_id))",
          "            │       └─TableFullScan cop[tikv] table:tr keep order:false, stats:pseudo",
          "            └─HashJoin(Probe) root  left outer join, left side:TableReader, equal:[eq(test.testorg.org_id, test.testpay.org_id)]",
          "              ├─HashAgg(Build) root  group by:test.testpay.org_id, funcs:sum(Column)->Column, funcs:firstrow(test.testpay.org_id)->test.testpay.org_id",
          "              │ └─TableReader root  data:HashAgg",
          "              │   └─HashAgg cop[tikv]  group by:test.testpay.org_id, funcs:sum(ifnull(test.testpay.amt, 0.00))->Column",
          "              │     └─Selection cop[tikv]  ge(test.testpay.pay_date, \"2024-06-01\"), le(test.testpay.pay_date, \"2024-07-31\"), not(isnull(test.testpay.org_id))",
          "              │       └─TableFullScan cop[tikv] table:tp keep order:false, stats:pseudo",
          "              └─TableReader(Probe) root  data:TableFullScan",
          "                └─TableFullScan cop[tikv] table:org keep order:false, stats:pseudo"
        ]
      }
    ]
  }
]
