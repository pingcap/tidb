[
  {
    "Name": "TestConstantPropagateWithCollation",
    "Cases": [
      {
        "SQL": "",
        "Plan": null,
        "Warning": null
      },
      {
        "SQL": "select * from t where name='a' and length(name)=1; -- without constant propagated",
        "Plan": [
          "TableReader root  data:Selection",
          "└─Selection cop[tikv]  eq(length(test.t.name), 1), eq(test.t.name, \"a\")",
          "  └─TableFullScan cop[tikv] table:t keep order:false, stats:pseudo"
        ],
        "Warning": null
      },
      {
        "SQL": "",
        "Plan": null,
        "Warning": null
      },
      {
        "SQL": "select * from t where name='a' and length(name)=1; -- without constant propagated",
        "Plan": [
          "TableReader root  data:Selection",
          "└─Selection cop[tikv]  eq(length(test.t.name), 1), eq(test.t.name, \"a\")",
          "  └─TableFullScan cop[tikv] table:t keep order:false, stats:pseudo"
        ],
        "Warning": null
      },
      {
        "SQL": "select * from (select 'test' as b from t) n where length(b) > 2; -- can be substituted",
        "Plan": [
          "Projection root  test->Column#3",
          "└─TableReader root  data:TableFullScan",
          "  └─TableFullScan cop[tikv] table:t keep order:false, stats:pseudo"
        ],
        "Warning": null
      },
      {
        "SQL": "select foo.a, foo.b, bar.c from foo join bar on foo.a=bar.a where (foo.a,foo.b) in ((1,2),(3,4));",
        "Plan": [
          "IndexJoin root  inner join, inner:TableReader, outer key:test.foo.a, inner key:test.bar.a, equal cond:eq(test.foo.a, test.bar.a)",
          "├─Selection(Build) root  or(and(eq(test.foo.a, 1), eq(test.foo.b, 2)), and(eq(test.foo.a, 3), eq(test.foo.b, 4)))",
          "│ └─Batch_Point_Get root table:foo handle:[1 3], keep order:false, desc:false",
          "└─TableReader(Probe) root  data:TableRangeScan",
          "  └─TableRangeScan cop[tikv] table:bar range: decided by [test.foo.a], keep order:false, stats:pseudo"
        ],
        "Warning": null
      },
      {
        "SQL": "select foo.a, foo.b, bar.c from foo left join bar on foo.a=bar.a where (foo.a,foo.b) in ((1,2),(3,4));",
        "Plan": [
          "IndexJoin root  left outer join, inner:TableReader, left side:Selection, outer key:test.foo.a, inner key:test.bar.a, equal cond:eq(test.foo.a, test.bar.a)",
          "├─Selection(Build) root  or(and(eq(test.foo.a, 1), eq(test.foo.b, 2)), and(eq(test.foo.a, 3), eq(test.foo.b, 4)))",
          "│ └─Batch_Point_Get root table:foo handle:[1 3], keep order:false, desc:false",
          "└─TableReader(Probe) root  data:TableRangeScan",
          "  └─TableRangeScan cop[tikv] table:bar range: decided by [test.foo.a], keep order:false, stats:pseudo"
        ],
        "Warning": null
      }
    ]
  }
]
