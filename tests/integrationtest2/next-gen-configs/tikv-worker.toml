[dfs]
prefix = "tikv"
s3-endpoint = "http://127.0.0.1:9000"
s3-key-id = "minioadmin"
s3-secret-key = "minioadmin"
s3-bucket = "next-gen-test"
s3-region = "local"

[ia]
mem-cap = "5GB"   # or "10%"
disk-cap = "20GB" # or "20%"
