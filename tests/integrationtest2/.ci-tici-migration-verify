Temporary marker for verifying the tici job migration.

The pull-integration-tici-test presubmit only runs on feature/fts branches;
this file exists solely to make the PR a non-docs-only change so the job is
not filtered out by skip_if_only_changed. This PR is a throwaway and will be
closed right after the verification.
