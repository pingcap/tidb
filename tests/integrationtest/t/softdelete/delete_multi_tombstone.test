create table t (id int primary key) softdelete retention 7 day;

# multiple soft-delete tombstone
insert into t(id) values (1), (2);
delete from t where id in (1, 2);

insert into t(id) values (1), (2);
select id from t;
drop table t;
