[
  {
    "name": "TestJSONPlanInExplain",
    "cases": [
      "explain format = json update t2 set id = 1 where id =2",
      "explain format = json insert into t1 values(1)",
      "explain format = json select count(*) from t1",
      "explain format = json select * from t1",
      "explain analyze format = json select * from t1, t2 where t1.id = t2.id"
    ]
  }
]
