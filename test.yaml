# All instances in a single host, but on different disks.

global:
  user: zzywq
  deploy_dir: tidb-deploy
  data_dir: tidb-data

pd_servers:
  - host: 127.0.0.1

tikv_servers:
  - host: 127.0.0.1

tidb_servers:
  - host: 127.0.0.1
    port: 4001
    status_port: 10082
    config:
      labels: 
        tidb_role: "ddl_worker"
  - host: 127.0.0.1
    port: 4002
    status_port: 10083
    config:
      labels: 
        tidb_role: "ddl_worker"
    
grafana_servers:
  - host: 127.0.0.1
