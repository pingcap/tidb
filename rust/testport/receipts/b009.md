# b009 — port `pkg/planner/funcdep` unit tests to Rust

Batch id: b009
Go packages: `pkg/planner/funcdep` (tests read from `origin/master`)
Owning crate: `tidb-funcdep`
Gate: `cargo nextest run --locked -p tidb-funcdep --no-fail-fast` (run from `rust/`)

## State found

All 12 Go test functions in the package were already translated in this crate:

* The 9 unit tests of `fd_graph_test.go` live as inline tests under
  `src/fd_graph.rs::mod tests` (the crate's convention for this file's
  Go-verified port).
* The 3 end-to-end pipeline tests of `extract_fd_test.go` live as
  `#[ignore]`-gapped stubs in `src/tests_extract_fd.rs`.
* `main_test.go` is goleak/testkit scaffolding only — nothing to port.

This pass re-derived every Go assertion from `origin/master` sources and
tightened two ports that asserted less than their Go originals:

1. `TestFDSet_ClosureOf`: Go appends `fe1..fe4` a **second** time (duplicate
   edges) before closing `{A,B}`; the Rust test now mirrors that.
2. `TestFDSet_AddEquivalence`: added Go's per-element `EquivalenceCols()`
   assertions (`"(1,2)"`, `"(3,4)"`) and full edge strict/equiv checks after
   the second `AddEquivalence`.

## Checklist

| Go test | Rust test(s) | status |
| --- | --- | --- |
| TestAddStrictFunctionalDependency (fd_graph_test.go) | `fd_graph::tests::add_strict_keeps_only_the_strongest` | ported |
| TestFDSet_ClosureOf (fd_graph_test.go) | `fd_graph::tests::closure_of_strict_is_transitive` | ported (strengthened: duplicated-edge case) |
| TestFDSet_ReduceCols (fd_graph_test.go) | `fd_graph::tests::reduce_cols_drops_derivable_determinants` | ported |
| TestFDSet_InClosure (fd_graph_test.go) | `fd_graph::tests::in_closure_matches_go` | ported |
| TestFDSet_AddConstant (fd_graph_test.go) | `fd_graph::tests::add_constant_absorbs_and_simplifies` | ported |
| TestFDSet_LaxImplies (fd_graph_test.go) | `fd_graph::tests::lax_implies_only_on_equal_dependency_side` | ported |
| TestFDSet_AddEquivalence (fd_graph_test.go) | `fd_graph::tests::add_equivalence_merges_classes` | ported (strengthened: EquivalenceCols element assertions) |
| TestFindCommonEquivClasses (fd_graph_test.go) | `fd_graph::tests::find_common_equiv_classes_between_sets` | ported |
| TestFDSet_ExtractFD (extract_fd_test.go) | `tests_extract_fd::fd_set_extract_fd` | ignored-gap |
| TestFDSet_ExtractFDForApplyAndUnion (extract_fd_test.go) | `tests_extract_fd::fd_set_extract_fd_for_apply_and_union` | ignored-gap |
| TestFDSet_MakeOuterJoin (extract_fd_test.go) | `tests_extract_fd::fd_set_make_outer_join` | ignored-gap |
| TestMain (main_test.go) | — | skipped-reason: goleak/testkit setup only, no assertions to port |

go-parity-gap lines for the three ignored tests:

- go-parity-gap: needs plan-build + LogicalOptimize + per-operator ExtractFD pipeline; tidb-planner's extract_fd is still a stub
- go-parity-gap: needs plan-build + LogicalOptimize + Apply/UnionAll ExtractFD; not present outside tidb-planner stubs
- go-parity-gap: golden depends on datasource-FD extraction + outer-join elimination invoking FdSet::make_outer_join within an optimized plan

## Baseline failure set

Clean tree, before edits: no failing tests. 12 run / 12 passed / 3 skipped
(the 3 pre-existing `#[ignore]` gaps above). Nothing to list; per contract,
`baseline-failing:` would have been recorded here — the set is empty.

## Commands run

```
cargo nextest run --locked -p tidb-funcdep --no-fail-fast   # baseline (clean tree)
#   Summary [   0.030s] 12 tests run: 12 passed, 3 skipped
cargo nextest run --locked -p tidb-funcdep --no-fail-fast   # after edits (from rust/)
#   Summary [   0.031s] 12 tests run: 12 passed, 3 skipped
git diff --check                                            # clean
```

Subset property holds trivially: baseline failure set is empty, final failure
set is empty.

## Not verified locally

* The three ignored extract_fd goldens are not executed on the Rust side (no
  planner pipeline); they remain pinned verbatim only in Go source.
* Go tests were not executed here; assertions were transcribed from
  `origin/master` sources and cross-checked against the Rust implementation.
