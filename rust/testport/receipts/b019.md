# testport batch b019 — pkg/util/dbterror → tidb-util

Go tests read from `origin/master` snapshot (`git show origin/master:...`).
The Go package has two real test functions: `terror_test.go::TestErrorRedact`
and `plannererrors/errors_test.go::TestError` (`main_test.go` is only
failpoint-style setup, no assertions).

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| pkg/util/dbterror/terror_test.go::TestErrorRedact | `tidb_util::dbterror::tests::error_redact` (pre-existing inline port in `rust/crates/tidb-util/src/dbterror/mod.rs`; re-verified line-by-line against master: all 17 errno cases, Enabled `?` substitution + no sensitive leak, Marker `‹…›` markers) | ported (pre-existing, verified) |
| pkg/util/dbterror/plannererrors/errors_test.go::TestError | `tidb_util::tests_dbterror::error` (new; added in this batch so the parity proof runs inside the owning/gated crate `tidb-util`, exercising `tidb_error::plannererrors`) | ported |

No ignored-gap tests. No skipped tests. No golden/testdata files required
(both Go tests are assertion-only).

## Notes

- Go's `checkErrMsg` in marker mode calls `markerRegexp.MatchString(...)` and
  discards the result (upstream quirk); the Rust port asserts marker presence
  instead — strictly stronger, same intent.
- The batch brief said "approx 3" Go test functions; master actually contains 2.

## Baseline failures

None — clean-tree baseline was fully green:

```
Summary [   5.414s] 706 tests run: 706 passed, 6 skipped
```

(no `baseline-failing:` lines)

## Commands run

From `/root/work/wt-b019/rust` (workspace root):

- Baseline (before edits):
  `cargo nextest run --locked -p tidb-util -E 'not test(/bench/)' --no-fail-fast`
  → `706 tests run: 706 passed, 6 skipped`
- After edits (identical gate):
  `cargo nextest run --locked -p tidb-util -E 'not test(/bench/)' --no-fail-fast`
  → `707 tests run: 707 passed, 6 skipped`
- `git diff --check` → clean.

Failure-set subset property holds trivially: baseline failure set was empty,
post-change failure set is empty; the one new test (`tidb-util::tests_dbterror::error`)
passes.

## Not verified locally

- Nothing else; both ported tests execute under the scoped gate.
