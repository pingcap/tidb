# Batch b019 — complete `pkg/util/dbterror` root-package audit

Owning crate: `tidb-util`. Comparison commit:
`e2788410d8d696605e8cb002585877a063ccc909`.

The complete root-package inventory was read: `BUILD.bazel`, `terror.go`,
`ddl_terror.go`, `terror_test.go`, and `main_test.go`. The nested
`exeerrors` and `plannererrors` directories are separate Go packages and are
not part of this receipt.

## Production parity

- All 22 exported error classes and `ErrClass.NewStd` are represented by
  `tidb_util::dbterror`.
- All 228 DDL error prototypes match the pinned Go package by variable name,
  code, RFC identity, and message template.
- `ReorgRetryableErrCodes` contains the same 19 codes, including
  `terror.CodeResultUndetermined`; `ReorgRetryableErrMsgs` contains the same
  four strings in order.
- The plain-message constructor remains crate-private implementation machinery;
  it is not an additional public API absent from Go.

## Test parity

| Go test | Rust test | Status |
| --- | --- | --- |
| `terror_test.go::TestErrorRedact` | `tidb_util::dbterror::tests::error_redact` | ported |
| `main_test.go::TestMain` | n/a (test-process setup and goleak harness only) | skipped-reason |

`dbterror::tests::reorg_retryable_errors_match_go` protects the restored
production collections. Fixture verification for the separate Go
`exeerrors` package now lives beside that Rust submodule, and the redundant
cross-crate plannererrors test carrier was removed because the owning
`tidb-error::plannererrors::tests::test_error` is the exact Go test.

## WIP validation

- `cargo test -p tidb-util --locked --lib 'dbterror::' -- --test-threads=1`
  → 4 passed.
- `cargo test -p tidb-error --locked --lib 'plannererrors::tests::' -- --test-threads=1`
  → 2 passed.

No Ready-profile claim is made by this receipt.
