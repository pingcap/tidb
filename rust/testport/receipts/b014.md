# b014 — port `pkg/util/checksum` unit tests (tidb-util)

Go source of truth read from `origin/master`:
- `pkg/util/checksum/checksum_test.go` (branch copy verified byte-identical to master)
- `pkg/util/checksum/main_test.go` (`TestMain` only; no behavior to port)

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| TestChecksumReadAt | checksum::tests::TestChecksumReadAt | ported (4-layer writer/reader nesting, offsets 0/5/10195, EOF zero-fill tail) |
| TestAddOneByte | checksum::tests::TestAddOneByte | ported (unencrypted + encrypted variants via `check_corruption`; insert at logical pos 5000 corrupts current+following blocks) |
| TestDeleteOneByte | checksum::tests::TestDeleteOneByte | ported (both variants) |
| TestModifyOneByte | checksum::tests::TestModifyOneByte | ported (both variants; only current block fails) |
| TestReadEmptyFile | checksum::tests::TestReadEmptyFile | ported (both variants; EOF at every block-aligned offset of an empty file) |
| TestModifyThreeBytes | checksum::tests::TestModifyThreeBytes | ported (both variants) |
| TestReadDifferentBlockSize | checksum::tests::TestReadDifferentBlockSize | ported (all 8 offset/len/eof cases from the Go table) |
| TestWriteDifferentBlockSize | checksum::tests::TestWriteDifferentBlockSize | ported (whole-write vs 100-byte batches produce identical files; both readable back) |
| TestChecksumWriter | checksum::tests::TestChecksumWriter | ported (explicit flush, cache offset, empty cache, read-back) |
| TestChecksumWriterAutoFlush | checksum::tests::TestChecksumWriterAutoFlush | ported (auto-flush on overflow, cache offset/content assertions) |
| main_test.go TestMain | n/a | skipped-reason: Go TestMain registers parallel/serial test setup only; Rust harness needs no equivalent |

The port lives inline as `#[cfg(test)] mod tests` in
`rust/crates/tidb-util/src/checksum/mod.rs`, matching this crate's convention
(tidb-util has no `src/tests_*.rs` sibling modules). It was landed previously in
commit dc535a86; this batch verified each assertion against the Go source on
`origin/master`, recorded the gate baseline, and found no gaps requiring new
code. Two extra source-derived wraparound tests (`flushed_offset_wraps_like_the_source_int64_counter`,
`read_cursor_wraps_like_source_int64_arithmetic`) pin int64 arithmetic parity.

## Baseline failures (clean tree, before edits)

baseline-failing: tidb-util memory::arbitrator::tests::bench::bench

(700 tests run: 699 passed, 1 failed, 1 skipped — the single failure is a
pre-existing flaky allocation-count assertion in
`crates/tidb-util/src/memory/arbitrator/tests/bench.rs:143`, unrelated to checksum.)

## Post-edit gate

Identical command re-run; result identical: added/pre-existing checksum tests
all pass; failure set unchanged (subset property holds — same single pre-existing
failure, no new failures, none disappeared).

## Commands run

- `cd /root/work/wt-b014/rust && PROTOC=/tmp/pw/protoc/data/bin/protoc cargo nextest run --locked -p tidb-util --no-fail-fast`
  → Summary [9.071s] 700 tests run: 699 passed, 1 failed, 1 skipped
  (FAIL tidb-util memory::arbitrator::tests::bench::bench)
- `git diff --check` → clean

Environment note: no system protoc; used `/tmp/pw/protoc/data/bin/protoc`
(PyPI protoc-wheel-0, same workaround documented in receipts b007/b008) purely
to drive `crates/tidb-proto/build.rs`.

## Not verified locally

- No re-execution against real TiKV or under encryption-at-rest hardware paths;
  the encrypted variants use the crate's software `encrypt::CtrCipher`.
- `memory::arbitrator` baseline failure was not investigated/fixed (out of batch scope).
