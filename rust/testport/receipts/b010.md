# b010 — port `pkg/sessionctx/variable` unit tests, part 1 (tidb-vardef)

Go source of truth read from `origin/master`:
- `pkg/sessionctx/variable/embedding_vars_test.go` (3 tests)
- `pkg/sessionctx/variable/main_test.go` (TestMain)
- `pkg/sessionctx/variable/mock_globalaccessor_test.go` (1 test)
- `pkg/sessionctx/variable/nextgen_test.go` (3 tests, `//go:build nextgen`)
- `pkg/sessionctx/variable/removed_test.go` (1 test)
- `pkg/sessionctx/variable/statusvar_test.go` (1 test)
- `pkg/sessionctx/variable/sysvar_test.go`, first 50 tests in line order
  (`TestSQLSelectLimit` .. `TestTiDBIgnoreInlistPlanDigest`)

This is tests 1–60 of the package's canonical ordering (alphabetical path,
line number; 150 entries including 3 `TestMain`s). Part 3 (tests 121–150) was
batch b012 (`receipts/b012.md`).

The owning crate ports only the `vardef` constants layer (name constants,
`Def*` defaults, mode enums) and pure `GlobalSystemVariableInitialValue`
policy. Tests whose subject is the `SysVar` registry, `SessionVars`,
validation/clamping, or `MockGlobalAccessor4Tests` are recorded as
`#[ignore]`d stubs with a `go-parity-gap` reason. Constant-level assertions
that ARE expressible in this crate are written as real partial-port tests.

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| TestNormalizeOpenAIEmbeddingAPIBase | tidb-session::tests_session_embedding_source::normalize_openai_embedding_api_base_matches_go | already-ported (the session crate owns the endpoint normalizer and its whitelist; focused source matrix passes) |
| TestGetOpenAIEmbeddingBaseURL | tidb-session::tests_session_embedding_source::openai_embedding_global_write_normalizes_and_versions | already-ported (GLOBAL writes normalize endpoints and advance the config generation only on effective changes) |
| TestEmbeddingAPIKeySysVars | tidb-session::tests_session_embedding_source::embedding_api_keys_are_masked_and_versioned | already-ported (all six embedding API-key globals mask values and version updates are live) |
| TestMain (main_test.go) | main_test_harness | skipped-reason: Go test-harness entry point, no Rust counterpart needed |
| TestMockAPI | mock_api_unported | ignored-gap |
| TestTiDBPessimisticTransactionFairLocking | pessimistic_transaction_fair_locking_nextgen_initial_value | partial port (initial-value assertion via global_system_variable_initial_value; Validate/SetSessionFromHook halves are gap) |
| TestTiDBDMLTypeInNextGen | tidb_dml_type_in_next_gen_unported | ignored-gap |
| TestTiDBReplicaReadInNextGen | tidb_replica_read_in_next_gen_unported | ignored-gap |
| TestRemovedOpt | tidb-session::tests_global_vars::removed_system_variables_ignore_set_and_explain_reads | already-ported (Go removed-variable registry, case-insensitive lookup, parse-but-ignore SET/SET GLOBAL compatibility, and 8136 explanatory reads are live) |
| TestStatusVar | status_var_unported | ignored-gap |
| TestSQLSelectLimit | tidb-session::vars::tests::sql_select_limit_uses_go_typed_state; tidb-session::tests_core::session_state::sql_select_limit_caps_a_statement_that_wrote_no_limit | partial port (typed setter and top-level limit injection are live; the existing scalar-subquery planner fixture still fails with `Unsupported("plan produces no schema")`) |
| TestSQLModeVar | tidb-session::tests_core::session_state::sql_mode_is_normalized_when_it_is_set; tidb-session::vars::tests::session_sql_mode_uses_go_typed_state | already-ported (Go mode expansion, case normalization, duplicate elimination, typed state, restore, inheritance, and 1231 rejection are live) |
| TestTiDBTraceEventSysVar | tidb_trace_event_sys_var_unported | ignored-gap |
| TestMaxExecutionTime | tidb-session::vars::tests::max_execution_time_uses_go_typed_state; tidb-session::tests_core::session_state::set_var_hint_overlays_one_statement | already-ported (negative values clamp to zero, the session hook stores the millisecond deadline, and statement contexts consume the typed value) |
| TestTiDBMaxKeysRead | tidb-session::vars::tests::max_keys_read_uses_go_select_gate_and_typed_state; tidb-exec::hint_updatable_vars::hint_updatable_registry_preserves_source_membership | already-ported (negative values clamp to zero, positive values update typed session state, and the Go hint-updatable marker is preserved) |
| TestGetMaxKeysRead | tidb-session::vars::tests::max_keys_read_uses_go_select_gate_and_typed_state | already-ported (the accessor returns the configured value in SELECT context and Go's zero/unlimited sentinel outside SELECT) |
| TestTiFlashMaxBytes | tidb-session::sysvar::tests::tiflash_signed_quota_validation_matches_go | partial port (Go signed-range clamping and Int64 rejection are live; SessionVars typed TiFlash hook fields remain a documented boundary) |
| TestTiFlashMemQuotaQueryPerNode | tidb-session::sysvar::tests::tiflash_signed_quota_validation_matches_go | partial port (Go signed-range clamping and Int64 rejection are live; SessionVars typed TiFlash hook fields remain a documented boundary) |
| TestTiFlashQuerySpillRatio | tidb-session::sysvar::tests::tiflash_query_spill_ratio_validation_matches_go | partial port (Go 0.85 upper-bound refusal and generic negative clamp are live; typed SessionVars hook storage remains a documented boundary) |
| TestTiDBTTLJobEnableSetGlobalUpdatesLocalWithoutExternalWorkload | ttl_job_enable_set_global_updates_local_without_external_workload_unported | ignored-gap |
| TestTiFlashHashJoinVersion | tidb-session::sysvar::tests::tiflash_hash_join_version_validation_matches_go | already-ported (Go legacy/optimized case-insensitive validation and refusal are live) |
| TestCollationServer | tidb-session::tests_session_var_hooks::collation_server_normalizes_rejects_and_updates_charset | already-ported (collation names canonicalize case-insensitively, unknown names return 1273, and the session hook mirrors the owning charset) |
| TestDefaultCollationForUTF8MB4 | tidb-session::tests_session_var_hooks::default_collation_for_utf8mb4_normalizes_warns_and_rejects_other_charsets | already-ported (accepted utf8mb4 collations normalize and warn with 1681; other charsets return registered 3721) |
| TestTimeZone | tidb-session::sysvar::tests::time_zone_uses_go_parser_and_error; tidb-session::vars::tests::time_zone_uses_go_typed_state | already-ported (Go timezone validation, fixed-offset/name semantics, and typed session-zone propagation are live) |
| TestTxnIsolation | tidb-session::tests_global_vars::global_isolation_skip_waits_for_the_next_session; tidb-session::tests_global_vars::an_unsupported_isolation_level_is_refused_8048; tidb-session::tests_global_vars::skipping_the_isolation_check_warns_and_stores | already-ported (Go validation normalization, aliases, unsupported-level warning/refusal, global inheritance, and session-scope isolation semantics are live) |
| TestTiDBMultiStatementMode | tidb-session::vars::tests::multi_statement_mode_uses_go_typed_state; tidb-session::tests_multi_table_dml::multi_statement_admission_follows_capability_then_sysvar | already-ported (enum values normalize into Go's OFF/ON/WARN typed mode and admission honors capability, refusal, and deferred-warning behavior) |
| TestReadOnlyNoop | tidb-session::tests_core::session_state::read_only_noop_variables_need_the_noop_gate; tidb-session::tests_global_vars::global_read_only_noop_variables_need_the_global_gate | already-ported (Go session/global no-op gating, aliases, transaction READ ONLY, OFFLINE MODE, and 1235 refusal/allowance semantics are live) |
| TestSkipInit | skip_init_unported | ignored-gap |
| TestSessionGetterFuncs | tidb-session::tests_session_var_hooks::session_getter_functions_read_live_defaults; tidb-session::tests_core::transactions::*; tidb-session::tests_binding::*; tidb-session::tests_non_prepared_plan_cache::* | partial port (Go live getters for current TSO, last txn/query info, plan-cache, and binding state are wired; query-diagnostic mutation/RU accounting remains a documented boundary) |
| TestInstanceScopedVars | instance_scoped_vars_unported | ignored-gap |
| TestSecureAuth | tidb-session::sysvar::tests::secure_auth_rejects_off_and_accepts_on; tidb-session::tests_global_vars::secure_auth_global_write_rejects_off | already-ported (Go permanent-ON validation, exact 1231 refusal, unchanged value, and global assignment path are live) |
| TestTiDBReplicaRead | replica_read_unported | ignored-gap (classic-kernel branch; nextgen initial-value pattern covered by pessimistic_transaction_fair_locking_nextgen_initial_value) |
| TestSQLAutoIsNull | tidb-session::tests_session_var_hooks::a_noop_gated_variable_refuses_to_off_and_a_hint_takes_that_value | already-ported (Go no-op gate, relaxed SET_VAR value, refusal, and enabled-session behavior are live) |
| TestLastInsertID | tidb-session::tests_core::status_values::last_insert_id_sysvar_preserves_unsigned_publication; tidb-session::tests_core::status_values::last_insert_id_argument_form_publishes_for_the_next_statement | already-ported (Go zero/default getter, previous-statement publication, 64-bit unsigned native value, and identity alias are live) |
| TestTimestamp | tidb-session::sysvar::tests::timestamp_validation_matches_go; tidb-session::tests_timestamp_range::* | already-ported (Go timestamp range validation, truncation warning, timezone-aware DDL/write bounds, DST gap, and read asymmetry are live) |
| TestIdentity | tidb-session::tests_core::status_values::last_insert_id_argument_form_publishes_for_the_next_statement | already-ported (Go identity alias reads the published LAST_INSERT_ID value, including 64-bit values and rollback persistence) |
| TestLcTimeNamesReadOnly | tidb-session::tests_session_var_hooks::locale_and_charset_compatibility_variables_match_go | already-ported (Go read-only 1238 refusal and default `en_US` getter are live) |
| TestLcMessages | tidb-session::tests_session_var_hooks::locale_and_charset_compatibility_variables_match_go | already-ported (Go mutable session locale setter/getter and `zh_CN` value are live) |
| TestDDLWorkers | ddl_workers_bounds | partial port (Min/Max DDLReorgBatchSize bounds pinned test-local per b012 convention + name/default constants; Validate clamping is gap) |
| TestDefaultCharsetAndCollation | tidb-session::tests_session_var_hooks::locale_and_charset_compatibility_variables_match_go | already-ported (Go `utf8mb4` / `utf8mb4_bin` connection defaults are live) |
| TestIndexMergeSwitcher | index_merge_switcher_default | partial port (DefTiDBEnableIndexMerge==true constant half) |
| TestNetBufferLength | tidb-session::sysvar::tests::net_buffer_length_validation_matches_go | already-ported (Go unsigned min/max clamping is live) |
| TestTiDBBatchPendingTiFlashCount | tidb-session::sysvar::tests::batch_pending_tiflash_count_validation_matches_go | already-ported (Go unsigned range and decimal-type rejection are live) |
| TestTiDBMemQuotaQuery | tidb-session::sysvar::tests::mem_quota_query_validation_matches_go | already-ported (Go signed -1 unlimited sentinel and both-scope clamping are live) |
| TestTiDBQueryLogMaxLen | tidb-session::sysvar::tests::query_log_max_len_validation_matches_go | already-ported (Go global [0, 1 GiB] clamping is live) |
| TestTiDBCommitterConcurrency | tidb-session::sysvar::tests::committer_concurrency_validation_matches_go | already-ported (Go global [1, 10000] clamping is live) |
| TestTiDBDDLFlashbackConcurrency | tidb-session::sysvar::tests::ddl_flashback_concurrency_validation_matches_go | already-ported (Go global [1, 256] clamping is live) |
| TestDefaultMemoryDebugModeValue | tidb-session::sysvar::tests::memory_debug_mode_defaults_match_go | already-ported (Go zero defaults for both memory-debug controls are live) |
| TestSetTIDBDistributeReorg | tidb-session::tests_global_vars::distribute_reorg_global_switch_round_trips | already-ported (Go global boolean setter/getter round-trip is live) |
| TestDefaultPartitionPruneMode | default_partition_prune_mode_constant | partial port (DefTiDBPartitionPruneMode=="dynamic" constant half) |
| TestSetTIDBFastDDL | fast_ddl_default_on | partial port (default On half) |
| TestSetTIDBDiskQuota | disk_quota_default_100gb | partial port (default 100GB half) |
| TestTiDBServerMemoryLimit | server_memory_limit_defaults | partial port (DefTiDBServerMemoryLimitSessMinSize=128<<20 + name constants; DefTiDBServerMemoryLimit is computed dynamically in Go so has no static constant here; accessor round-trips are gap) |
| TestTiDBServerMemoryLimit2 | server_memory_limit2_unported | ignored-gap |
| TestTiDBServerMemoryLimitSessMinSize | server_memory_limit_sess_min_size_default | partial port (same default pin as above) |
| TestTiDBServerMemoryLimitGCTrigger | server_memory_limit_gc_trigger_default_format | partial port (DefTiDBServerMemoryLimitGCTrigger formats to "0.7"; gctuner interactions are gap) |
| TestSetAggPushDownGlobally | agg_push_down_default_off | partial port (DEF_OPT_AGG_PUSH_DOWN=false default half; accessor round-trip is gap) |
| TestSetDeriveTopNGlobally | derive_top_n_default_off | partial port (same shape for tidb_opt_derive_topn) |
| TestSetJobScheduleWindow | tidb-session::sysvar::tests::ttl_job_schedule_window_validation_matches_go | partial port (Go UTC full-day normalization and clock validation are live; process-wide typed atomic getter state remains a documented boundary) |
| TestTiDBIgnoreInlistPlanDigest | ignore_inlist_plan_digest_default_on | partial port (DefTiDBIgnoreInlistPlanDigest=true default half) |

## Baseline failures

Baseline recorded before any edit:

```
cargo nextest run --locked -p tidb-vardef --no-fail-fast
Summary [   0.016s] 11 tests run: 11 passed, 26 skipped
```

No baseline-failing lines (clean tree had zero failures).

## Commands run and summaries

1. Baseline (clean tree): `cargo nextest run --locked -p tidb-vardef --no-fail-fast`
   → `11 tests run: 11 passed, 26 skipped`
2. After edits (identical gate): `cargo nextest run --locked -p tidb-vardef --no-fail-fast`
   → `24 tests run: 24 passed, 73 skipped`
   (13 new tests: 12 passing partial ports + inventory stubs; failure set
   remains empty = subset of empty baseline)
3. `cargo fmt -p tidb-vardef`
4. `git diff --check` → clean

## Not verified locally

- The Go tests were read from the `origin/master` snapshot via
  `git show`/`git grep`; the nextgen-tagged tests (`nextgen_test.go`) could
  not be executed on the Go side (build tag), only re-derived from source.
- All ignored-gap tests are unexecuted by design until their owning code
  lands in a Rust crate.
