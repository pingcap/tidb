# Batch b005 — testport receipt: pkg/util/kvcache

Gate crate: `tidb-kvcache`. Go tests read from `origin/master`
(`git show origin/master:pkg/util/kvcache/simple_lru_test.go`).

## Checklist

| Go test (pkg/util/kvcache/simple_lru_test.go) | Rust test(s) | Status |
| --- | --- | --- |
| TestMain (main_test.go) | n/a — Rust has no failpoint init for this crate | skipped-reason |
| TestPut | `tidb-kvcache::simple_lru_test::test_put` (tests/simple_lru_test.rs, pre-existing port; verified against master source) | ported |
| TestZeroQuota | `tidb-kvcache::simple_lru_test::test_zero_quota` | ported |
| TestOOMGuard | `tidb-kvcache::simple_lru_test::test_oom_guard` | ported |
| TestGet | `tidb-kvcache::simple_lru_test::test_get`; Peek sub-assertions → `tidb-kvcache::tests_simple_lru::get_peek_does_not_promote_entry` | ported / ignored-gap |
| TestDelete | `tidb-kvcache::simple_lru_test::test_delete` | ported |
| TestDeleteAll | `tidb-kvcache::simple_lru_test::test_delete_all` | ported |
| TestValues | `tidb-kvcache::simple_lru_test::test_values` | ported |
| TestPutProfileName | `tidb-kvcache::simple_lru_test::test_put_profile_name` (+ `kvcache_source::profile_name_is_stable`) | ported |

## go-parity gaps

- `SimpleLruCache::peek` (Go `(*SimpleLRUCache).Peek`, non-promoting lookup,
  asserted in Go `TestGet`) is not implemented in the Rust crate. Adding a new
  public API exceeds this batch's edit scope (`src/tests_*.rs`, lib.rs mod
  registrations only), so it is pinned as
  `#[ignore = "go-parity-gap: ..."]` in
  `crates/tidb-kvcache/src/tests_simple_lru.rs`.

## Baseline

Recorded on the clean tree before any edit:

```
Summary [   0.023s] 16 tests run: 16 passed, 0 skipped
```

baseline-failing: (none)

## Commands run

- `cargo nextest run --locked -p tidb-kvcache --no-fail-fast` (baseline,
  clean tree): 16 passed, 0 failed.
- Same gate after edits: 16 run, 16 passed, 1 skipped (the new
  `get_peek_does_not_promote_entry` ignore). Added-test failure set is empty;
  subset property holds trivially.
- `git diff --check`: clean.

Note: gate must be invoked from the workspace root directory `rust/` (no
Cargo.toml at repo root).

## Not verified locally

- The ignored Peek behavior itself (unimplemented API); assertions are written
  as comments in the ignored test to be activated once `peek` exists.
- Behavior under real memory-pressure conditions: Go's `memory.MemTotal()` /
  `InstanceMemUsed()` are approximated in tests by `/proc/meminfo` reads and a
  fixed half-of-total probe, matching the pre-existing port's helpers.
