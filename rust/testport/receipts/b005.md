# Batch b005 — testport receipt: pkg/util/kvcache

Gate crate: `tidb-kvcache`. The complete Go package was read from pinned
commit `e2788410d8d696605e8cb002585877a063ccc909`, including `simple_lru.go`,
`simple_lru_test.go`, `main_test.go`, and `BUILD.bazel`.

## Checklist

| Go test (pkg/util/kvcache/simple_lru_test.go) | Rust test(s) | Status |
| --- | --- | --- |
| TestMain (main_test.go) | n/a — Rust has no failpoint init for this crate | skipped-reason |
| TestPut | `tidb-kvcache::simple_lru_test::test_put` | ported |
| TestZeroQuota | `tidb-kvcache::simple_lru_test::test_zero_quota` | ported |
| TestOOMGuard | `tidb-kvcache::simple_lru_test::test_oom_guard` | ported |
| TestGet | `tidb-kvcache::simple_lru_test::test_get` | ported |
| TestDelete | `tidb-kvcache::simple_lru_test::test_delete` | ported |
| TestDeleteAll | `tidb-kvcache::simple_lru_test::test_delete_all` | ported |
| TestValues | `tidb-kvcache::simple_lru_test::test_values` | ported |
| TestPutProfileName | `tidb-kvcache::simple_lru_test::test_put_profile_name` | ported |

## Go-parity gaps

None in the pinned package inventory. An earlier receipt claimed that Go had
`(*SimpleLRUCache).Peek`; neither `simple_lru.go` nor `TestGet` at the pinned
commit contains that API or assertion, so the ignored Rust placeholder was
removed rather than implemented.

The second `kvcache_source.rs` carrier duplicated the eight original tests and
depended on Rust-only `len`, `is_empty`, and capacity accessors. It and those
public APIs were removed. `Delete` now has Go's no-result contract, and the
fresh capacity error no longer has Rust-only clone/copy/equality semantics.
ApplyCache callers use the source `Size` surface.

## Current gate

```
Summary: 8 tests run: 8 passed, 0 skipped
```

## Commands run

- `cargo test -p tidb-kvcache --locked`: 8 passed, 0 failed.
- `cargo check -p tidb-stmtsummary -p tidb-datatype -p tidb-session
  -p tidb-executor --lib --locked`: all consumers compiled; only pre-existing
  warnings were emitted.
- `git diff --check`: clean.

Note: gate must be invoked from the workspace root directory `rust/` (no
Cargo.toml at repo root).

## Not verified locally

- The pinned Go test target is blocked on this host by the existing
  `google.golang.org/grpc/internal/transport` reference to missing
  `http2.TrailerPrefix`. The package has no failpoint usage.
- No real process-memory-pressure run was performed; the Rust test injects a
  deterministic successful probe into the same quota/guard policy.
