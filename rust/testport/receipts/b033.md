# Batch b033 — pinned root `pkg/tablecodec` → `tidb-tablecodec`

Go source of truth: commit
`e2788410d8d696605e8cb002585877a063ccc909`, root package
`pkg/tablecodec` only.

## Atomic source inventory

- `BUILD.bazel`
- `OWNERS`
- `tablecodec.go`
- `tablecodec_test.go`
- `bench_test.go`
- `main_test.go`

The nested `pkg/tablecodec/rowindexcodec` directory is a separate Go package
and is not included in this receipt.

## Rust ownership

- Production behavior: `rust/crates/tidb-tablecodec/src/table_index.rs`,
  `table_row.rs`, and `table_key.rs`, with handle representation in
  `rust/crates/tidb-txnkv/src/handle.rs`.
- Root-package tests and benchmarks:
  `rust/crates/tidb-tablecodec/tests/tablecodec_package_source.rs`,
  `tablecodec_bench_source.rs`, and `index_prefix_truncation_go_vectors.rs`.
- Go-generated prefix fixtures:
  `rust/crates/tidb-tablecodec/tests/fixtures/index_prefix_truncation.hex`.

All 26 Go test functions (including `TestMain`) and nine Go benchmark functions
have executable Rust coverage. The package suite also pins behavior that
self-round-trip tests cannot distinguish: invalid UTF-8 prefix counting,
Bytes-to-String conversion, V0 single-integer common handles, V1
binary-collation restored padding, and handle-less extensible values.

## Parity corrections

- Removed Rust-only tablecodec wrapper APIs and constants.
- Restored Go's unchecked malformed-input behavior and slice arity rules.
- Restored Go's old-row time, duration, BIT, row-portal, and unflatten behavior.
- Restored Go's V0/V1 handle decoding and nullable handle result contract.
- Restored full-column rowcodec offsets and mem-comparable binary padding for
  clustered-index restored values.
- Restored Go rune decoding, charset source, and string-datum conversion for
  prefix indexes.

## WIP validation

```text
cargo test -p tidb-tablecodec --locked --test all -- --test-threads=1
# 55 passed; 0 failed; 0 ignored

cargo check -p tidb-tablecodec --all-targets --locked
cargo check -p tidb-executor --lib --locked
git diff --check
```

The checks emit pre-existing warnings in shared dependencies but no errors.
Ready-profile repository lint and broad workspace tests were not run because
the repository-wide parity audit remains in progress.
