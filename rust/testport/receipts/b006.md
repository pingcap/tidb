# Batch b006 — pkg/meta/metadef → tidb-metadef

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| `pkg/meta/metadef/db_test.go` `TestIsMemDB` | `tests_metadef::is_mem_db_go_test` | ported |
| `pkg/meta/metadef/db_test.go` `TestIsSystemRelatedDB` | `tests_metadef::is_system_related_db_go_test` | ported |
| `pkg/meta/metadef/db_test.go` `TestIsSystemDB` | `tests_metadef::is_system_db_go_test` | ported |
| `pkg/meta/metadef/system_test.go` `TestIsReservedID` | `tests_metadef::is_reserved_id_go_test` | ported |

No go-parity gaps: all four predicates (`is_mem_db`, `is_system_related_db`,
`is_system_db`, `is_reserved_id`) and the reserved-ID bounds exist in the crate
with matching semantics (re-derived against
`git show origin/master:pkg/meta/metadef/db.go` / `system.go`).

## Baseline

Baseline on clean tree: 7 tests run, 7 passed, 0 failed — empty failure set.

## Commands

- Baseline: `cargo nextest run --locked -p tidb-metadef --no-fail-fast`
  → `Summary [   0.011s] 7 tests run: 7 passed, 0 skipped`
- After edits (identical gate):
  `cargo nextest run --locked -p tidb-metadef --no-fail-fast`
  → `Summary [   0.017s] 11 tests run: 11 passed, 0 skipped`
- `git diff --check` → clean.
- Re-verification on a later tree (still clean, HEAD includes subsequent
  batches): identical gate → `Summary [  0.035s] 19 tests run: 19 passed,
  0 skipped`. Failure set remains empty; subset property trivially holds.

baseline-failing: (none)

## Not verified locally

Nothing outstanding; no golden files were needed. Note the crate already had
inline equivalents of these cases in `src/db.rs` / `src/system.rs`; the ported
module pins the Go test functions by name without touching those.
