# Batch b004 — port `pkg/util/hack` unit tests to `tidb-hack`

Go packages read from `origin/master`: `pkg/util/hack`
(`hack_test.go`, `map_abi_test.go`, `main_test.go`).
Owning Rust crate: `tidb-hack`. New module:
`rust/crates/tidb-hack/src/tests_hack.rs` (registered as `#[cfg(test)] mod tests_hack;`
in `rust/crates/tidb-hack/src/lib.rs`). No testdata/golden files needed.

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| hack_test.go: TestString | `hack_string_observes_mutation_and_survives_append` | ported |
| hack_test.go: TestByte | `hack_slice_converts_string_to_bytes` | ported (read-only view; Go's mutable alias of immutable string storage is unsound in Rust, matching the crate's documented disposition) |
| hack_test.go: TestMutable | `hack_mutable_string_changes_after_source_is_modified` | ported |
| map_abi_test.go: TestSwissTable (geometry + seeded-map blocks) | `hack_swiss_table_geometry_and_mem_aware_accounting` | ported |
| map_abi_test.go: TestSwissTable (small-map growth block) | `hack_swiss_table_small_map_growth_threshold` | ported-with-gap (see below) |
| map_abi_test.go: TestSwissTable (MemAwareMap delta/clear/SetExt block) | `hack_swiss_table_mem_aware_delta_clear_and_set_ext` | ported |
| map_abi_test.go: MockSeedForTest-on-non-empty panic | `hack_mock_seed_rejects_a_used_map` | ported |
| map_abi_test.go: exact Go ABI byte totals (`GroupSize` 136/200/264, `Size()` 184/360/102608, `RealBytes()` 2165296, `delta` 2702278, `dirLen`) | — | ignored-gap: go-parity-gap: values come from Go's private runtime Swiss-map layout; the Rust transcreation pins hashbrown's layout, so those literals are not reproducible. Layout-independent counterparts are asserted instead. No `#[ignore]` stub emitted because no partial test could pass meaningfully; covered by the portable tests above. |
| map_abi_test.go: private directory/group slot probing (`testMapTable`, `directoryAt`, `ref.cap/key/elem`) | — | skipped-reason: relies on Go-internal runtime types with no Rust counterpart; the equivalent observable behavior (key 1234 visible, used count) is asserted via public `iter()`/`len()`. |
| map_abi_test.go: BenchmarkMemAwareIntMap / BenchmarkNativeIntMap | `benches/hack.rs` (pre-existing per crate docs) | skipped-reason: benchmarks, not unit tests |
| main_test.go: TestMain (goleak setup) | — | skipped-reason: crate starts no background workers; documented in lib.rs source disposition |

## Baseline failure set (clean tree)

None. Baseline run passed all 6 pre-existing tests.

## Commands and summaries

```
cargo nextest run --locked -p tidb-hack --no-fail-fast   # baseline
#   Summary [   0.043s] 6 tests run: 6 passed, 0 skipped
cargo nextest run --locked -p tidb-hack --no-fail-fast   # after edits
#   Summary [   0.052s] 13 tests run: 13 passed, 0 skipped
git diff --check                                         # clean
```

Failure set is a subset of baseline (both empty). No pre-existing failures
disappeared or appeared.

## Not verified locally

- Go-side execution of `origin/master` tests (read-only source snapshot).
- jemalloc-feature build of the crate (`allocator_stats` module untouched by
  this batch).
