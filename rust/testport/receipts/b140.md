# testport batch b140 — `pkg/executor.part22` → `tidb-executor`

Gate command (run from `rust/`):

```text
cargo nextest run --locked -p tidb-executor -E 'not test(/bench/)' --no-fail-fast
```

## Scope

Go code: `pkg/executor` part 22, items 1261–1320 of the deterministic
`func Test*`/`func Benchmark*` enumeration over `pkg/executor/*_test.go`,
recursive excluding `pkg/executor/internal/`, sorted by (file path, line
number). The local snapshot contains 1326 top-level/receiver matches, while
the assigned manifest has 1389 total items; the b140 boundary is stable from
the manifest and the local anchors:

- item 1261: `pkg/executor/test/unstabletest/memory_test.go:105::TestPBMemoryLeak`
- item 1262: `pkg/executor/test/writetest/main_test.go:27::TestMain`
- item 1308: `pkg/executor/write_concurrent_test.go:25::TestBatchInsertWithOnDuplicate`
- item 1309: `pkg/executor/test/loadremotetest/error_test.go:36::TestErrorMessage`
- item 1320: `pkg/executor/test/loadremotetest/one_csv_test.go:129::TestIgnoreNLines`

Thus this batch contains the 48 local top-level entries from
`TestPBMemoryLeak` through `TestBatchInsertWithOnDuplicate`, followed by the
first 12 remote-load suite methods. The later remote-load methods belong to
b141/b142.

## Files changed

- `rust/crates/tidb-executor/src/tests_executor_part22_source.rs`: 60 Rust
  test rows, each mapped to one Go source test/benchmark.
- `rust/crates/tidb-executor/src/lib.rs`: one `#[cfg(test)] mod` registration.
- `rust/testport/receipts/b140.md`: this receipt.

No Cargo files, production implementation files, fixtures, generated files,
or Go files changed.

## Port status

The module has exactly 60 `#[test]` functions: 3 running and 57 explicit
`#[ignore]` rows. The running rows use the crate-owned in-memory catalog and
executor surfaces:

| Go test | Rust test | Status |
| --- | --- | --- |
| `pkg/executor/test/writetest/write_test.go:42::TestInsertIgnore` | `tests_executor_part22_source::insert_ignore_keeps_the_nonduplicate_rows` | running; verifies duplicate-key ignore leaves the original row and inserts the non-conflicting row |
| `pkg/executor/utils_test.go:184::TestWorkerPool` | `tests_executor_part22_source::worker_pool_preserves_the_go_submission_contract` | running; verifies concurrent task execution preserves indexed submission order |
| `pkg/executor/windows/window_executor_test.go:38::TestWindowExecutorsBasic` | `tests_executor_part22_source::window_executor_basic_row_number_and_sum_match_go` | running; verifies partitioned `ROW_NUMBER()` over the in-memory SQL gateway |

The 57 ignored rows are not approximations. Their `#[ignore]` messages record
the concrete missing boundary:

- `TestPBMemoryLeak`: Go runtime GC/`runtime.MemStats` and allocation checks.
- `TestMain`: Go suite bootstrap only.
- `TestLoadDataMissingColumn`, `TestIssue18681`, `TestIssue34358`: local
  `LOAD DATA`, reader injection, importer conversions, and warning accounting.
- `TestLatch`, `TestDeferConstraintCheckForInsert`,
  `TestPessimisticDeleteYourWrites`, `TestPessimisticUpdatePKLazyCheck`,
  `TestLockUnchangedUniqueKeys`, `TestLockUnchangedKeysGlobalIndex`,
  `TestUpdateRowRetryAndThenDupKey`, and `TestBatchInsertWithOnDuplicate`:
  multi-session transactions, pessimistic locks, failpoint/stepped execution,
  session variables, or async sessions.
- `TestReplaceLog`, `TestRebaseIfNeeded`, `TestUpdateWithOnUpdateAndAutoGenerated`:
  raw storage/index repair, direct table APIs, auto-ID/session clocks, or
  generated-column/index consistency.
- `TestTikvRegionPeers`: PD/TiKV HTTP and infoschema region metadata.
- `TestTraceExec`: session trace spans and trace result formatting.
- `TestTrafficForm`, `TestTrafficError`, `TestCapturePath`, `TestReplayPath`,
  `TestTrafficShow`, `TestTrafficPrivilege`: TiProxy, external storage, and
  privilege-manager surfaces.
- `TestUnionScanForMemBufferReader`, `TestIssue53951`, `TestIssue28073`,
  `TestIssue32422`, `TestSnapshotWithConcurrentWrite`: transaction mem-buffer,
  cached-table, partition-MVCC, and raw-key inspection behavior.
- `BenchmarkUnionScanRead`, `BenchmarkUnionScanIndexReadDescRead`,
  `BenchmarkUnionScanTableReadDescRead`, `BenchmarkUnionScanIndexLookUpDescRead`,
  and `TestBenchDaily`: benchmark-only or benchmark-bootstrap rows.
- `TestBatchRetrieverHelper`, `TestEqualDatumsAsBinary`,
  `TestEncodePasswordWithPlugin`, and `TestEncodedPassword`: Go-private helper
  functions with no Rust public seam at this crate boundary.
- `TestBuildOrderedWindowExec` and `TestWindowReturnColumnNullableAttribute`:
  Go private mock-executor construction or session result-field metadata.
- `TestWindowFunctions`, `TestWindowFunctionsDataReference`,
  `TestSlidingWindowFunctions`, `TestIssue45964And46050`, and
  `TestVarSampAsAWindowFunction`: full session window-variable, prepared-frame,
  chunk-boundary, precision, and result-metadata surfaces.
- `TestErrorMessage`, `TestColumnNumMismatch`, `TestEvalError`, `TestDataError`,
  `TestIssue43555`, `TestFilenameAsterisk`, `TestLastInsertID`,
  `TestMultiBatchWithIgnoreLines`, `TestMixedCompression`,
  `TestLoadCSV`, `TestLoadCsvInTransaction`, and `TestIgnoreNLines`: fake-GCS,
  remote readers, CSV/importer row loops, compression, wildcard expansion,
  transaction rollback, and LOAD DATA warning/row-counter behavior.

## Validation

Formatting and whitespace:

```text
rustfmt --edition 2024 --config skip_children=true \
  --check rust/crates/tidb-executor/src/tests_executor_part22_source.rs
→ passed

git diff --check
→ passed
```

Library compilation:

```text
cd rust && cargo check --locked -p tidb-executor --lib
→ passed
```

The focused b140 test command was run with `/usr/local/bin` prepended to
`PATH` so the available protobuf compiler was selected:

```text
cd rust && PATH=/usr/local/bin:$PATH \
  cargo nextest run --locked -p tidb-executor \
  -E 'test(tests_executor_part22_source)' --no-fail-fast
→ Summary: 3 tests run: 3 passed, 2784 skipped
```

The full scoped gate was run with the same temporary compatibility substitutions
used only during validation:

- `tidb_chunk::Chunk` → `tidb_chunk::chunk::Chunk` in the existing
  `tests_set_show_stats_slow_query_source.rs` test module.
- `.gen()` → `.r#gen()` in the existing
  `driver/tests/column_defaults.rs` test module.

Both files were restored exactly to `HEAD` after the command. The gate result
was:

```text
cd rust && PATH=/usr/local/bin:$PATH \
  cargo nextest run --locked -p tidb-executor \
  -E 'not test(/bench/)' --no-fail-fast
→ Summary: 1550 tests run: 1502 passed (1 slow), 48 failed, 1237 skipped
```

The 48 failures are the established executor baseline, including the known
DDL unique-null clustered-index inconsistency; no b140 test failed. The b140
module's 57 ignored tests account for the intentionally unexecuted gaps.

## Validation profile and risks

- Profile: `Ready` for the scoped Rust crate. The changed code is a test
  module plus registration; the required scoped compile, focused tests,
  formatting, diff check, and full crate gate were run. `make lint` was
  attempted, but the environment has no Go binary (`make: go: No such file or
  directory`; `Makefile.common` also cannot evaluate `go env GOPATH`). The full
  Rust gate was still executed with the two temporary compatibility
  substitutions restored afterward.
- Correctness risk: the 3 running tests cover only the executor surfaces that
  are actually reachable from this crate. The 57 ignored rows intentionally do
  not claim behavior for missing session, storage, remote-load, or service
  layers.
- Compatibility risk: no production behavior or dependency/configuration was
  changed. The receipt documents the two measured Rust test-build compatibility
  substitutions; neither remains in the tree.
- Performance risk: no production hot path changed. Worker-pool validation uses
  small deterministic tasks only.

Not verified locally: the Go tests themselves were not run; the ignored Go
integration/benchmark behaviors require the Go session/storage/service stack;
repository-wide lint and unrelated Rust test-build cleanup were not performed.
