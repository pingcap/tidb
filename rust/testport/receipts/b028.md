# b028 — port `pkg/util/schemacmp` unit tests (Go → Rust)

Owning crate: `tidb-schemacmp`. Go tests read from `origin/master`
(`git show origin/master:pkg/util/schemacmp/<file>_test.go`), not the branch copy.

## Finding

All 9 Go test functions were already ported 1:1 in
`rust/crates/tidb-schemacmp/tests/{charset_collation,lattice,table,type}_test.rs`
(aggregate root `tests/all.rs` via `rust/scripts/aggregate-tests.rs`). This batch
re-verified each port against the origin/master Go source line-by-line:

- case-for-case correspondence of every table-driven case (all 48 active
  `TestJoinSchemas` cases incl. `latin1_to_utf8mb4`; `DM_074` and `DM_086` are
  commented out in the Go test itself and likewise absent in Rust);
- identical error regexps (`distinct singletons.*`,
  `at tuple index \d+: incompatible charset.*`, etc.);
- both compare directions (`a<b`, `b<a`) plus join/compare-back assertions;
- identical golden strings in `TestTableString`.

No code or test changes were needed; the only artifact added is this receipt.

## Checklist

| Go test (origin/master) | Rust test | Status |
| --- | --- | --- |
| `charset_collation_test.go: TestCharsetCompare` | `charset_collation_test::test_charset_compare` | ported |
| `charset_collation_test.go: TestCollationCompare` | `charset_collation_test::test_collation_compare` | ported |
| `charset_collation_test.go: TestCharsetJoin` | `charset_collation_test::test_charset_join` | ported |
| `charset_collation_test.go: TestCollationJoin` | `charset_collation_test::test_collation_join` | ported |
| `lattice_test.go: TestCompatibilities` | `lattice_test::test_compatibilities` | ported |
| `table_test.go: TestJoinSchemas` | `table_test::test_join_schemas` | ported |
| `table_test.go: TestTableString` | `table_test::test_table_string` | ported |
| `type_test.go: TestTypeUnwrap` | `type_test::test_type_unwrap` | ported |
| `type_test.go: TestTypeCompareJoin` | `type_test::test_type_compare_join` | ported |

No ignored tests; no go-parity gaps.

## Baseline

Baseline gate run on the clean tree before any edit:

```
cargo nextest run --locked -p tidb-schemacmp -E 'not test(/bench/)' --no-fail-fast
```

Result: `9 tests run: 9 passed, 0 skipped`. Failure set is empty — no
`baseline-failing:` lines.

## Commands run

```
cargo nextest run --locked -p tidb-schemacmp -E 'not test(/bench/)' --no-fail-fast
```

- Pre-edit (clean tree): `Summary [   0.307s] 9 tests run: 9 passed, 0 skipped`
- Post-edit (receipt only): `Summary [   0.467s] 9 tests run: 9 passed, 0 skipped`
- `git diff --check`: clean.

## Not verified locally

- Nothing in scope. The Go tests themselves were not executed on this machine;
  their expectations were taken from the `origin/master` snapshot text.
