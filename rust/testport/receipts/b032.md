# b032 — port `pkg/parser/charset` unit tests to `tidb-datatype`

Go packages: `pkg/parser/charset` (charset_test.go, encoding_test.go), read from
`origin/master`.

Rust home: the crate's existing aggregate integration-test binary
(`tests/all.rs`, auto-registered by `rust/scripts/aggregate-tests.rs`). The
parser/charset package tests already lived in
`rust/crates/tidb-datatype/tests/parser_charset_package_source.rs`; this batch
extended that file to full Go coverage instead of creating parallel modules.

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| charset_test.go::TestValidCharset | `test_valid_charset` | ported (pre-existing, verified against master, all 17 rows present) |
| charset_test.go::TestGetDefaultCollation | `test_get_default_collation` | ported (incl. collations-table/charset-desc consistency loop; pre-switch registry state entered deliberately — see file comment) |
| charset_test.go::TestGetCharsetDesc | `test_get_charset_desc` | ported |
| charset_test.go::TestGetCollationByName | `test_get_collation_by_name` | extended this batch: now iterates ALL supported collations with full-struct equality + unknown-name error text, matching Go |
| charset_test.go::TestValidCustomCharset | `test_valid_custom_charset` | ported |
| charset_test.go::TestUTF8MB3 | `test_utf8mb3` | ported |
| charset_test.go::BenchmarkGetCharsetDesc | `benchmark_get_charset_desc_obligation_executes_one_lookup` | ported as a smoke test (benchmarks are not translated) |
| encoding_test.go::TestEncoding | `test_encoding` | ported; all 15 GBK decode-replace rows and 7 encode-replace rows. Note: Go builds reference GBK bytes via `x/text` `charset.Lookup("gbk")`; Rust round-trips through its own ENCODE since no `x/text` dependency exists |
| encoding_test.go::TestEncodingValidate | `test_encoding_validate` | extended this batch from 11 to all 35 Go rows (ascii/utf8mb4/utf8/utf8mb3-strict/gbk/gb18030, incl. `\xff\xfe\xfd` and U+FFFD cases); nSrc values are not asserted by Go either |
| encoding_test.go::TestEncodingGB18030 | `test_encoding_gb18030` | extended this batch with missing DECODE_REPLACE rows (`一二三123`, `案1案2`, `焊䏷菡釬`, `鞍杏以伊位依`, `佝佗…`, `\x80aa`, `a你好\x80a测试`, `aa\x80`, `\xb0\xb2`); all 7 ENCODE_REPLACE rows already present |

No go-parity gaps requiring `#[ignore]`.

## Baseline failures

None — clean-tree gate passed fully:

```
baseline-failing: (none)
```

## Commands run

1. Baseline (before edits):
   `cargo nextest run --locked -p tidb-datatype --no-fail-fast`
   → Summary: 418 tests run: 418 passed, 0 skipped.
2. Scoped during iteration:
   `cargo nextest run --locked -p tidb-datatype --no-fail-fast parser_charset`
   → 10 tests run: 10 passed.
3. Final gate (after edits):
   `cargo nextest run --locked -p tidb-datatype --no-fail-fast`
   → Summary: 418 tests run: 418 passed, 0 skipped.
   Failure set (empty) is a subset of baseline (empty). No new failures; no
   pre-existing failure disappeared.
4. `git diff --check` → clean.

## Not verified locally

- Nothing in scope. Go tests were read from `origin/master`; the Rust side was
  validated only through the crate's own transform implementations (the Go
  tests' `x/text` cross-validation of GBK/GB18030 byte sequences is approximated
  by golden expected bytes taken verbatim from the Go test source).
