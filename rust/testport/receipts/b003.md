# b003 — port `pkg/parser/terror` unit tests to `tidb-error`

Batch: b003
Go package: `pkg/parser/terror` (tests read from `origin/master`)
Owning Rust crate: `tidb-error`
Branch: (current worktree branch, committed, not pushed)

## Checklist

| Go test (`terror_test.go`) | Rust test | Status |
| --- | --- | --- |
| `TestErrCode` | `tidb-error::all::tests_terror::err_code_preserves_sentinel_values` | ported |
| `TestTError` | `tidb-error::all::tests_terror::terror_class_membership_identity_and_sql_conversion` | ported |
| `TestJson` | `tidb-error::all::tests_terror::json_round_trip_preserves_compatible_error_identity` | ported |
| `TestErrorEqual` | `tidb-error::all::tests_terror::error_equal_follows_causes_and_rfc_identity` | ported |
| `TestLog` | `tidb-error::all::tests_terror::log_accepts_an_error_without_panicking` | ported |
| `TestTraceAndLocation` | `tidb-error::all::tests_terror::trace_and_location_names_the_test_source_file` | ported (partial: Go's GOROOT frame line-count arithmetic is Go-runtime specific and intentionally omitted; the file-name-in-stack assertion is kept) |

No `go-parity-gap` ignores were needed.

## Baseline failures

Baseline run on the clean tree before any edit:

```
baseline-failing:
```

(empty — all 37 pre-existing tests passed)

## Commands run

From `rust/`:

```
cargo nextest run --locked -p tidb-error --no-fail-fast   # baseline: 37 tests run: 37 passed, 0 skipped
cargo nextest run --locked -p tidb-error --no-fail-fast   # after edits: 43 tests run: 43 passed, 0 skipped
git diff --check                                          # clean
```

Failure set after edits (empty) is a subset of baseline (empty).

## Notes / not verified locally

- The Go test file has exactly 6 test functions; all 6 are covered above.
- `TestTraceAndLocation`'s exact stack-frame count assertion (9 lines minus
  GOROOT frames) depends on the Go runtime/panic backtrace format; the Rust
  port asserts the equivalent intent (stack captured and names this source
  file) instead of the runtime-specific arithmetic.
- Nothing else was left unverified locally.
