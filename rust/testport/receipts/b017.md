# b017 — port `pkg/util/chunk` (part 3) unit tests to Rust (`tidb-chunk`)

Go source of truth: `origin/master:pkg/util/chunk/{chunk_test.go,column_test.go,pool_test.go,row_container_test.go,row_container_reader_test coverage in row_container_test.go,allocator_test.go}`.

Part 3 = the alphabetically-last third of the package's 100 test functions
(`TestPanicDuringSortedRowContainerSpill` … `TestVectorizedNulls`, 33 tests).
The Rust transcreation of `pkg/util/chunk` lives in `rust/crates/tidb-chunk`
(the task's "owning crate: tidb-util" refers to its dependency root;
the chunk tests are owned by `tidb-chunk`, which is also gated below).

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| TestPanicDuringSortedRowContainerSpill | `sorted_row_container::tests::go_test_panic_during_sorted_row_container_spill` | ported (pre-existing, verified against origin/master) |
| TestPanicWhenSpillToDisk | `row_container_tests::disk_quota_failure_is_sticky_for_reads_and_later_adds` | ported (pre-existing; uses a real quota-limited storage authority instead of Go's failpoint) |
| TestPoolGetChunk | `pool::tests::go_test_pool_get_chunk` | ported (pre-existing, verified) |
| TestPoolPutChunk | `pool::tests::put_clears_chunk_and_reuses_reset_columns` | ported (pre-existing, cites Go TestPoolPutChunk; asserts columns cleared + reset-column reuse) |
| TestPreAllocDecimal | `column_tests::go_test_pre_alloc_decimal` | ported (pre-existing, verified) |
| TestPreAllocFloat32 | `column_tests::go_test_pre_alloc_float32` | ported (pre-existing, verified) |
| TestPreAllocFloat64 | `column_tests::go_test_pre_alloc_float64` | ported (pre-existing, verified) |
| TestPreAllocInt64 | `column_tests::go_test_pre_alloc_int64` | ported (pre-existing, verified) |
| TestPreAllocTime | `column_tests::go_test_pre_alloc_time` | ported (pre-existing, verified) |
| TestPreAllocUint64 | `column_tests::go_test_pre_alloc_uint64` | ported (pre-existing, verified) |
| TestReadAfterSpillWithRowContainerReader | `row_container_reader::tests::the_reader_crosses_a_mid_read_spill` | ported (pre-existing, verified) |
| TestReconstructFixedLen | `column_tests::reconstruct_fixed_len` | ported (pre-existing, same rand.Int63-derived pattern as Go column_test.go:432) |
| TestReconstructVarLen | `column_tests::reconstruct_var_len` | ported (pre-existing, verified) |
| TestReserve | `column_tests::go_test_reserve` | ported (pre-existing, verified) |
| TestResetColumn | `column_tests::go_test_reset_column` | ported (pre-existing, verified) |
| TestResize | `column_tests::go_test_resize` | ported (pre-existing, verified) |
| TestResizeReserve | `column_tests::go_test_resize_reserve` | ported (pre-existing, verified) |
| TestReuseHookAllocator | `alloc::tests::reuse_hook_runs_once_only_when_reuse_is_configured` | ported (pre-existing, verified against alloc_test.go:295) |
| TestRowContainerReaderInDisk | `row_container_reader::tests::the_reader_walks_a_spilled_container` | ported (pre-existing, verified) |
| TestRowContainerResetAndAction | `row_container_tests::a_reset_container_spills_again` | ported (pre-existing, verified) |
| TestSel | `row_container_tests::a_selection_vector_survives_the_spill` | ported (pre-existing, verified — Go TestSel is in row_container_test.go:40) |
| TestSetColumn | `column_tests::go_test_set_column` | ported (pre-existing, verified) |
| TestSetNulls | `column_tests::go_test_set_nulls` | ported (pre-existing, verified) |
| TestSortedRowContainerSortSpillAction | `sorted_row_container::tests::go_test_sorted_row_container_sort_spill_action` | ported (pre-existing, verified) |
| TestSpillAction | `row_container_tests::the_spill_action_moves_the_container_to_disk` | ported (pre-existing, verified) |
| TestSpillActionDeadLock | `row_container_tests::a_concurrent_action_and_add_do_not_deadlock` | ported (pre-existing, verified) |
| TestStringColumn | `column_tests::go_test_string_column` | ported (pre-existing, verified) |
| TestSwapColumn | `chunk::tests::go_test_swap_column` | ported (pre-existing, verified) |
| TestSyncAllocator | `alloc::tests::sync_allocator_is_thread_safe` | ported (pre-existing; reduced worker/loop counts vs Go's 1000×10×100, same lock-boundary contract) |
| TestTimeColumn | `column_tests::go_test_time_column` | ported (pre-existing, verified) |
| TestToString | `chunk::tests::go_test_to_string` | **ported in this batch** (`rust/crates/tidb-chunk/src/chunk.rs`) — was missing entirely |
| TestTruncateTo | `chunk::tests::go_test_truncate_to` | ported (pre-existing, verified) |
| TestVectorizedNulls | `column_tests::go_test_vectorized_nulls` | ported (pre-existing, verified) |

Notes:
- Every pre-existing Rust test listed above was re-derived this batch against
  `git show origin/master:...`; each keeps Go's inputs and expected values
  (or an explicitly documented deterministic substitute for Go randomness /
  failpoints / goroutine timing). No approximation was introduced.
- `TestMain` is testkit bootstrap only — nothing to port.
- The only true gap found was `TestToString`: `Chunk::to_string` existed with
  no test pinning it. Ported verbatim from chunk_test.go:711 including the
  zero-DATE / zero-DATETIME renderings.

## Baseline failures

Recorded on the clean tree before editing (stashed), both gates:

```
baseline-failing: tidb-util::memory::arbitrator::tests::bench::bench
```

(`cargo nextest run --locked -p tidb-util --no-fail-fast`:
700 tests run: 699 passed, 1 failed, 1 skipped.
`cargo nextest run --locked -p tidb-chunk --no-fail-fast`:
295 tests run: 295 passed.)

## Commands run

```
export PROTOC=/tmp/pw/protoc/data/bin/protoc   # build prerequisite for tidb-proto build.rs
cargo nextest run --locked -p tidb-util --no-fail-fast    # baseline: 699 passed, 1 failed (arbitrator bench)
cargo nextest run --locked -p tidb-chunk --no-fail-fast   # baseline: 295 passed
cargo nextest run -p tidb-chunk go_test_to_string          # new test: 1 passed
cargo nextest run --locked -p tidb-chunk --no-fail-fast   # after edits: 296 tests run: 296 passed, 0 skipped
cargo nextest run --locked -p tidb-util --no-fail-fast    # after edits: 700 tests run: 699 passed, 1 failed, 1 skipped (same single baseline failure)
git diff --check                                           # clean
```

Failure-set subset property holds: post-edit failures == baseline failures
(the pre-existing `tidb-util memory::arbitrator::tests::bench::bench`,
untouched by this batch).

## Not verified locally

- `tidb-util::memory::arbitrator::tests::bench::bench` fails on the clean tree
  too; it is outside batch scope and was not investigated.
- Go's goroutine-timing aspects (TestSyncAllocator scale,
  TestConcurrentSpillWithRowContainerReader overlap window) are pinned by
  deterministic equivalents, not wall-clock reproduction of Go's timing.
