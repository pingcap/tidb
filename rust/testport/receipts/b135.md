# testport batch b135 — `pkg/executor.part17` → `tidb-executor`

Gate command (as assigned, run from `rust/`):

```
cargo nextest run --locked -p tidb-executor -E 'not test(/bench/)' --no-fail-fast
```

Go code: `pkg/executor` part 17 = items 961–1020 of the deterministic
`func Test*`/`func Benchmark*`/`func Fuzz*` enumeration over
`pkg/executor/*_test.go`, recursively excluding `pkg/executor/internal/`,
sorted by `(path, line number)`. The neighboring receipts pin item 960 at
`pkg/executor/test/infoschema/infoschema_test.go:960::TestInfoschemaTablesSpecialOptimizationCovered`
and item 1021 at
`pkg/executor/test/jointest/join_test.go:880::TestIssue49033`; this batch's
bounds and local enumeration are:

- item 961: `infoschema/infoschema_test.go:960::TestInfoschemaTablesSpecialOptimizationCovered`
- item 962: `infoschema/infoschema_test.go:995::TestInfoSchemaExcludeNonPublicColumns`
- item 966: `infoschema/main_test.go:26::TestMain`
- item 967: `issuetest/executor_issue_test.go:43::TestIssue24210`
- item 992: `issuetest/main_test.go:26::TestMain`
- item 993: `jointest/hashjoin/hash_join_test.go:32::TestIndexNestedLoopHashJoin`
- item 1014: `jointest/hashjoin/main_test.go:26::TestMain`
- item 1015: `jointest/join_test.go:32::TestJoin2`
- item 1020: `jointest/join_test.go:753::TestIssue30211`
- item 1021: `jointest/join_test.go:880::TestIssue49033`

Item 961 is already covered by `tests_infoschema_b134_source`; b135 adds
items 962–1020. The owning crate is `rust/crates/tidb-executor`.

## Files changed

- `rust/crates/tidb-executor/src/tests_infoschema_b135_source.rs`
- `rust/crates/tidb-executor/src/tests_issuetest_b135_source.rs`
- `rust/crates/tidb-executor/src/tests_jointest_hashjoin_b135_source.rs`
- `rust/crates/tidb-executor/src/tests_jointest_join_b135_source.rs`
- `rust/crates/tidb-executor/src/lib.rs`
- `rust/testport/receipts/b135.md`

No production behavior or dependencies were changed.

## Port status

The batch has one compiled source-mapped function for each assigned Go
function, with additional split rows where one Go test contains both a
runnable SQL assertion and an unavailable session/worker assertion.

Running SQL-result carriers cover:

- UNION type/NULL and BIT row behavior from `TestUnionIssue`;
- blob-prefix LIKE matching (`TestIssue50393`);
- constant DOUBLE/TRUNCATE/MIN behavior (`TestIssue52978`);
- preserved-side outer hash-join rows (`TestOuterTableBuildHashTableIsuse13933`);
- build-side projection after a residual (`TestInlineProjection4HashJoinIssue15316`);
- outer-join NULL preservation (`TestIssue54755`);
- CHAR/VARCHAR equality (`TestIssue55016`);
- outer residual joins (`TestIssue56825`); and
- deterministic outer/duplicate join rows from `TestJoin2`.

The remaining assigned cases are explicit `#[ignore]` tests with concrete
reasons. They require one or more of information_schema retrievers, DDL
schema-state callbacks, statistics usage accounting, keyspace metadata,
failpoints, EXPLAIN ANALYZE, runtime statistics, session variables, prepared
statements, pessimistic/MVCC transactions, table cache, server memory
management, query OOM actions, or concurrent executor/result-set workers.
Those surfaces are not reachable from the owning catalog-only executor crate
and are not approximated.

## Risks

- Correctness: running tests pin data-level SQL results only; they do not
  claim the Go physical strategy, failpoint, runtime-statistics, or session
  lifecycle behavior.
- Compatibility: only `#[cfg(test)]` modules and the receipt were changed;
  no production API, dependency, or SQL behavior changed.
- Performance: the running fixtures are small and deterministic. The large
  Go stress/concurrency fixtures remain ignored rather than recreated with a
  weaker workload.

## Validation

Validation profile: **Ready attempted, with environment blockers**. The four
new source modules pass direct rustfmt, production compilation succeeds, and
all nine runnable b135 SQL-result tests pass when the unrelated pre-existing
`tidb_chunk::Chunk` import is temporarily corrected for the test build. The
normal test build cannot complete because that unrelated source file imports
`tidb_chunk::Chunk`, which is not re-exported by the current `tidb-chunk`
crate. The temporary compatibility edit was restored and is not part of this
batch.

Commands run and results:

- `cd rust && PATH=/opt/protoc/bin:$PATH cargo check --locked -p tidb-executor` — **passed**.
- `cd rust && rustfmt --edition 2024 --check crates/tidb-executor/src/tests_infoschema_b135_source.rs crates/tidb-executor/src/tests_issuetest_b135_source.rs crates/tidb-executor/src/tests_jointest_hashjoin_b135_source.rs crates/tidb-executor/src/tests_jointest_join_b135_source.rs` — **passed**.
- With only the unrelated import temporarily changed from
  `tidb_chunk::Chunk` to `tidb_chunk::chunk::Chunk`:
  `cd rust && PATH=/opt/protoc/bin:$PATH cargo nextest run --locked -p tidb-executor -E 'test(tests_infoschema_b135_source) or test(tests_issuetest_b135_source) or test(tests_jointest_hashjoin_b135_source) or test(tests_jointest_join_b135_source)' --no-fail-fast` — **9 passed, 0 failed, 2,779 filtered**.
- The same focused nextest command against the unmodified tree — **blocked at test compilation** by the pre-existing `tidb_chunk::Chunk` import in `tests_set_show_stats_slow_query_source.rs`.
- `make lint` — **blocked before linting** because `go` is unavailable and the Makefile requires `GOPATH`.

## Not verified locally

- Go test execution and Go-vs-Rust differential output;
- information_schema/DDL/statistics/keyspace behavior;
- failpoint, session-manager, prepared-statement, transaction/lock, OOM,
  EXPLAIN ANALYZE, table-cache, and concurrent worker contracts represented by
  ignored rows;
- the full unfiltered `cargo nextest run --locked -p tidb-executor -E 'not test(/bench/)' --no-fail-fast` gate, which cannot build until the unrelated `tidb_chunk::Chunk` import is fixed; and
- repository `make lint`, blocked by the missing Go/GOPATH environment.
