# b016 — port `pkg/util/chunk` unit tests, part 2 (tidb-chunk)

Go source of truth read from `origin/master`: `pkg/util/chunk/*_test.go`
(alloc_test.go, chunk_test.go, chunk_in_disk_test.go, chunk_util_test.go,
codec_test.go, column_test.go, iterator_test.go, list_test.go, mutrow_test.go,
pool_test.go, row_container_test.go, row_in_disk_test.go).

Note on scope metadata: the batch header named `tidb-util` as gate scope, but
the Rust transcreation of Go `pkg/util/chunk` lives in the `tidb-chunk` crate
(`rust/crates/tidb-chunk`); `tidb-util` contains no chunk module. All ports
were added to `tidb-chunk`, and BOTH gate commands were run.

Part-2 gap analysis: an inventory of every Go `func Test*` in
`pkg/util/chunk/*_test.go` against citations in `rust/crates/tidb-chunk/src/`
showed ~87 of ~90 test functions already ported by earlier batches (as
`go_test_*` / cited tests inside the crate's `#[cfg(test)]` modules and the
integration contract files under `tests/`). Three functions had no Rust port:

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| TestToString (chunk_test.go:711) | chunk::tests::go_test_to_string | ported (5-type chunk; zero-date vs zero-datetime rendering pinned byte-exactly) |
| TestColumnAllocatorCheck (alloc_test.go:276) | alloc::tests::go_test_column_allocator_check | ported ((10,20) bounds; 4 Float + 4 Datetime columns cached per fixed type size after reset; CONFIG_TEST_LOCK held because InitChunkAllocSize is process-global) |
| TestDataInDiskByChunks (chunk_in_disk_test.go:103) | chunk_in_disk::tests::go_test_data_in_disk_by_chunks | ported (both GetChunk and FillChunk paths over 100x1000 chunks with Go initChunks payload + addAuxDataForChunks aux data; aux round-trip asserted as in checkAuxDataForChunk, plus a strengthened physical-index payload comparison -- see comment in source re: Go's vacuous post-strip row loop) |
| all other Test* funcs in pkg/util/chunk/*_test.go | already present in tidb-chunk from earlier batches (verified by citation grep; spot-checked e.g. go_test_avoid_column_reuse, serialized_bytes_match_go, spill/reader suites) | skipped-reason: already ported before this batch |

Benchmarks (`func Benchmark*`) are not ported, consistent with prior batches.
Go `main_test.go` TestMain has no equivalent need.

## Baseline failures (clean tree, before edits)

First full run of the assigned gate passed clean:

baseline-failing: (none — `cargo nextest run --locked -p tidb-util --no-fail-fast`: 700 tests run: 700 passed, 1 skipped)

A later re-run of the same gate failed one unrelated, known-flaky test that
also fails on the STASHED CLEAN TREE (verified by `git stash` + isolated rerun):

baseline-failing: tidb-util memory::arbitrator::tests::bench::bench

(This is the same pre-existing flaky allocation-count assertion documented in
receipts b014; it is intermittent under load and is not affected by this batch,
whose edits are confined to `rust/crates/tidb-chunk/src/*.rs`. tidb-util does
not depend on tidb-chunk.)

## Post-edit gate

- `cargo nextest run --locked -p tidb-chunk --no-fail-fast`
  → Summary [8.529s] 298 tests run: 298 passed, 0 skipped
  (295 baseline + 3 new tests, all green)
- `cargo nextest run --locked -p tidb-util --no-fail-fast`
  → Summary [23.873s] 700 tests run: 699 passed, 1 failed, 1 skipped
  → failure set = {memory::arbitrator::tests::bench::bench}, which fails on the
  clean tree too (subset property holds; no new failures).
- `git diff --check` → clean

## Commands run

- `cargo nextest run --locked -p tidb-util --no-fail-fast` (baseline)
- `cargo nextest run --locked -p tidb-chunk --no-fail-fast` (baseline: 295 passed)
- `cargo nextest run --locked -p tidb-chunk --no-fail-fast` (post-edit)
- `cargo nextest run --locked -p tidb-util --no-fail-fast` (post-edit)
- `cargo nextest run --locked -p tidb-util memory::arbitrator::tests::bench`
  on stashed clean tree (flakiness confirmation)

All commands run from `/root/work/wt-b016/rust`.

## Not verified locally

- The flaky `memory::arbitrator::tests::bench::bench` failure was confirmed
  pre-existing but not root-caused or fixed (outside batch scope).
- The other ~87 already-ported chunk tests were verified by citation grep and
  spot-checks of assertions against master's Go source, not re-derived line by
  line in this batch.
