# Batch b026 — pkg/util/naming test port

Owning crate gate scope: `tidb-util` (naming lives in `tidb-naming`, re-exported
as `tidb_util::naming`; the ported tests live in `tidb-util/src/tests_naming.rs`
per the batch assignment).

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| `pkg/util/naming/naming_test.go` `TestScope` | `tests_naming::scope_check_accepts_only_alnum_hyphen_underscore_within_64_chars` | ported |

No golden files required. No `go-parity-gap`s: the Rust side (`check`,
`check_keyspace_name`, `check_with_max_len`) covers the full Go contract, so no
`#[ignore]` tests were needed.

## Baseline failures (clean tree, before edits)

baseline-failing: tidb-util::memory::arbitrator::tests::bench::bench

Notes: this failure is flaky — it passed on the first baseline run and failed on
the second; it is a pre-existing timing-sensitive benchmark-style test in
`memory::arbitrator` and is unrelated to naming.

## Commands run

From `/root/work/wt-b026/rust` (workspace root), with `PATH` including a local
`protoc` build (`/tmp/pw/protoc/data/bin`) required by `tidb-proto/build.rs`:

1. Baseline: `cargo nextest run --locked -p tidb-util --no-fail-fast`
   - run 1: `700 tests run: 700 passed, 1 skipped`
   - run 2: `700 tests run: 699 passed, 1 failed, 1 skipped`
     (`memory::arbitrator::tests::bench::bench`)
2. After edits (identical gate):
   `cargo nextest run --locked -p tidb-util --no-fail-fast`
   - `701 tests run: 700 passed, 1 failed, 1 skipped`
   - added test passes:
     `PASS tidb-util tests_naming::scope_check_accepts_only_alnum_hyphen_underscore_within_64_chars`
   - failure set = {pre-existing flaky `memory::arbitrator::tests::bench::bench`}
     ⊆ baseline. No new failures.
3. `git diff --check` — clean.

## Not verified locally

- Nothing for this batch's scope. The unrelated flaky
  `memory::arbitrator::tests::bench::bench` instability was not investigated
  (out of scope).
