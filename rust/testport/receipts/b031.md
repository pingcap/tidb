# b031 — port pkg/util/zeropool unit tests

Owning crate: `tidb-util`. Go source read from `origin/master`
(`pkg/util/zeropool/pool.go`, `pkg/util/zeropool/pool_test.go`).

The Rust implementation (`rust/crates/tidb-util/src/zeropool/mod.rs`) is a
value-moving pool (no Go pointer-pool / GC eviction semantics), so the two
`testing.AllocsPerRun` subtests are pinned by observable value-reuse identity
(pointer stability) instead of allocation counting; noted per test.

## Checklist

| Go test | Rust test | status |
| --- | --- | --- |
| TestPool/"provides correct values" | `tests_zeropool::pool_provides_correct_values` | ported |
| TestPool/"is not racy" | `tests_zeropool::pool_is_not_racy` | ported |
| TestPool/"does not allocate" | `tests_zeropool::pool_does_not_allocate_after_warmup` | ported (allocation-count bound approximated via pooled-value pointer-identity reuse check; no stable alloc counter in Rust tests) |
| TestPool/"zero value is valid" | `tests_zeropool::pool_zero_value_is_valid` | ported (same approximation as above) |
| BenchmarkZeropoolPool / BenchmarkSyncPool* | — | skipped-reason: benchmarks, not unit tests |

No go-parity gaps requiring `#[ignore]`.

## Baseline failures (clean tree, before edits)

None. Baseline run:

```
cargo nextest run --locked -p tidb-util --no-fail-fast
Summary [   9.034s] 709 tests run: 709 passed, 3 skipped
```

baseline-failing: (none)

## Validation

Commands run (from `/root/work/wt-b031/rust`, the workspace root):

1. Baseline gate: `cargo nextest run --locked -p tidb-util --no-fail-fast`
   → 709 passed, 3 skipped, 0 failed.
2. After edits, identical gate:
   → `Summary [   9.628s] 713 tests run: 713 passed, 3 skipped`
   All 4 added tests pass; failure set is trivially a subset of baseline (both empty).
3. `git diff --check` → clean.

## Not verified locally

- Race-detector behavior (`is_not_racy`) verified only without `-Zmiri`/TSan.
- Exact zero-allocation property not directly measured (see approximation note above).
