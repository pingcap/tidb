# testport batch b008 — pkg/meta/model.part2

Branch: `testport/pod2-b008`. Go tests read from `origin/master` snapshots.

## Crate-scope note (manifest discrepancy)

The batch manifest assigns `pkg/meta/model.part2` to crate `tidb-metadef`, but
`tidb-metadef` implements `pkg/meta/metadef` (system-database identifiers) and
does not depend on the model types. The `pkg/meta/model` transcreation lives in
**`tidb-model`**, so all ports were placed there. Both gates were run; see
below. No files outside `rust/crates/tidb-model/` and this receipt were touched.

## Checklist

| Go test (file:function) | Rust test | Status |
| --- | --- | --- |
| bdr_test.go:TestActionBDRMap | `bdr.rs::tests::action_bdr_map` (pre-existing inline port) | ported (pre-existing) |
| column_test.go:TestDefaultValue | `tests_pkg_meta_model_part2::column_default_value` (full port incl. JSON round-trip consistency matrix + NewExtraPhysTblIDColInfo) | ported |
| index_test.go:TestIsIndexPrefixCovered | `tests_pkg_meta_model_part2::index_is_index_prefix_covered` (incl. FK partial-index variants and FindIndexByColumnsForForeignKey pointer identity) | ported |
| index_test.go:TestGlobalIndexV1SupportedForNextGen | `tests_pkg_meta_model_part2::index_global_index_v1_supported_for_next_gen` | ported (guarded: assertion body only under a nextgen kernel build, mirroring Go's `nextgen` build tag; this workspace compiles Classic) |
| placement_test.go:TestPlacementSettingsString | `placement.rs::tests::placement_settings_string` (pre-existing inline port) | ported (pre-existing) |
| placement_test.go:TestPlacementSettingsClone | `placement.rs::tests::placement_settings_clone` (pre-existing inline port) | ported (pre-existing) |
| placement_test.go:TestPlacementPolicyClone | `placement.rs::tests::policy_clone` (pre-existing inline port) | ported (pre-existing) |
| table_mode_test.go:TestTableModeCanTransitionTo | `table_mode.rs::tests::can_transition_to` (pre-existing inline port) | ported (pre-existing) |
| table_test.go:TestMoveColumnInfo | `tests_pkg_meta_model_part2::table_move_column_info` (exact Go move sequence + checkOffsets helper) | ported |
| table_test.go:TestModelBasic | `tests_pkg_meta_model_part2::table_model_basic` | ported |
| table_test.go:TestTTLInfoClone | `tests_pkg_meta_model_part2::table_ttl_info_clone` | ported |
| table_test.go:TestTTLJobInterval | `tests_pkg_meta_model_part2::table_ttl_job_interval` | ported |
| table_test.go:TestClearReorgIntermediateInfo | `tests_pkg_meta_model_part2::clear_reorg_intermediate_info` | ported |
| table_test.go:TestTTLDefaultJobInterval | `tests_pkg_meta_model_part2::ttl_default_job_interval` | ported |

(`job_test.go` and `job_args_test.go` belong to part1/b007 and are already
ported in `src/job_tests.rs` / `src/job_args_tests.rs`; not in this batch.)

## Baseline failure sets

Recorded before any edit, on the clean tree:

- `cargo nextest run --locked -p tidb-metadef --no-fail-fast`: 7 tests run,
  7 passed — no baseline failures.
- `cargo nextest run --locked -p tidb-model --no-fail-fast`: 301 tests run,
  301 passed — no baseline failures.

baseline-failing: (none)

## Commands run and summaries

Environment: workspace root `rust/`; `PROTOC=/tmp/pw/protoc/data/bin/protoc`
(no system protoc on this Rocky Linux 9 box; a protoc binary was obtained from
the PyPI `protoc-wheel-0` wheel purely to drive `tidb-proto/build.rs`, which is
a pre-existing build requirement of `tidb-model`'s dependency chain).

```
cd rust && cargo nextest run --locked -p tidb-metadef --no-fail-fast
# baseline: Summary: 7 tests run: 7 passed, 0 skipped
# after:    Summary: 7 tests run: 7 passed, 0 skipped   (unchanged)

cd rust && PROTOC=... cargo nextest run --locked -p tidb-model --no-fail-fast
# baseline: Summary: 301 tests run: 301 passed, 0 skipped
# after:    Summary: 310 tests run: 310 passed, 0 skipped  (+9 new, all passing)

git diff --check   # clean
```

Subset property holds: failure set after == baseline failure set == ∅.

## Not verified locally

- The NextGen-kernel branch of `TestGlobalIndexV1SupportedForNextGen` was not
  exercised: the gate builds the Classic kernel (`nextgen` cargo feature of
  `tidb-config` is unset and `tidb-model` has no such feature). The Rust test
  mirrors Go's build-tag guard with `cfg!(feature = "nextgen")`.
