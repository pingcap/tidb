# b027 — port pkg/util/redact unit tests to tidb-util

Go code: pkg/util/redact (read from origin/master: redact.go, redact_test.go)

## Checklist

| Go test (pkg/util/redact/redact_test.go) | Rust test | Status |
| --- | --- | --- |
| TestRedact | `tidb-util tests_redact::redact_string` | ported |
| TestDeRedact | `tidb-util tests_redact::de_redact_cases` | ported |
| TestRedactInitAndValueAndKey | `tidb-util tests_redact::redact_init_value_key` | ported |

Notes:
- These three Go tests already existed as inline ports inside
  `src/redact.rs`'s `mod tests`; they were moved verbatim (case tables and
  assertions unchanged) into `src/tests_redact.rs` per batch naming
  convention, with a citation comment per Go function. No behavioral change.
- The Go test file contains exactly 3 test functions; all ported. Nothing
  ignored, no parity gaps found.
- Extra crate-internal tests in `src/redact.rs` (scanner/utf8 edge cases,
  DeRedactFile behaviors, BR TaskInfo goldens) are beyond the Go test file
  scope and left untouched; the MARKER-flag cross-check was split into its own
  inline test `redact::tests::redact_init_visible_via_tidb_error_mode`.

## Baseline failures

baseline-failing: tidb-util memory::arbitrator::tests::bench::bench

This single failure is timing-sensitive and flaky: it failed once on the clean
tree and passed on an immediate re-run before any edits.

## Commands run

- `cargo nextest run --locked -p tidb-util --no-fail-fast`
  - baseline (clean tree): 709 tests run: 708 passed, 1 failed, 1 skipped
    (`memory::arbitrator::tests::bench::bench`, flaky; passed on re-run)
  - after edits: 710 tests run: 710 passed, 1 skipped
- `git diff --check` → clean

Failure-set subset property holds: post-edit run had zero failures ⊆ baseline {bench}.
