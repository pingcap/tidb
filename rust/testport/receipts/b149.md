# testport batch b149 — `pkg/session.part6` → `tidb-session`

## Scope

The manifest assigns batch b149 to `pkg/session.part6`, the final 11 assigned
session/upgrade declarations after batches b144–b148. The separate
`TestSchemaCacheSizeVar` declaration is already covered by the existing
source-backed `tidb-exec` bootstrap test and is not duplicated here:

```json
{"id":"b149","gopath":"pkg/session.part6","base_gopath":"pkg/session","funcs":11,"crate":"tidb-session"}
```

The requested `origin/master` ref is not present in this worktree; `git show
origin/master:<path>` and `git grep ... origin/master` failed with `Invalid
object name origin/master`. No remote operation was performed. The source
anchors below were checked against the checked-out Go source at the same
commit used by this branch, and the absence of `origin/master` is not claimed
as verified locally.

## Files changed

- `rust/crates/tidb-session/src/tests_session_part6_source.rs` — one
  ignored Rust carrier for each assigned Go test;
- `rust/crates/tidb-session/src/lib.rs` — test-only module registration;
- `rust/testport/receipts/b149.md` — this receipt.

No production Rust behavior, SQL implementation, storage format, dependency,
CI configuration, Makefile, Go module, or testdata was changed.

## Checklist

All 11 assigned tests are represented one-to-one. They remain ignored because
the relevant Go behavior is private or requires a Domain, session-pool,
TiKV/storage-bootstrap, resource-control, RU-v2, binding-storage, or private
upgrade-registry composition that `tidb-session` does not expose. No behavior
was approximated.

| Go test | Rust test | Status |
|---|---|---|
| `pkg/session/tidb_test.go:33::TestDomapHandleNil` | `test_domap_handle_nil` | ignored-gap — private `domainMap.Get` nil-store branch (`pkg/session/tidb.go:68-89`) and Domain map are not transcreated |
| `pkg/session/tidb_test.go:41::TestSysSessionPoolGoroutineLeak` | `test_sys_session_pool_goroutine_leak` | ignored-gap — Go `sysSessionPool`/restricted-session worker cleanup (`pkg/session/session.go:1298`, `pkg/session/session.go:2254`) is not transcreated |
| `pkg/session/tidb_test.go:70::TestRUV2SessionParserTotalDoesNotLeakAcrossStandaloneParse` | `test_ruv2_session_parser_total_does_not_leak_across_standalone_parse` | ignored-gap — RU-v2 parser accounting, bypass, and restricted-session restoration (`pkg/session/session.go:1981-2038`, `pkg/session/session.go:2450-2458`, `pkg/session/session.go:2768-2792`, `pkg/session/session.go:2233-2249`) are not transcreated |
| `pkg/session/tidb_test.go:153::TestCrossKSSessionDistSQLCtxDoesNotExposeTypedNilRUReporter` | `test_cross_ks_session_dist_sql_ctx_does_not_expose_typed_nil_ru_reporter` | ignored-gap — cross-keyspace Domain and DistSQL RU reporter construction (`pkg/session/session.go:3453-3465`) are not transcreated |
| `pkg/session/tidb_test.go:167::TestDistSQLCtxPagingSizeBytesRequiresHardCappedResourceGroup` | `test_dist_sql_ctx_paging_size_bytes_requires_hard_capped_resource_group` | ignored-gap — resource-group burst-limit paging policy (`pkg/session/session.go:3467-3473`) is not transcreated |
| `pkg/session/tidb_test.go:199::TestRUV2MetricsIsolatedPerStatementInExplicitTxn` | `test_ruv2_metrics_isolated_per_statement_in_explicit_txn` | ignored-gap — per-statement RU-v2 identity and failpoint-controlled retry accounting (`pkg/session/session.go:2450-2458`) are not transcreated |
| `pkg/session/upgrade_backfill_test.go:32::TestUpgradeToVer259BackfillsIgnoreInlistPlanDigest` | `test_upgrade_to_ver259_backfills_ignore_inlist_plan_digest` | ignored-gap — versioned bootstrap/global-variable backfill (`pkg/session/upgrade_def.go:2140-2142`) is not transcreated |
| `pkg/session/upgrade_backfill_test.go:97::TestUpgradeToVer262RefreshesBindingDigest` | `test_upgrade_to_ver262_refreshes_binding_digest` | ignored-gap — `bind_info` normalization, duplicate cleanup, and versioned upgrade execution (`pkg/session/upgrade_def.go:2165-2265`) are not transcreated |
| `pkg/session/upgrade_backfill_test.go:247::TestUpgradeToVer261BackfillsDefaultStringMatchSelectivity` | `test_upgrade_to_ver261_backfills_default_string_match_selectivity` | ignored-gap — versioned bootstrap/global-variable backfill (`pkg/session/upgrade_def.go:2148-2150`) is not transcreated |
| `pkg/session/upgrade_backfill_test.go:312::TestUpgradeToVer263BackfillsAnalyzeDefaultOptions` | `test_upgrade_to_ver263_backfills_analyze_default_options` | ignored-gap — versioned bootstrap/global-variable backfill (`pkg/session/upgrade_def.go:2267-2271`) is not transcreated |
| `pkg/session/upgrade_test.go:52::TestUpgradeToVerFunctionsCheck` | `test_upgrade_to_ver_functions_check` | ignored-gap — private ordered upgrade registry and function identity (`pkg/session/upgrade_def.go:530-536`, `pkg/session/upgrade_def.go:700-720`) are not transcreated |

## Baseline and after-gate failure set

The baseline and post-edit commands emitted no nextest test IDs. Therefore
there are no `baseline-failing:` lines: both runs stopped during dependency
compilation before nextest test discovery. The observed failure was the same
in both runs:

```text
failed to run custom build command for `tidb-proto v0.0.0`
crates/tidb-proto/build.rs:42
compile checked-in dependency-closed TiDB protocol inputs: Custom { kind: Other, error: "protoc failed: " }
```

Because the baseline failure occurred before discovery, the required subset
comparison is vacuous: the after run had the same zero discovered test IDs and
no new test failure set.

## Validation

Validation profile: **Ready**, because this batch adds a complete source-backed
Rust test inventory and receipt, and Ready is the repository completion
profile.

Exact commands run and measured summaries:

```text
# Initial invocation from repository root; workspace path correction was needed.
cargo nextest run --locked -p tidb-session -E 'not test(/bench/)' --no-fail-fast
# failed before Cargo metadata: could not find Cargo.toml in /root/work/wt-b149

# Required baseline gate, in a clean detached worktree at HEAD, reusing the
# repository target directory.
git worktree add --detach /tmp/tidb-b149-baseline HEAD
(cd /tmp/tidb-b149-baseline/rust && \
  CARGO_TARGET_DIR=/root/work/wt-b149/rust/target \
  cargo nextest run --locked -p tidb-session -E 'not test(/bench/)' --no-fail-fast)
git worktree remove --force /tmp/tidb-b149-baseline
# exit 101; tidb-proto/build.rs:42; "protoc failed: "; no nextest test IDs

rustfmt --edition 2024 --config skip_children=true --check \
  rust/crates/tidb-session/src/tests_session_part6_source.rs
# passed

git diff --check
# passed

# Required identical post-edit gate, from the Rust workspace.
cd rust && cargo nextest run --locked -p tidb-session -E 'not test(/bench/)' --no-fail-fast
# exit 101; tidb-proto/build.rs:42; "protoc failed: "; no nextest test IDs

# Ready-profile lint gate.
make lint
# exit 2 before lint: make reports "go: No such file or directory" and
# "Please set the environment variable GOPATH before running `make`"

## Risks and unverified items

- Correctness risk is limited to inventory fidelity and the explicit ignored
  status. The carriers do not claim runtime parity.
- Compatibility and performance behavior are unchanged because no production
  code or dependency was modified.
- The full Go behavior was not run locally.
- The requested `origin/master` snapshot could not be read because this
  worktree has no `origin/master` ref; this is withdrawn as a locally verified
  source claim, not silently substituted with a remote fetch.
- The Rust package gate could not compile because the checked-in
  `tidb-proto` build requires a working `protoc` invocation; no nextest test
  execution or ignored-test invocation was verified locally.
- `make lint` was attempted but could not start because `go` is unavailable and
  `GOPATH` is unset; repository-wide Go lint is not verified locally.
