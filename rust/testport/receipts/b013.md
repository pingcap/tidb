# b013 — port pkg/util/backoff unit tests

## Checklist

| Go test | Rust test | Status |
| --- | --- | --- |
| `pkg/util/backoff/backoff_test.go` `TestExponential` | `tidb-util::tests_backoff::exponential` | ported |

No gaps; all Go behavior is supported on the Rust side. The crate's existing
`backoff.rs` internal `#[cfg(test)] mod tests` already contained an equivalent
port plus extra boundary tests; this batch adds the canonical
`tests_<area>.rs` module per repo convention.

## Baseline failures (clean tree, before edits)

None — baseline run: 709 tests run, 709 passed, 1 skipped.

`baseline-failing:` (empty list)

## Post-edit gate

Command: `cargo nextest run --locked -p tidb-util --no-fail-fast`

Summary: 710 tests run: 709 passed, 1 failed, 1 skipped.
The single failure was `tidb-util::memory::arbitrator::tests::bench::bench`,
which is **not** in scope and **passed in the clean-tree baseline**. Verified
flaky by re-running it in isolation (`cargo nextest run -p tidb-util
memory::arbitrator::tests::bench --no-fail-fast`) three times:
FAIL, PASS, PASS (assertion `1508 == 1542` at
`crates/tidb-util/src/memory/arbitrator/tests/bench.rs:143` — timing/memory
dependent). It is timing-flaky, not caused by this change; no backoff-related
test failed. Scoped check of added test passed:
`cargo nextest run --locked -p tidb-util tests_backoff --no-fail-fast`
→ 1 test run: 1 passed.

## Commands

- `cargo nextest run --locked -p tidb-util --no-fail-fast` (baseline): 709 passed, 1 skipped
- `cargo nextest run --locked -p tidb-util --no-fail-fast` (post-edit): subset property holds for deterministic tests; only flaky bench failed
- `cargo nextest run --locked -p tidb-util memory::arbitrator::tests::bench --no-fail-fast` ×3: FAIL/PASS/PASS (flake confirmation)
- `cargo nextest run --locked -p tidb-util tests_backoff --no-fail-fast`: 1 passed
- `cargo fmt -p tidb-util`
- `git diff --check`: clean

## Not verified locally

- Nothing specific to this batch; the flaky arbitrator bench failure was not
  root-caused beyond confirming non-determinism (out of scope).
