# b013 — port `pkg/util/backoff` Go unit tests to `tidb-util`

Go code: pkg/util/backoff (backoff_test.go).

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| TestExponential | tidb-util backoff::tests::test_exponential (src/backoff.rs) + supplementary edge-case tests `source_reset_signed_and_float_boundaries_are_exact`, `backoffer_trait_dispatches_the_mutating_source_contract`, `zero_value_and_copies_have_independent_state` | ported (pre-existing on branch; verified against origin/master source this batch) |

Notes:
- The Go package has exactly one test function (`TestExponential`). It was
  re-derived from `git show origin/master:pkg/util/backoff/backoff.go`: a
  stateful `Exponential` whose `Backoff(0)` resets to `baseBackoff` and whose
  later calls compute `min(next*multiplier, maxBackoff)`. The existing Rust
  `test_exponential` asserts all three scenarios of the Go test verbatim:
  (1,1,1)→constant 1; (1,1,10)→constant 1; (1,2,10)→{1,2,4,8,10,10,10,10,10,10}.
- This crate's convention is in-module `#[cfg(test)] mod tests` (no
  `src/tests_*.rs` sibling modules exist in tidb-util), so no new module file or
  lib.rs registration was required.
- No golden files/testdata are used by the Go package.

## Baseline failures

baseline-failing: tidb-util memory::arbitrator::tests::bench::bench

(The failure is a flaky-looking count assertion in an unrelated benchmark-style
test; unchanged before/after this batch.)

## Commands run

```
cargo nextest run --locked -p tidb-util --no-fail-fast
# Summary: 700 tests run: 699 passed, 1 failed, 1 skipped
cargo nextest run --locked -p tidb-util backoff
# Summary: 4 tests run: 4 passed, 697 skipped
git diff --check   # clean
```

Subset property holds: added/ported tests all pass; post-batch failure set
equals baseline (`memory::arbitrator::tests::bench::bench` only); no new
failures.

## Not verified locally

- Nothing for this package's scope; all Go behavior of TestExponential is
  asserted and passing on the Rust side.
