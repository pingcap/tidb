# testport batch b049 — pkg/statistics (part8) → tidb-stats

Gate: `cd rust && cargo nextest run --locked -p tidb-stats -E 'not test(/bench/)' --no-fail-fast`

## Scope definition

The manifest does not enumerate per-batch functions. Following the b044
precedent, part8 was derived from the `pkg/statistics/**` test inventory on
`origin/master`: the one remaining unclaimed Go test file whose function count
matches the manifest's ~17 is
`pkg/statistics/handle/handletest/statstest/stats_test.go`, which contains
exactly 17 `func Test*` functions. That file is batch b049.

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| stats_test.go::TestStatsCacheProcess | go_parity_b049_source::stats_cache_process | ignored-gap: requires testkit session/domain plus StatsHandle runtime (GetPhysicalTableStats, MaxTableStatsVersion, GetNextCheckVersionWithOffset) |
| stats_test.go::TestStatsCache | go_parity_b049_source::stats_cache | ignored-gap: testkit + StatsHandle analyze/alter/Clear+Update runtime |
| stats_test.go::TestStatsCacheMemTracker | go_parity_b049_source::stats_cache_mem_tracker | ignored-gap: testkit + StatsHandle memory-usage accounting through analyze/reload cycles |
| stats_test.go::TestStatsStoreAndLoad | go_parity_b049_source::stats_store_and_load | ignored-gap: store/load round-trip with internal.AssertTableEqual over full Table payloads |
| stats_test.go::TestInitStatsMemTraceWithLite | go_parity_b049_source::init_stats_mem_trace_with_lite | ignored-gap: InitStats over mysql.stats_* tables, global config (LiteInitStats), MemConsumed tracking |
| stats_test.go::TestInitStatsMemTraceWithoutLite | go_parity_b049_source::init_stats_mem_trace_without_lite | ignored-gap: same as above (lite=false) |
| stats_test.go::TestInitStatsMemTraceWithConcurrentLite | go_parity_b049_source::init_stats_mem_trace_with_concurrent_lite | ignored-gap: concurrent InitStats runtime + config toggles |
| stats_test.go::TestInitStatsMemTraceWithoutConcurrentLite | go_parity_b049_source::init_stats_mem_trace_without_concurrent_lite | ignored-gap: concurrent InitStats runtime + config toggles |
| stats_test.go::TestInitStats | go_parity_b049_source::init_stats | ignored-gap: testkit ANALYZE, statstestutil.HandleNextDDLEventWithTxn, StatsHandle.InitStats runtime |
| stats_test.go::TestInitStatsForPartitionedTable | go_parity_b049_source::init_stats_for_partitioned_table | ignored-gap: ANALYZE of range-partitioned tables with global indexes + InitStats runtime |
| stats_test.go::TestInitStatsWithoutHandlingDDLEvent | go_parity_b049_source::init_stats_without_handling_ddl_event | ignored-gap: flush-stats-delta + InitStats over partially populated metadata |
| stats_test.go::TestInitStatsVer2 | go_parity_b049_source::init_stats_ver2 | ignored-gap: ANALYZE + DDL event handling + lease/SetLease InitStats runtime |
| stats_test.go::TestInitStats51358 | go_parity_b049_source::init_stats_51358 | ignored-gap: StatsCacheGetNil failpoint cache-miss path in handle cache |
| stats_test.go::TestInitStatsIssue41938 | go_parity_b049_source::init_stats_issue_41938 | ignored-gap: ANALYZE of timestamp-PK table + InitStats runtime |
| stats_test.go::TestDumpStatsDeltaInBatch | go_parity_b049_source::dump_stats_delta_in_batch | ignored-gap: FLUSH STATS_DELTA statement execution + mysql.stats_meta query surface |
| stats_test.go::TestInitStatsForTableWithTopNButNoBuckets | go_parity_b049_source::init_stats_for_table_with_top_n_but_no_buckets | ignored-gap: testkit ANALYZE + InitStats over topn-only histograms |
| stats_test.go::TestInitStatsMemoryFullBlocksBucketsButKeepsTopN | go_parity_b049_source::init_stats_memory_full_blocks_buckets_but_keeps_top_n | ignored-gap: mockBucketsLoadMemoryLimit failpoint inside bucket-load path |

All 17 tests are `#[ignore = "go-parity-gap: …"]` markers with re-derived
intent comments; no behavior was approximated to make anything pass.
`tidb-stats` owns only dependency-closed leaf primitives; every one of these
Go tests drives the session/domain/mock-store statistics-handle runtime that
does not exist on the Rust side yet (same boundary documented by b044).

## Baseline failures

Baseline on clean tree before edits:

```
Summary [  10.358s] 516 tests run: 516 passed, 19 skipped
```

`baseline-failing:` none.

## Commands run and summaries

- Baseline: `cargo nextest run --locked -p tidb-stats -E 'not test(/bench/)' --no-fail-fast`
  → `516 tests run: 516 passed, 19 skipped` (0 failures)
- After edits (identical gate):
  → `516 tests run: 516 passed, 36 skipped` (0 failures; +17 = all new tests
  are `#[ignore]`d; failure set trivially a subset of the empty baseline set)
- `cargo fmt -p tidb-stats -- --check` → clean.
- `git diff --check` → clean.

## Not verified locally

- None of the 17 pinned behaviors could be exercised: the statistics-handle
  runtime (session, domain, mock store, ANALYZE executor, failpoints,
  InitStats) is not present in the Rust workspace. Each marker documents the
  exact missing surface so a later port can turn it into a real assertion.
