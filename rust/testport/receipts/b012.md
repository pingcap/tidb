# b012 — port `pkg/sessionctx/variable` unit tests, part 3 (tidb-vardef)

Go source of truth read from `origin/master`:
- `pkg/sessionctx/variable/tests/variable_test.go`
- `pkg/sessionctx/variable/varsutil_test.go`

Part 3 = tests 121–150 of the package's canonical ordering
(alphabetical path, line number; 150 `func Test*` including 3 `TestMain`).

This historical receipt records the original `tidb-vardef` seed. The
`GetNativeValType`, `SkipSysvarCache`, and `OrderByDependency` empty stubs
were removed after executable replacements landed in
`tidb-session::sysvar`; `TestScopeToString` is covered by
`tidb-exec::sysvar_scope` tests. The rows retain their original source
mapping below for audit traceability.

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| TestFloatValidation | tidb-session::sysvar::tests::primitive_type_validation_matches_go | ported (float parsing and Min/Max clamping) |
| TestBoolValidation | tidb-session::sysvar::tests::primitive_type_validation_matches_go | ported (ON/OFF, 0/1, and negative-bool conversion) |
| TestTimeValidation | tidb-session::sysvar::tests::time_and_duration_validation_match_go | ported (clock parsing and full offset rendering) |
| TestGetNativeValType | `tidb-session::sysvar::native_val_types_follow_the_registry_type` | moved to executable owner |
| TestDeprecation | tidb-session::tests_global_vars::deprecated_index_lookup_concurrency_warns_like_go | partial port (deprecated index-lookup concurrency warning and replacement text are live; the full synthetic registry callback audit remains outside this SQL owner) |
| TestBuiltInCase | built_in_case_names_are_lowercase | ported (asserted over the crate's 470 sysvar-name constants instead of the Go SysVar registry, which is not part of the rewrite) |
| TestIsNoop | tidb-session::sysvar::tests::noop_metadata_matches_go; tidb-session::tests_global_vars::noop_globals_are_not_copied_into_new_sessions | partial port (documented no-op metadata and SkipInit behavior are live for the tested read-only compatibility variables; the broader MySQL noop catalog remains outside this hand-maintained marker set) |
| TestDefaultValuesAreSettable | default_values_are_settable_unported | ignored-gap |
| TestLimitBetweenVariable | limit_between_variable | ported (DefTiDBGOGCTunerThreshold pinned as test-local const until it lands in defaults) |
| TestSysVarNameIsLowerCase | sysvar_name_is_lowercase | ported (same adaptation as TestBuiltInCase) |
| TestSettersandGetters | settersandgetters_unported | ignored-gap |
| TestScopeToString | `tidb-exec::sysvar_scope_source::sysvar_scope_string_matches_source_order_and_zero_case` | moved to executable owner |
| TestValidateWithRelaxedValidation | tidb-session::sysvar::tests::relaxed_validation_matches_go | ported (normalization survives while type/closure refusals return original text) |
| TestValidateInternalSessionVariable | validate_internal_session_variable_unported | ignored-gap |
| TestInstanceConfigHasMatchingSysvar | instance_config_has_matching_sysvar_unported | ignored-gap |
| TestInstanceScope | instance_scope_unported | ignored-gap |
| TestSetSysVar | set_sys_var_unported | ignored-gap |
| TestSkipSysvarCache | `tidb-session::sysvar::only_the_tikv_backed_variables_skip_the_cache` | moved to executable owner |
| TestTimeValidationWithTimezone | tidb-session::vars::tests::generic_time_and_duration_globals_use_go_normalization | partial port (GLOBAL TypeTime writes use the issuing session's IANA-zone offset; the Go session-only synthetic SysVar shape remains outside the registry owner) |
| TestOrderByDependency | `tidb-session::sysvar::depended_variables_order_first` | moved to executable owner |
| TestTiDBOptOn | tidb_opt_on_unported | ignored-gap |
| TestNewSessionVars | new_session_vars_unported | ignored-gap |
| TestVarsutil | varsutil_unported | ignored-gap |
| TestValidate | validate_unported | ignored-gap |
| TestValidateStmtSummary | validate_stmt_summary_unported | ignored-gap |
| TestConcurrencyVariables | concurrency_variables_unported | ignored-gap |
| TestHelperFuncs | helper_funcs_tidb_opt_enable_clustered (ported, TiDBOptEnableClustered portion); remaining helpers covered by on_off_helpers/assertion_level/tidb_opt_on gap stubs where applicable | partial port |
| TestSessionStatesSystemVar | session_states_system_var_unported | ignored-gap |
| TestOnOffHelpers | on_off_helpers_unported | ignored-gap |
| TestAssertionLevel | assertion_level_unported | ignored-gap |

## Baseline failures

Baseline recorded before any edit:

```
cargo nextest run --locked -p tidb-vardef --no-fail-fast
Summary [   0.012s] 7 tests run: 7 passed, 0 skipped
```

No baseline-failing lines (clean tree had zero failures).

## Commands run and summaries

1. Baseline (clean tree): `cargo nextest run --locked -p tidb-vardef --no-fail-fast`
   → `7 tests run: 7 passed, 0 skipped`
2. After edits (identical gate): `cargo nextest run --locked -p tidb-vardef --no-fail-fast`
   → `11 tests run: 11 passed, 26 skipped`
3. `git diff --check` → clean.

Subset property holds: added tests all pass; failure set (empty) is a subset of baseline (empty). No pre-existing failure disappeared or appeared.

## Not verified locally

- Go tests themselves were not executed (`go test`) — behavior re-derived from Go sources on `origin/master`.
- The two lowercase-name tests iterate this crate's name constants rather than the Go `GetSysVars()` registry; registry-level invariants remain pinned only when a registry lands.
