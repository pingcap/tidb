# testport batch b137 — `pkg/executor.part19` → `tidb-executor`

## Scope

Manifest batch `b137` is the deterministic items 1081–1140 of the upstream
`pkg/executor` test enumeration, sorted by `(path, line)` across `Test*`,
`Benchmark*`, and `Fuzz*` functions, recursively excluding
`pkg/executor/internal/`. The batch contains exactly 60 functions, bounded by:

- `pkg/executor/test/passwordtest/password_management_test.go:131::TestPasswordManagement`;
- `pkg/executor/test/seqtest/seq_executor_test.go:766::TestAdminShowNextID`.

The assigned manifest row is:

```json
{"id":"b137","gopath":"pkg/executor.part19","base_gopath":"pkg/executor","crate":"tidb-executor","funcs":60}
```

The enumeration was reconstructed from the checked-out `pkg/` snapshot:
`manifest_total=1308`, `b137_slice=60`, `source_anchors=60`,
`rust_tests=60`, and basename/function-name matching passed.

## Files changed

- `rust/crates/tidb-executor/src/tests_executor_part19_source.rs` — one
  source-anchored Rust test for each of the 60 assigned Go functions;
- `rust/crates/tidb-executor/src/lib.rs` — registers the source module;
- `rust/testport/receipts/b137.md` — this receipt.

No production behavior or dependencies were changed.

## Port status

Nine entries run against existing executor carriers:

- prepared PointGet plan rebinding and cache hits;
- prepared point-update fast-path execution;
- parameterized range plan-cache rebinding;
- parameterized operator-tree plan-cache rebinding;
- prepared SELECT rebinding;
- integer `LIMIT`/`OFFSET` parameters;
- prepared aggregation rebinding;
- prepared INSERT on a common-handle table;
- prepared UPDATE assignment and handle rebinding.

The other 51 entries are explicit `#[ignore]` inventory tests. They retain the
Go source anchor and a concrete gap reason:

- 8 password/account-management tests require tidb-session authentication,
  account metadata, password history/expiration, or failed-login state;
- 1 plan-cache `TestMain` is Go bootstrap/goleak setup only;
- 4 plan-cache tests require transaction-aware sessions, system variables,
  clustered-index session state, or session cache interactions;
- 1 plan-replayer `TestMain` is bootstrap only;
- 10 plan-replayer tests require session/domain capture, archive generation,
  external storage, or hypothetical TiFlash metadata;
- 1 recovery `TestMain` is bootstrap only;
- 12 recovery/flashback tests require DDL history, asynchronous schema jobs,
  MVCC/GC safe timestamps, TiKV integration, or account privileges;
- 1 sequential-suite `TestMain` is bootstrap only;
- 5 prepared-statement tests require session PREPARE/EXECUTE registries,
  prepared DELETE dispatch, statement-time/session clock behavior, or server
  process information;
- 8 sequential executor tests require distributed request lifecycle,
  failpoints, SHOW/session statistics, allocator metadata, or goroutine-leak
  observation.

The nine running tests are deliberately limited to behavior exposed by
`build_prepared_select_plan`, `run_prepared_select`,
`run_fast_prepared_insert`, and `run_fast_prepared_update`; unsupported session,
DDL, remote-storage, and failpoint behavior is not approximated.

## Validation

Validation profile: **Ready attempted; completion checks scoped because the
workspace lacks the Go toolchain and the test target has pre-existing Rust
2024 test-build blockers**.

Exact successful commands:

```text
rustfmt --edition 2024 --config skip_children=true \
  --check rust/crates/tidb-executor/src/tests_executor_part19_source.rs
→ passed

git diff --check
→ passed

cd rust && cargo check --locked -p tidb-executor --lib
→ Finished `dev` profile successfully

cd rust && cargo nextest run --locked -p tidb-executor \
  -E 'test(tests_executor_part19_source)' --no-fail-fast
→ 9 tests run: 9 passed, 2718 skipped
```

The focused nextest command required temporary, restored-only workarounds for
pre-existing unrelated test-build blockers. An EXIT trap restored both files:

- four `rand::Rng::gen()` calls in
  `rust/crates/tidb-executor/src/driver/tests/column_defaults.rs` were changed
  to `r#gen()` for Rust 2024 parsing;
- `use tidb_chunk::Chunk` in
  `rust/crates/tidb-executor/src/tests_set_show_stats_slow_query_source.rs`
  was changed to `use tidb_chunk::chunk::Chunk` because `Chunk` is not
  re-exported at the crate root.

The working-tree restoration check passed and neither workaround remains in
the diff.

Commands attempted but blocked:

```text
make lint
→ blocked: `go` is not installed and Makefile.common requires GOPATH

rustfmt --edition 2024 rust/crates/tidb-executor/src/tests_executor_part19_source.rs \
  rust/crates/tidb-executor/src/lib.rs
→ blocked while rustfmt recursively parsed the pre-existing Rust 2024
  `expected_rng.gen()` calls in driver/tests/column_defaults.rs
```

## Risks

- Correctness: the nine runnable tests cover prepared-plan rebinding/cache-hit
  behavior and the existing fast prepared DML carriers. The 51 ignored tests
  do not claim account, DDL recovery, plan-replayer, distributed cleanup,
  session, or exact SHOW parity.
- Compatibility: no production code path or public API was changed; only a
  test module registration and source-backed tests were added.
- Performance: no production hot path or dependency was changed.

## Not verified locally

- The 51 ignored Go behaviors were not executed as Rust behavior tests because
  their owning session, account, DDL, domain, remote-storage, or failpoint
  surfaces are not transcreated in this executor test boundary.
- The full `tidb-executor` test suite was not run to completion; the ordinary
  test build requires the two restored-only workarounds documented above.
- `make lint` was not run successfully because the environment has no Go toolchain
  and no `GOPATH`.
- No Go test execution was performed in this session.
