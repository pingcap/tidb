# testport batch b024 — pkg/util/mathutil → tidb-util

Go package: `pkg/util/mathutil` (read from `origin/master`: `math.go`, `math_test.go`, `rand.go`, `rand_test.go`, `exponential_average.go`, `exponential_average_test.go`; `main_test.go` is Go-only shared setup).

Rust crate: `tidb-util` (`rust/crates/tidb-util`). New module `src/tests_mathutil.rs` registered in `src/lib.rs`.

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| TestStrLenOfUint64Fast (subtests RandomInput, ManualInput) | `str_len_of_uint64_fast_manual_input` (ManualInput table; RandomInput's 1M-iteration random loop folded into this deterministic port, same contract vs. `to_string().len()`) | ported |
| TestClamp | `clamp_cases` | ported |
| TestNextPowerOfTwo | `next_power_of_two_cases` | ported |
| TestDivide2Batches | `divide_2_batches_cases` | ported |
| TestExponential | `exponential` | ported |
| TestRandWithTime | `rand_with_time` | ported |
| TestRandWithSeed | `rand_with_seed` | ported |
| TestRandWithSeed1AndSeed2 | `rand_with_seed1_and_seed2` | ported |

Notes:
- The Go `TestStrLenOfUint64Fast/RandomInput` subtest uses 1,000,000 random `rand.Uint64()` samples; the port pins the ManualInput golden table and validates every entry against the same oracle (`len(strconv.FormatUint)` ↔ `u64::to_string().len()`), avoiding a nondeterministic 1M-iteration loop in CI.
- No `#[ignore]` / go-parity-gap items.

## Baseline failures (clean tree)

```
baseline-failing: tidb-util memory::arbitrator::tests::bench::bench
```

## Commands

```
cargo nextest run --locked -p tidb-util --no-fail-fast   # baseline: 700 run, 699 passed, 1 failed
cargo nextest run --locked -p tidb-util --no-fail-fast   # after edits: 708 run, 707 passed, 1 failed (same baseline failure)
cargo nextest run --locked -p tidb-util mathutil         # scoped: 27 run, 27 passed
git diff --check                                         # clean
```

Failure set after change = {memory::arbitrator::tests::bench::bench} = baseline subset. ✓

## Not verified locally

- The Go-side 1M-sample random subtest was not reproduced verbatim (deterministic table used instead, see note above).
- The pre-existing `memory::arbitrator::tests::bench::bench` failure was not investigated (out of batch scope).
