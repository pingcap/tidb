# testport batch b142 — `pkg/executor.part24` → `tidb-executor`

Gate command (as assigned, run from `rust/`):

```
cargo nextest run --locked -p tidb-executor -E 'not test(/bench/)' --no-fail-fast
```

Go code: `pkg/executor` (part 24 = items 1381–1440 of the deterministic
`func Test*`/`func Benchmark*` enumeration over `pkg/executor/*_test.go`,
recursive excluding `pkg/executor/internal/`, sorted by (file path, line
number) — the scheme the landed sibling receipts pin, 60 items per part;
part 21 = items 1201–1260 per b139, so part 24 = items 1381–1440). The
manifest's enumeration total is 1389 items, so part 24 contains exactly the
last 9 items — matching the batch's assigned count of ~9 Go test functions.

## Which 9 Go tests part 24 is (enumeration reconstruction)

The b139 receipt re-verified this session's snapshot anchors: manifest items
1200/1201 (`pkg/executor/test/tiflashtest/tiflash_test.go:144/185`) and
1260/1261 (`pkg/executor/test/unstabletest/memory_test.go:35/105`) equal the
snapshot's (path, line)-sorted indices, so manifest order = snapshot order
through item 1261. The snapshot's remaining top-level items 1262–1308 run
from `pkg/executor/test/writetest/main_test.go:27::TestMain` through
`pkg/executor/write_concurrent_test.go:25::TestBatchInsertWithOnDuplicate`.

For the tail, the landed b119/b121 receipts pin that the suite-method
matches (`func (s *mockGCSSuite) TestXxx`, 18 of them in
`pkg/executor/test/loadremotetest/*.go`) sort AFTER the whole
`pkg/executor` tree — i.e. the manifest enumeration ends with the
suite-method block, and with it the 1389-item total:

- 1372–1380 (or 1371–1380 with the `pkg/executor/load_data.go:741`
  `(*LoadDataWorker).TestLoadLocal` method included): the
  `error_test.go` methods (36 `TestErrorMessage`, 82 `TestColumnNumMismatch`,
  149 `TestEvalError`, 192 `TestDataError`, 266 `TestIssue43555`) and the
  `multi_file_test.go` methods (28 `TestFilenameAsterisk`, 94
  `TestLastInsertID`, 127 `TestMultiBatchWithIgnoreLines`, 165
  `TestMixedCompression`) — part 22/23 territory (batches b140/b141, no
  local worktrees),
- **1381–1389 = the nine `one_csv_test.go` methods**: 25 `TestLoadCSV`,
  88 `TestLoadCsvInTransaction`, 129 `TestIgnoreNLines`, 188
  `TestCustomizeNULL`, 312 `TestGeneratedColumns`, 356 `TestMultiValueIndex`,
  384 `TestGBK`, 486 `TestOtherCharset`, 574 `TestColumnsAndUserVars`.

This tail assignment is invariant to whether the 19th suite method
(`TestLoadLocal`) is counted in the appended block, and it is the only
reading that yields a stable, visible 9-item slice: the batch assignment's
"approx 9 Go test functions" confirms the 1381–1389 tail arithmetic. The
manifest total (1389) exceeds anything countable on this snapshot (1308
top-level in `_test.go`, 1328 with the 19 suite methods and the
`pkg/executor/sortexec/topn_chunk_heap.go:173 TestKillSignalInTopN`
match) — the same manifest-vs-snapshot drift b121 documented; because all
verified anchors through 1261 match exactly, the drift items sort in the
pre-block tail region and do not affect the last-9 slice.

## Master snapshot

No `origin/master` ref exists in this offline worktree (remote operations
forbidden; `git rev-parse origin/master` fails — the convention the landed
sibling batches b106/b115/b121/b127/b133/b136/b139 document). Tests were
read from the branch's Go snapshot at `pkg/` (clean at HEAD `77d2bf02`);
the `pkg/executor/test/loadremotetest/one_csv_test.go` contents above were
read from that snapshot and every cited line number was re-verified this
session with `grep -n` over the file.

## Files added

- `rust/crates/tidb-executor/src/tests_loadremotetest_one_csv_source.rs`
  (9 `#[ignore]` gap tests), registered as one `#[cfg(test)] mod` line in
  `rust/crates/tidb-executor/src/lib.rs`. No other existing file touched,
  no testdata needed (Go uploads its fixtures to the fake GCS at runtime).

Totals: 9 new Rust test functions, all `#[ignore]` — every Go test in this
batch drives `LOAD DATA INFILE 'gs://...'` through the executor's remote
reader + `LoadDataController` row loop (`pkg/executor/load_data.go`), and
this crate has no LOAD DATA execution at all: `grep -rn LoadData
rust/crates/tidb-executor/src` finds only the `StmtContext` statement
classification (`src/stmt_context.rs:52`) and a `FlagInLoadDataStmt` note
(`src/statement_pushdown.rs:206`); the driver's `run_*` entries accept no
LOAD DATA statement. The statement PARSES — probe-verified this session for
every SQL shape in the 9 tests against `tidb-parser::parse` (FIELDS/LINES
options, `ESCAPED BY '!'`, `ESCAPED BY ''`, `DEFINED NULL BY 'NULL'/
'mynull'/x'00'` ± `OPTIONALLY ENCLOSED`, `CHARACTER SET gbk/utf8/latin1/
ascii/binary/utf8mb4`, `IGNORE INTO`, wildcard `cols_and_vars-*.tsv`,
`(@V1, @v2, @v3) set a=@V1, b=@V2*10, c=123`, column lists `(a)`) — and
the parsed `LoadDataStmt` carries all of them, but no executor consumes
it. The parse-level probe is scratch evidence only; it was run through a
temporary module that is NOT part of the commit. One measured divergence
is recorded in the `gbk_charset_conversion_on_load` docs: Go rejects
`CHARACTER SET unknown` at parse time with the
`[parser:1115]Unknown character set: 'unknown'` terror
(`pkg/parser/ddl_load_data_parser.go:74`), while the Rust parser rejects
the same statement but renders `unknown LOAD DATA character set`
(`rust/crates/tidb-parser/src/load_data.rs:77`) — the Go observable
substring is not produced, so that arm stays a gap rather than a running
pin.

## Checklist

Statuses: **ignored-gap** (`#[ignore]`, missing Rust surface). Every Rust
test maps 1:1 to one Go suite method; per-arm expectations live in each
test's doc comment.

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| `pkg/executor/test/loadremotetest/one_csv_test.go:25::TestLoadCSV` | `tests_loadremotetest_one_csv_source::load_csv_loads_quoted_remote_csv_rows` | ignored-gap — fake-GCS remote reader + `LoadDataController` CSV row loop unported; `/etc/passwd` arm needs the 8154 raise at `pkg/executor/importer/import.go:1475` (message `pkg/errno/errname.go:1070`), unreachable without the executor |
| `pkg/executor/test/loadremotetest/one_csv_test.go:88::TestLoadCsvInTransaction` | `tests_loadremotetest_one_csv_source::load_csv_in_transaction_rolls_back_with_the_txn` | ignored-gap — no `begin pessimistic`/`rollback` surface and no LOAD DATA executor to participate in it |
| `pkg/executor/test/loadremotetest/one_csv_test.go:129::TestIgnoreNLines` | `tests_loadremotetest_one_csv_source::ignore_n_lines_counts_terminators_not_quotes` | ignored-gap — `IGNORE n LINES` terminator counting (quote-unaware) lives in the unported controller |
| `pkg/executor/test/loadremotetest/one_csv_test.go:188::TestCustomizeNULL` | `tests_loadremotetest_one_csv_source::customize_null_defined_null_by_matrix` | ignored-gap — `DEFINED NULL BY` field decoding and the `must specify FIELDS [OPTIONALLY] ENCLOSED BY when use NULL DEFINED BY OPTIONALLY ENCLOSED` guard (`pkg/executor/importer/import.go:719`) unported |
| `pkg/executor/test/loadremotetest/one_csv_test.go:312::TestGeneratedColumns` | `tests_loadremotetest_one_csv_source::generated_columns_substitute_on_load` | ignored-gap — load-time generated-column substitution unported |
| `pkg/executor/test/loadremotetest/one_csv_test.go:356::TestMultiValueIndex` | `tests_loadremotetest_one_csv_source::multi_value_index_maintained_by_load` | ignored-gap — `cast(j as signed array)` multi-valued index maintenance unported |
| `pkg/executor/test/loadremotetest/one_csv_test.go:384::TestGBK` | `tests_loadremotetest_one_csv_source::gbk_charset_conversion_on_load` | ignored-gap — charset decode of file bytes, the 1366 Incorrect-string-value client error (`pkg/table/column.go:227`), the `Records: 2  Deleted: 0  Skipped: 0  Warnings: 2` message, and Go's exact parse-error text (divergence measured at `rust/crates/tidb-parser/src/load_data.rs:77`) all unported |
| `pkg/executor/test/loadremotetest/one_csv_test.go:486::TestOtherCharset` | `tests_loadremotetest_one_csv_source::other_charset_conversions_on_load` | ignored-gap — utf8/latin1/ascii/binary byte decoding on load unported |
| `pkg/executor/test/loadremotetest/one_csv_test.go:574::TestColumnsAndUserVars` | `tests_loadremotetest_one_csv_source::columns_and_user_vars_wildcard_mapping` | ignored-gap — wildcard object expansion and `(@V1, @v2, @v3) set ...` user-variable mapping unported |

## Validation

Exact commands (run from `rust/` unless noted) and their summary lines:

1. Baseline (clean tree, before any edit):
   `cargo nextest run --locked -p tidb-executor -E 'not test(/bench/)' --no-fail-fast`
   → `Summary [ 139.679s] 1528 tests run: 1480 passed (1 slow), 48 failed, 1020 skipped`
2. Parse-surface probe (temporary scratch module, removed before commit):
   `cargo nextest run -p tidb-executor probe_one_csv_shapes --nocapture`
   → 29 PROBE lines: all one_csv SQL shapes PARSE except
   `CHARACTER SET unknown` (PARSE-ERROR; message divergence quoted above).
3. New tests compile and pass when forced:
   `cargo nextest run --locked -p tidb-executor --run-ignored only -E 'test(tests_loadremotetest_one_csv_source)' --no-fail-fast`
   → `Summary [ 0.019s] 9 tests run: 9 passed, 2548 skipped`
4. Post-change gate (identical command):
   `cargo nextest run --locked -p tidb-executor -E 'not test(/bench/)' --no-fail-fast`
   → `Summary [ 137.762s] 1528 tests run: 1480 passed (1 slow), 48 failed, 1029 skipped`

The post-change failure set is IDENTICAL to the baseline set (48 tests,
compared by exact test id): no new failures, no pre-existing failure
masked, and the +9 skipped are exactly the new `#[ignore]` tests.
`git diff --check` passes (no whitespace errors).

## Baseline failure set (48, unchanged post-change)

baseline-failing: tidb-executor access_path::tests::a_common_handle_row_in_reads_only_the_named_prefixes
baseline-failing: tidb-executor access_path::tests::explain_analyze_act_rows_reflect_the_truncation
baseline-failing: tidb-executor access_path::tests::the_double_read_issues_one_batch_get_per_index_batch
baseline-failing: tidb-executor driver::tests::aggregates::aggregate_having_and_order_by
baseline-failing: tidb-executor driver::tests::aggregates::distinct_range_orders_gos_hash_agg_over_reader_tree
baseline-failing: tidb-executor driver::tests::aggregates::grouped_partial_count_carries_the_group_key
baseline-failing: tidb-executor driver::tests::aggregates::joined_integer_sum_uses_root_stream_agg
baseline-failing: tidb-executor driver::tests::aggregates::select_distinct
baseline-failing: tidb-executor driver::tests::aggregates::tpcc_condition_eight_uses_index_join_and_carries_warehouse_ytd
baseline-failing: tidb-executor driver::tests::aggregates::tpcc_condition_eleven_pushes_filters_through_nested_derived_joins
baseline-failing: tidb-executor driver::tests::aggregates::tpcc_condition_four_streams_across_a_grouped_derived_table
baseline-failing: tidb-executor driver::tests::aggregates::tpcc_condition_nine_rebuilds_grouped_history_over_index_lookup
baseline-failing: tidb-executor driver::tests::aggregates::tpcc_condition_six_simplifies_and_pushes_through_derived_tables
baseline-failing: tidb-executor driver::tests::aggregates::tpch_q14_matches_recorded_hash_join_plan
baseline-failing: tidb-executor driver::tests::aggregates::tpch_q1_splits_avg_and_sorts_the_restored_output
baseline-failing: tidb-executor driver::tests::aggregates::tpch_q3_keeps_go_projections_around_grouped_topn
baseline-failing: tidb-executor driver::tests::dml::limit_zero_dml_still_validates_the_statement
baseline-failing: tidb-executor driver::tests::join_reorder::the_advanced_greedy_defers_non_equality_edges_until_the_second_round
baseline-failing: tidb-executor driver::tests::joins::index_join_probe_rows_use_only_the_access_paths_join_keys
baseline-failing: tidb-executor driver::tests::joins::tpcc_check_five_keeps_only_the_cross_leaf_residual
baseline-failing: tidb-executor driver::tests::joins::tpcc_check_seven_propagates_the_warehouse_range_to_both_leaves
baseline-failing: tidb-executor driver::tests::joins::tpcc_customer_warehouse_join_uses_two_point_gets
baseline-failing: tidb-executor driver::tests::joins::tpcc_stock_level_bounds_both_join_leaves
baseline-failing: tidb-executor driver::tests::mem_quota::selection_cached_chunk_is_part_of_the_query_quota
baseline-failing: tidb-executor driver::tests::point_get::a_handle_point_with_an_extra_conjunct_wins_over_the_unique_index_like_go
baseline-failing: tidb-executor driver::tests::point_get::a_point_plan_keys_by_the_constant_in_the_columns_domain
baseline-failing: tidb-executor driver::tests::point_get::exact_handle_range_uses_the_go_cop_projection_tree
baseline-failing: tidb-executor driver::tests::point_get::ordered_handle_range_keeps_the_go_cop_projection_below_sort
baseline-failing: tidb-executor driver::tests::predicate_pushdown::a_single_table_read_ends_in_the_cop_task_go_prints
baseline-failing: tidb-executor driver::tests::predicate_pushdown::tpch_q6_selection_keeps_go_conditions_and_cardinality_after_pruning
baseline-failing: tidb-executor driver::tests::primary_keys::the_clustered_index_mode_decides_the_handle
baseline-failing: tidb-executor driver::tests::subqueries::correlated_avg_predicate_decorrelates_to_grouped_join
baseline-failing: tidb-executor driver::tests::subqueries::correlated_sum_predicate_pulls_above_unique_outer_join
baseline-failing: tidb-executor driver::tests::subqueries::explaining_a_correlated_scalar_type_reads_no_storage
baseline-failing: tidb-executor driver::tests::subqueries::subqueries
baseline-failing: tidb-executor driver::tests::subqueries::tpcc_conditions_ten_and_twelve_decorrelate_scalar_sums
baseline-failing: tidb-executor driver::tests::subqueries::tpch_q2_correlated_min_matches_recorded_hash_join_plan
baseline-failing: tidb-executor driver::tests::through_proj::an_equality_inside_one_relation_is_not_injected
baseline-failing: tidb-executor driver::tests::through_proj::an_expression_join_key_gets_an_injected_column
baseline-failing: tidb-executor driver::tests::through_proj::the_index_joins_outer_leaf_is_asked_for_the_order_through_the_derived_table
baseline-failing: tidb-executor driver::tests::through_proj::the_injected_wrapper_is_pruned_and_its_leaf_takes_the_covering_index
baseline-failing: tidb-executor hash_agg::tests::final_decimal_avg_uses_parallel_worker_window
baseline-failing: tidb-executor hash_agg::tests::grouped_binary_strings_use_go_parallel_hashagg_pipeline
baseline-failing: tidb-executor hash_agg::tests::integer_count_agg_uses_parallel_worker_window
baseline-failing: tidb-executor hash_agg_spill_tests::test_get_correct_result
baseline-failing: tidb-executor remote_scan::tests::a_pushed_predicate_does_not_move_an_aggregate_value
baseline-failing: tidb-executor remote_scan::tests::an_empty_handle_range_reads_nothing_instead_of_a_rangeless_request
baseline-failing: tidb-executor::all db_integration_ddl_types_source::unique_key_null_value_cluster_index_unique_index_allows_nulls

## Not verified locally

- The nine Go tests were not executed (`go test` on the Go snapshot was
  not run in this session); all Go-side expectations were re-derived by
  reading the Go source and helpers
  (`one_csv_test.go`, `util_test.go`, `error_test.go:28
  checkClientErrorMessage`, `pkg/executor/importer/import.go:719/1475`,
  `pkg/errno/errname.go:1070`, `pkg/table/column.go:227`,
  `pkg/parser/ddl_load_data_parser.go:74`) and their line numbers
  re-verified by grep. The Rust-side gap claims (no LOAD DATA executor,
  parse shapes, charset-error text) were measured by probe and grep as
  quoted above.
- The manifest's exact 1389-item tail composition beyond the appended
  suite-method block is inferred (the drift items themselves are not
  visible in this snapshot); the inference is anchored by the receipts'
  "suite-method matches sort after the whole pkg/executor tree" rule,
  the verified equality of manifest and snapshot indices through item
  1261, and the assigned count of ~9 tests, but the tail items outside
  the block were not observed directly.
