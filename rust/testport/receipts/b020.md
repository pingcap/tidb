# b020 — port pkg/util/disjointset unit tests

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| `pkg/util/disjointset/int_set_test.go` `TestIntDisjointSet` | `tidb-util tests_disjointset::int_disjoint_set_unions_and_roots` | ported |
| `pkg/util/disjointset/set_test.go` `TestDisjointSet` | `tidb-util tests_disjointset::disjoint_set_sparse_string_elements` | ported |
| `pkg/util/disjointset/main_test.go` `TestMain` (test harness bootstrap) | n/a | skipped-reason: Go TestMain only registers failpoint enable/disable; no behavior to pin |

Note: the owning modules already contained inline ports
(`disjointset::int_set::tests::test_int_disjoint_set`,
`disjointset::set::tests::test_disjoint_set`); this batch adds the sibling
`tests_<area>.rs` module per convention, using public API only. The Go
assertions on `parent[i] == i` / `len(set.parent)` were re-expressed via
public `find_root` identity and `Set::len()`; the Rust `SimpleIntSet` has no
length accessor, so the initial-length assertion is covered by root-identity
checks over `0..10`.

## Baseline failures

None. Baseline run on clean tree:

```
Summary [ 9.048s] 709 tests run: 709 passed, 1 skipped
```

No `baseline-failing:` lines.

## Commands

```
cargo nextest run --locked -p tidb-util --no-fail-fast   # baseline: 709 passed, 1 skipped, 0 failed
cargo nextest run --locked -p tidb-util --no-fail-fast   # after edits: 711 passed, 1 skipped, 0 failed (subset property holds)
git diff --check                                          # clean
```

Environment note: `PROTOC=/tmp/pw/protoc/data/bin/protoc` was required because
`protoc` is not installed system-wide and the workspace build of
`tidb-proto/build.rs` needs it.

## Not verified locally

- Nothing outstanding for this batch; both added tests pass.
