# `pkg/extension/_import` — complete Go-master parity boundary receipt

Comparison source: Go `origin/master` at commit
`0bc44483e3e41a8ea917d4382dc202369468d200` (2026-09-01).

## Complete inventory

The package contains two tracked artifacts and 26 lines. Both were read line
by line before this receipt was written. There are no current generated import
files, production functions, tests, platform/build-tag variants, benchmarks,
fuzz targets, or fixtures.

| Artifact | Lines | Git blob | SHA-256 | Role |
| --- | ---: | --- | --- | --- |
| `BUILD.bazel` | 8 | `35a194b2e5aca89d7aff2902469fdc2423c5570f` | `b907c12212d4d9199ee7f44cb6a3c98cc3fd0d728fa3f16d70b378379dc499ac` | public Go library target for the extension import package |
| `import.go` | 18 | `4b089e78e76e227ba680925d1e834fa48707cf12` | `59e29271efea189e2110d85f813818aab786d10e2fa1d674446eb8fe983c0486` | empty package declaration and generator-placement documentation |

The current package has no executable Go behavior. It is a stable landing
directory for autogenerated blank imports that register extensions located
outside the TiDB repository. No such generated file is present at the pinned
revision, and the Bazel target contains only the empty package declaration.

## Rust ownership and explicit boundary

The Rust workspace has no external-extension import generator or Cargo build
hook corresponding to this Go-only integration directory. Because the pinned
Go package contains neither a registered extension nor executable behavior,
creating a Rust registry entry or placeholder crate would invent behavior and
an ownership boundary absent from the source package.

No Rust-only behavior was found to remove and no compatibility API was added.
The external generator/build integration remains an explicit boundary; any
future generated Go artifact must be inventoried as part of this same atomic
package before its registration behavior can be claimed.

## Validation and risk

Profile: **WIP** for this documentation-only boundary record. No Go source,
imports, Bazel metadata, or module files changed; `make bazel_prepare` and the
Ready lint gate are not required.

```text
PATH=/Users/chenhuansheng/.cache/codex-go1.25.10/go/bin:$PATH \
GOPATH=/Users/chenhuansheng/.cache/codex-gopath-1.25.10 \
go test ./pkg/extension/_import -count=1
# passed: package compiled; no test files
```

Not verified here: an out-of-repository extension generator, Bazel execution,
or full workspace tests. Existing unrelated session/planner worktree changes
remain outside this receipt.
