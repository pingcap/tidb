# b023 — port pkg/util/intest unit tests to tidb-util

## Checklist

| Go test (pkg/util/intest/assert_test.go) | Rust test | Status |
| --- | --- | --- |
| TestAssert | tidb-util::tests_intest::assert_cases | ported |

Notes: Go's `checkAssert`/`checkAssertNotNil`/`checkFuncAssert`/`checkAssertNoError`
helpers are all covered inside the single Rust test, mirroring `doCheckAssert`.
Go's typed nil / non-nil pointer cases map to `Option::None` / `Some(...)`
(the crate's established modeling). The disabled-assertion branch
(`no_assert.go`) is exercised by toggling `ENABLE_ASSERT` /
`ENABLE_INTERNAL_CHECK`, matching the Go build-tag variants without adding a
second implementation authority. No golden files needed.

No go-parity gaps.

## Baseline failures

None. Baseline run on clean tree:

```
Summary [   4.743s] 709 tests run: 709 passed, 10 skipped
```

## Commands

- Baseline (before edits): `cargo nextest run --locked -p tidb-util -E 'not test(/bench/)' --no-fail-fast`
  → 709 run, 709 passed, 10 skipped, 0 failed.
- After edits: same command → 710 run, 710 passed, 10 skipped, 0 failed.
- `git diff --check` → clean.

Failure-set subset property trivially holds (both sets empty).

## Not verified locally

- Nothing; full assigned-crate gate executed.
