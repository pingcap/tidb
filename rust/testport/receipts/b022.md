# b022 — port `pkg/util/fastrand` unit tests to Rust

Gate scope: crate `tidb-util`.
Go source read from origin/master snapshot: `pkg/util/fastrand/{random.go,runtime.go,random_test.go,main_test.go}`.

## Checklist

| Go test | Rust test | Status |
| --- | --- | --- |
| TestRand (random_test.go) | `tidb-util::tests_fastrand::rand` | ported |
| BenchmarkFastRandBuf (random_test.go) | `tidb-util::tests_fastrand::benchmark_fast_rand_buf` | ignored-gap — Go benchmark, not a unit test; kept as `#[ignore]` opt-in smoke check |
| BenchmarkFastRandUint32N | `tidb-util::tests_fastrand::benchmark_fast_rand_uint32_n` | ignored-gap — same as above |
| BenchmarkFastRand | `tidb-util::tests_fastrand::benchmark_fast_rand` | ignored-gap — benchmarks runtime-linked `Uint32`; smoke-checked |
| BenchmarkGlobalRand | `tidb-util::tests_fastrand::benchmark_global_rand` | ignored-gap — compares against Go's global `math/rand`; only fastrand side smoked |
| TestMain (main_test.go) | n/a | skipped-reason: Go-only goleak/global-setup hook; no background workers on the Rust side |

Note: the batch brief said ~6 Go test functions; master actually contains 1 test
+ 4 benchmarks + TestMain. The package was already transcreated with inline
unit tests in `src/fastrand/random.rs`; this batch adds the
crate-convention `src/tests_fastrand.rs` port pinning `TestRand` and the
benchmarks.

## Baseline failures (clean tree)

```
baseline-failing: tidb-util memory::arbitrator::tests::bench::bench
```

## Commands

- Baseline: `cargo nextest run --locked -p tidb-util --no-fail-fast`
  → `Summary [8.816s] 709 tests run: 708 passed, 1 failed, 1 skipped`
  (FAIL `memory::arbitrator::tests::bench::bench`)
- After edits: identical command →
  `Summary [15.048s] 710 tests run: 709 passed, 1 failed, 5 skipped`
  (same single pre-existing failure; failure set is a subset of baseline)
- `git diff --check` → clean

## Not verified locally

- The `#[ignore]`d benchmark smoke tests were not executed.
- Pre-existing failure `memory::arbitrator::tests::bench::bench` is unrelated
  to this batch (flaky count assertion 1521 vs 1530) and was not investigated.
