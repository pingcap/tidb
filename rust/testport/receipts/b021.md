# `pkg/util/encrypt` — complete package transcreation

Pinned TiDB source: `e2788410d8d696605e8cb002585877a063ccc909`.
Pinned toolchain declared by that source: Go 1.25.10.

## Complete inventory

The package has exactly eight artifacts, all read in full:

- `aes.go` and `aes_test.go` — PKCS#7, AES ECB/CBC/OFB/CTR/CFB, MySQL key
  derivation, all fourteen source tests, and their NIST/golden vectors;
- `aes_layer.go` and `aes_layer_test.go` — random-access CTR cipher, buffered
  writer, positional reader, four composition cases, and `BenchmarkReadAt`;
- `crypt.go` and `crypt_test.go` — legacy SQL ENCODE/DECODE and both golden
  table tests;
- `main_test.go` — common test setup and leak checking;
- `BUILD.bazel` — one library and one flaky short test target.

There is no `doc.go`, generated/platform source, fixture, testdata, fuzz target,
example, or additional harness.

## Rust ownership and audit result

`rust/crates/tidb-util/src/encrypt/{aes,aes_layer,crypt}.rs` owns production
behavior and source-named tests. `rust/crates/tidb-util/benches/encrypt.rs`
owns the source benchmark. Live consumers are the expression crypto builtins,
checksum composition, and plaintext/encrypted chunk spill stacks.

The earlier audit had already removed the public export of Go's private block
size, equality/cloning from the error representation, and a Rust-only negative
offset refusal. This audit found and fixed three remaining source differences:

- writer cache offsets and positional-reader cursors now use explicit wrapping
  arithmetic, matching Go `int64` instead of panicking in debug builds;
- `pkcs7_unpad` returns a borrowed prefix of its input, matching Go's returned
  subslice instead of allocating a separate vector;
- invalid IVs panic with the constructor-specific Go 1.25.10 sentinel,
  including the AES-optimized CTR path's `bad IV length` value.

The `testexport` deterministic CTR constructor remains excluded from default
production builds. It is the Rust cross-crate equivalent of same-package test
access and exists only so chunk tests can replay Go-generated encrypted spill
bytes; removing it would remove parity evidence rather than production
behavior.

The seventeen source behavior tests remain. Three focused regressions cover
source-defined aliasing, panic, and overflow behavior not reached by the source
test inputs. The Go `TestMain` leak harness has no Rust runtime analogue because
this package creates no background worker.

## Validation

Profile: WIP; this is one completed package within the continuing repository
audit, not a repository-wide readiness claim.

- `go test ./pkg/util/encrypt`
- `cargo test --offline --locked -p tidb-util --lib 'encrypt::'`
- `cargo test --offline --locked -p tidb-util`
- `cargo test --offline --locked -p tidb-chunk 'encrypted_'`
- `cargo test --offline --locked -p tidb-expr crypto`
- `cargo check --offline --locked -p tidb-util -p tidb-chunk -p tidb-expr --all-targets`
- `cargo clippy --offline --locked -p tidb-util --lib --no-deps -- -A clippy::needless-borrows-for-generic-args -A clippy::chunks-exact-to-as-chunks -A clippy::new-without-default -D warnings`
- `cargo fmt --all --check`
- `git diff --check`

No Go or Bazel file changed, so `make bazel_prepare` is not required.

## Risk

- Correctness: reduced; extreme offsets, returned-slice identity, and invalid-IV
  failures now follow the pinned Go paths.
- Compatibility: `pkcs7_unpad` now exposes Go's borrowed result; its only Rust
  production consumers are adapted internally. No default production export
  was added.
- Performance: unpadding removes one allocation. Normal-range AES and spill
  arithmetic compiles to the same operations; cipher and I/O geometry are
  unchanged.
