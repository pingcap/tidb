# Batch b029 — port `pkg/util/slice` unit tests

## Checklist

| Go test | Rust test | Status |
| --- | --- | --- |
| pkg/util/slice/slice_test.go TestSlice | tidb-util::tests_slice::slice_all_of_matches_go_table_cases | ported |
| pkg/util/slice/main_test.go TestMain | (Go test harness main; no behavior to port) | skipped-reason |

Notes: the Go package contains exactly one test function (`TestSlice`),
table-driving `AllOf` with an even predicate over 4 cases including the empty
slice. Ported against the Rust mapping in `rust/crates/tidb-util/src/slice.rs`
(`all_of`). No golden/testdata files are used by the Go test. No unsupported
behavior found; no `go-parity-gap` ignores needed. An equivalent inline
`#[cfg(test)] mod tests` already existed in `slice.rs`; this batch adds the
convention-named sibling module `tests_slice.rs` without duplicating
infrastructure.

## Baseline failures

None — baseline gate was fully green:

```
Summary [   4.477s] 708 tests run: 708 passed, 6 skipped
```

(no `baseline-failing:` lines to record)

## Commands

- Baseline: `cargo nextest run --locked -p tidb-util -E 'not test(/bench/)' --no-fail-fast`
  → 708 run: 708 passed, 6 skipped, 0 failed.
- After edits: same command → `Summary [   4.623s] 709 tests run: 709 passed, 6 skipped`
  (added test passes; failure set still empty ⊆ baseline).
- `git diff --check` → clean.

## Not verified locally

Nothing outstanding for this batch.
