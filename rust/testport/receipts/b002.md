# Batch b002 — port `pkg/parser/mysql` unit tests to Rust

Owning crate: `tidb-mysql` (gate scope only). Go tests read from `origin/master`.

## Status

On inspection, all 11 Go test functions from the assigned packages already had
faithful ports in `rust/crates/tidb-mysql/tests/parser_mysql_package_source.rs`.
This batch therefore re-derived each Go test's intent from the origin/master Go
source and verified the existing Rust port line by line; no test-code edits were
required (receipt-only commit).

### Checklist

| Go file / test | Rust test | Status |
| --- | --- | --- |
| const_test.go TestSQLMode | parser_mysql_package_source::test_sql_mode | ported (verified: bit positions 0..31 asserted as `1<<bit`, plus ModeAllowInvalidDates=1<<32; equivalent to Go's hard-coded hex table) |
| const_test.go TestVersionSeparator | ::test_version_separator | ported (`VersionSeparator == "-TiDB-"`) |
| const_test.go TestBuildTiDBXReleaseVersion | ::test_build_tidbx_release_version | ported (all 4 positive rows + 5 error rows) |
| const_test.go TestNormalizeTiDBReleaseVersionForNextGen | ::test_normalize_tidb_release_version_for_next_gen | ported (placeholders match Go const.go lines 40/43) |
| error_test.go TestSQLError | — | skipped-reason: `NewErr`/`NewErrf` live in the `tidb-error` crate, outside this batch's gate scope and edit scope; behavior partially covered by `tidb-error::mysql::error::tests::source_error_rendering_and_redaction` (new(ErrNoDB), new_f(0,"customized error")) |
| privs_test.go TestPrivString | ::test_priv_string | ported (bits 0..=33 inclusive, matching Go's loop until p > AllPriv) |
| privs_test.go TestPrivColumn | ::test_priv_column | ported (AllGlobalPrivs ∪ StaticGlobalOnlyPrivs ∪ AllDBPrivs → ColumnString round-trip via privilege_from_column) |
| privs_test.go TestPrivSetString | ::test_priv_set_string | ported (AllTablePrivs ∪ AllColumnPrivs → SetString round-trip via privilege_from_set_enum) |
| privs_test.go TestPrivsHas | ::test_privs_has | ported |
| privs_test.go TestPrivAllConsistency | ::test_priv_all_consistency | ported (bits 1..=32 in Priv2UserCol-equivalent; len relations len(AllGlobalPrivs)+1 == len(Priv2UserCol), len(Priv2UserCol)+2 == len(Priv2Str)) |
| type_test.go TestFlags | ::test_flags | ported (all flag predicates from Go source) |

No `go-parity-gap` ignores were needed.

## Baseline failures (clean tree)

baseline-failing: tidb-mysql::parser_mysql_package_source generated_source_authorities_match_the_go_oracle

(Pre-existing environment failure: generator check reports "Go version drift:
expected go1.26.0, got go1.27.0" — local toolchain newer than pinned. Unrelated
to the ported tests.)

## Commands run

- `cargo nextest run --locked -p tidb-mysql --no-fail-fast`
  - Summary: `18 tests run: 17 passed, 1 failed, 0 skipped`
  - FAIL: `tidb-mysql::parser_mysql_package_source generated_source_authorities_match_the_go_oracle` (pre-existing baseline failure)
- Re-run after receipt-only change: same result; all 11 ported tests pass;
  failure set unchanged (identical to baseline).
- `git diff --check`: clean.

## Not verified locally

- `generated_source_authorities_match_the_go_oracle` could not pass locally due
  to Go toolchain version drift (go1.27.0 vs expected go1.26.0); pre-existing,
  environment-only.
