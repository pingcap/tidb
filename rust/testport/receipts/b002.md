# Batch b002 — port pkg/parser/mysql unit tests

Go package: `pkg/parser/mysql` (read from `origin/master`).
Owning crate: `tidb-mysql` (integration test file
`rust/crates/tidb-mysql/tests/parser_mysql_package_source.rs`).

## Checklist

| Go test (origin/master) | Rust test | Status |
| --- | --- | --- |
| const_test.go::TestSQLMode | `parser_mysql_package_source::test_sql_mode` | ported (pre-existing) |
| const_test.go::TestVersionSeparator | `test_version_separator` | ported (pre-existing) |
| const_test.go::TestBuildTiDBXReleaseVersion | `test_build_tidbx_release_version` | ported (pre-existing) |
| const_test.go::TestNormalizeTiDBReleaseVersionForNextGen | `test_normalize_tidb_release_version_for_next_gen` | ported (pre-existing) |
| error_test.go::TestSQLError | `test_sql_error` | ported (added this batch; uses `tidb_error::mysql::{SqlError, FormatArg}` where the NewErr/NewErrf transcreation lives) |
| privs_test.go::TestPrivString | `test_priv_string` | ported (pre-existing) |
| privs_test.go::TestPrivColumn | `test_priv_column` | ported (pre-existing) |
| privs_test.go::TestPrivSetString | `test_priv_set_string` | ported (pre-existing) |
| privs_test.go::TestPrivsHas | `test_privs_has` | ported (pre-existing) |
| privs_test.go::TestPrivAllConsistency | `test_priv_all_consistency` | ported (pre-existing) |
| type_test.go::TestFlags | `test_flags` | ported (pre-existing) |

No go-parity-gap ignores required.

## Baseline failures (clean tree, before edits)

baseline-failing: tidb-mysql::parser_mysql_package_source generated_source_authorities_match_the_go_oracle

(Pre-existing environmental failure: generator check reports "Go version drift:
expected go1.26.0, got go1.27.0". Unrelated to this batch.)

## Commands run and summaries

- Gate (baseline, before editing):
  `cargo nextest run --locked -p tidb-mysql --no-fail-fast`
  → `Summary [0.298s] 18 tests run: 17 passed, 1 failed, 0 skipped`
- Gate (after adding `test_sql_error`):
  `cargo nextest run --locked -p tidb-mysql --no-fail-fast`
  → `Summary [0.293s] 19 tests run: 18 passed, 1 failed, 0 skipped`
  Failure set is unchanged from baseline (subset property holds); all newly
  added tests pass.
- `git diff --check` → clean.

## Not verified locally

- The Go-side behavior itself was not re-executed (no Go toolchain matching
  go1.26.0 in this environment); assertions were derived by reading the Go
  test sources and the exercised production code on `origin/master`.
- The pre-existing `generated_source_authorities_match_the_go_oracle` failure
  was not investigated/fixed (out of batch scope).
