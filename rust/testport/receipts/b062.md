# testport batch b062 — pkg/store/mockstore (part2) → tidb-protocol

Gate command (as assigned):
`cargo nextest run --locked -p tidb-protocol -E 'not test(/bench/)' --no-fail-fast`

## Scope definition

`pkg/store/mockstore.part2` is the tail slice of the 65 inventory items under
`pkg/store/mockstore/**` on `origin/master`, sorted by (file path, line
number). The last 5 test functions are:

- `pkg/store/mockstore/unistore/tikv/util_test.go`: `TestExceedEndKey`,
  `TestSortAndDedupHashVals`, `TestSafeCopy`
- `pkg/store/mockstore/unistore/util/lockwaiter/lockwaiter_test.go`:
  `TestLockwaiterBasic`, `TestLockwaiterConcurrent`

(The top-level `pkg/store/mockstore` package itself contributes only
`TestClusterSplit` and `TestConfig`, which belong to part1's slice.)

## Crate-mapping note

The manifest maps this base package to crate `tidb-protocol`, but the Rust
transcreation of the unistore code lives in `tidb-unistore`
(`rust/crates/tidb-unistore`); `tidb-protocol` contains only the MySQL packet
framing and has no mockstore symbols. As in batch b050, the mandated gate was
run verbatim AND the owning-crate gate was run alongside it; both are
reported. Test-code edits are confined to `rust/crates/tidb-unistore`
(`src/tests_tikv_util_go_parity.rs` + its `mod` registration in `src/lib.rs`)
plus this receipt.

## Checklist

| Go test | Rust test(s) | Status |
| --- | --- | --- |
| TestExceedEndKey | tests_tikv_util_go_parity::exceed_end_key_table_vectors | ignored-gap: Go helper `exceedEndKey` (`tikv/util.go:26`) has no Rust transcreation anywhere in the workspace; no Rust symbol to exercise, so the 7 table vectors are pinned in an `#[ignore]`d test body |
| TestSortAndDedupHashVals | tests_tikv_util_go_parity::sort_and_dedup_hash_vals_table_vectors | ignored-gap: `sortAndDedupHashVals` (`tikv/util.go:34`) not transcreated; 8 vectors pinned under `#[ignore]` |
| TestSafeCopy | tests_tikv_util_go_parity::safe_copy_table_vectors | ignored-gap: `safeCopy` (`tikv/util.go:69`) not transcreated; independence-from-source semantics documented under `#[ignore]` |
| TestLockwaiterBasic | lockwaiter::tests::test_lockwaiter_basic (`src/lockwaiter.rs`) | ported (existing) — verified line-by-line against origin/master incl. the Go quirk that the final map check probes key 4 (the lock TS), not the key hash |
| TestLockwaiterConcurrent | lockwaiter::tests::test_lockwaiter_concurrent (`src/lockwaiter.rs`) | ported (existing) — verified: same rendezvous (Go `sync.WaitGroup` → Barrier), even-numbers woken by commit / odd by deadlock, `numbers-1` cleaned up and observing `WaitTimeout` with the 100ms timeout kept semantic |

No behavior was approximated to make a test pass.

## Baseline failures

Baseline on clean tree before edits:

```
cargo nextest run --locked -p tidb-protocol -E 'not test(/bench/)' --no-fail-fast
  → Summary [   0.578s] 111 tests run: 111 passed, 0 skipped
cargo nextest run --locked -p tidb-unistore --no-fail-fast
  → Summary [   2.076s] 104 tests run: 104 passed, 1 skipped
    (pre-existing #[ignore]: iterator.rs "Go skips this: unstable arena-block assertion, TiDB #26235")
```

`baseline-failing:` none.

## Commands run and summaries

1. Baseline (before any edit):
   - `cargo nextest run --locked -p tidb-protocol -E 'not test(/bench/)' --no-fail-fast`
     → `111 tests run: 111 passed, 0 skipped`
   - `cargo nextest run --locked -p tidb-unistore --no-fail-fast`
     → `104 tests run: 104 passed, 1 skipped`
2. Final (identical commands, after edits):
   - tidb-protocol → `111 tests run: 111 passed, 0 skipped` (failure set unchanged: empty)
   - tidb-unistore → `104 tests run: 104 passed, 4 skipped` (+3 = the new
     `#[ignore]`d gap pins; nothing newly failing)
3. `git diff --check` → clean.

Subset property holds: the final failure sets are identical to baseline
(tidb-protocol: none; tidb-unistore: none).

## Not verified locally

- The three gap-pinned util_test.go behaviors remain unexercised on the Rust
  side by construction (the helpers are not transcreated); they will need
  porting once `tikv/util.go` lands in the workspace.
- Go tests were not executed on the Go side; intent was re-derived from the
  master sources (`git show origin/master:...`) as required.
