diff --git a/Cargo.toml b/Cargo.toml
index d391817..e236dda 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -11,6 +11,10 @@ rust-version = "1.93.0"
 
 [features]
 default = ["prometheus"]
+# Expose the in-process mock PD/TiKV backend outside test builds, for
+# downstream crates (e.g. TiDB's tidb-unistore) that need a production-usable
+# in-process backend, not just #[cfg(test)] support within this crate.
+mock = ["dep:tokio-stream"]
 prometheus = ["prometheus/push", "prometheus/process"]
 # Enable integration tests with a running TiKV and PD instance.
 # Use $PD_ADDRS, comma separated, to set the addresses the tests use.
@@ -34,6 +38,7 @@ members = [
 ]
 
 [dependencies]
+tokio-stream = { version = "0.1", features = ["net"], optional = true }
 async-recursion = "0.3"
 async-trait = "0.1"
 chrono = { version = "0.4", default-features = false, features = ["std"] }
@@ -46,9 +51,9 @@ log = "0.4"
 openssl = "0.10"
 pin-project = "1"
 prometheus = { version = "0.13", default-features = false }
-prost = "0.13"
+prost = "0.14"
 prost-reflect = "=0.15.3"
-prost-types = "0.13"
+prost-types = "0.14"
 rand = "0.8"
 regex = "1"
 crc32fast = "1"
@@ -65,7 +70,8 @@ thiserror = "1"
 tikv-client-kvproto = { path = "kvproto" }
 unistore = { path = "unistore" }
 tokio = { version = "1.21", features = ["sync", "rt-multi-thread", "macros", "time", "net", "io-util"] }
-tonic = { version = "0.12", features = ["tls", "gzip"] }
+tonic = { version = "0.14", features = ["tls-ring", "gzip"] }
+tonic-prost = "0.14"
 url = "2"
 
 [dev-dependencies]
diff --git a/kvproto/Cargo.toml b/kvproto/Cargo.toml
index dab675a..dfa99f1 100644
--- a/kvproto/Cargo.toml
+++ b/kvproto/Cargo.toml
@@ -10,6 +10,7 @@ description = "Shared prost/tonic bindings for the kvproto revision used by tikv
 publish = false
 
 [dependencies]
-prost = "0.13"
-prost-types = "0.13"
-tonic = "0.12"
+prost = "0.14"
+prost-types = "0.14"
+tonic = "0.14"
+tonic-prost = "0.14"
diff --git a/proto-build/Cargo.toml b/proto-build/Cargo.toml
index a3a5a7a..91a927a 100644
--- a/proto-build/Cargo.toml
+++ b/proto-build/Cargo.toml
@@ -12,7 +12,7 @@ edition = "2021"
 
 [dependencies]
 glob = "0.3"
-prost-build = "0.12"
-tonic-build = { version = "0.10", features = ["cleanup-markdown"] }
+prost-build = "0.14"
+tonic-prost-build = "0.14"
 # Keep this compatible with rust-toolchain 1.84.1.
 tempfile = "=3.14.0"
diff --git a/proto-build/src/main.rs b/proto-build/src/main.rs
index cf347df..03c3b85 100644
--- a/proto-build/src/main.rs
+++ b/proto-build/src/main.rs
@@ -23,14 +23,18 @@ fn main() -> Result<(), Box<dyn std::error::Error>> {
     let mut prost = prost_build::Config::new();
     prost.enable_type_names();
 
-    tonic_build::configure()
+    tonic_prost_build::configure()
         .emit_rerun_if_changed(false)
         .build_server(true)
         .server_mod_attribute(".", "#[allow(non_camel_case_types)]")
         .include_file("mod.rs")
         .out_dir(staging.path())
         .file_descriptor_set_path(staging.path().join(DESCRIPTOR_FILE))
-        .compile_with_config(prost, &protos, &["proto/include", "proto"])?;
+        .compile_with_config(
+            prost,
+            &protos,
+            &["proto/include".into(), "proto".into()],
+        )?;
 
     install_generated_output(staging.path(), Path::new(GENERATED_DIR))?;
     Ok(())
