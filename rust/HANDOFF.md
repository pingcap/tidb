# Active package: `pkg/parser/duration`

## Done when

- Every production, test, and support artifact under `pkg/parser/duration` has a
  Rust equivalent, and the Go, Rust, differential, lint, and live checks pass.

## Now

- `pkg/parser/ast`, `pkg/parser/format`, `pkg/parser/opcode`, and
  `pkg/parser/util`, `pkg/parser/auth`, and `pkg/parser/charset` are closed as
  complete packages.
- Transcreate `pkg/parser/duration` as one complete package.

The package is open.
