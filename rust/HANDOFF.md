# Active package: `pkg/parser/auth`

## Done when

- Every production, test, and support artifact under `pkg/parser/auth` has a
  Rust equivalent, and the Go, Rust, differential, lint, and live checks pass.

## Now

- `pkg/parser/ast`, `pkg/parser/format`, `pkg/parser/opcode`, and
  `pkg/parser/util` are closed as complete packages.
- Transcreate `pkg/parser/auth` as one complete package.

The package is open.
