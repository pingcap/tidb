# Active package: `pkg/parser/format`

## Done when

- Every production, test, and support artifact under `pkg/parser/format` has a
  Rust equivalent, and the Go, Rust, differential, lint, and live checks pass.

## Now

- `pkg/parser/ast` is closed: all production, test, and support owners were
  transcreated and its package-close checks passed.
- Transcreate `pkg/parser/format` as one complete package.

The package is open.
