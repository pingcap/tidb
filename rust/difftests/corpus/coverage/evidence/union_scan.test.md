# `tests/integrationtest/t/executor/union_scan.test` evidence

The upstream fixture remains `PARTIAL`. These stateful differential slices are
independent, Go-generated corpus topics from the same source fixture.

| Upstream block | Rust artifact | What it proves |
|---|---|---|
| `TestIssue36903`, lines 187-194 | [`transaction_modes.txt`](../../table/transaction_modes.txt) | `BEGIN OPTIMISTIC` parses, restores, and executes with the seed executor's explicit transaction snapshot. |
| `TestForApplyAndUnionScan`, lines 198-206 | [`setop_fixture.txt`](../../table/setop_fixture.txt) | Transaction-local correlated-subquery results remain visible through commit. |

Both golden files are generated by `gorun` and asserted by `table_diff`.
