# `tests/integrationtest/t/executor/foreign_key.test` evidence

The integration fixture remains `PARTIAL`. Each linked corpus pair is a
stateful, Go-generated differential slice of the one upstream fixture; neither
claim represents every source block.

| Upstream block | Rust artifact | What it proves |
|---|---|---|
| `TestForeignKeyOnInsertIgnore`, lines 4-11 | [`foreign_key_ignore.txt`](../../table/foreign_key_ignore.txt) | `INSERT IGNORE` skips only child rows that violate a foreign key; valid and `NULL` rows remain. |
| `TestForeignKey`, lines 42-48 | [`foreign_key_fixture.txt`](../../table/foreign_key_fixture.txt) | Basic child-side foreign-key creation and valid inserts. |

Both golden files are generated by `gorun` and are asserted by `table_diff`.
