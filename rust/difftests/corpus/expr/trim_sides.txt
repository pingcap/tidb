## Go vectors from pkg/expression/builtin_string_test.go TestLTrim/TestRTrim.
## These trim only ASCII U+0020 spaces: tabs, CR, and LF deliberately remain.
hex(ltrim('   bar   '))
hex(ltrim('\t   bar   '))
hex(ltrim('   \tbar   '))
hex(ltrim('\r   bar   '))
hex(ltrim('   \rbar   '))
hex(ltrim('\n   bar   '))
hex(ltrim('   \nbar   '))
hex(ltrim(''))
hex(ltrim(null))
ltrim(123)
hex(rtrim('   bar   '))
hex(rtrim('bar     \n'))
hex(rtrim('bar\n     '))
hex(rtrim('bar     \r'))
hex(rtrim('bar\r     '))
hex(rtrim('bar     \t'))
hex(rtrim('bar\t     '))
hex(rtrim(''))
hex(rtrim(null))
rtrim(123)
