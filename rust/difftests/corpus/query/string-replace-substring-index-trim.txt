## Go vectors from pkg/expression/builtin_string_test.go (TestReplace,
## TestSubstringIndex, and TestTrim).  Charset/session-only error paths are
## represented by direct Rust scalar tests rather than SQL result rows.
select replace('www.mysql.com', 'mysql', 'pingcap')
select replace('www.mysql.com', 'w', 1)
select replace(1234, 2, 55)
select replace('', 'a', 'b')
select replace('abc', '', 'd')
select replace('aaa', 'a', '')
select replace(NULL, 'a', 'b')
select replace('a', NULL, 'b')
select replace('a', 'b', NULL)
select substring_index('www.pingcap.com', '.', 2)
select substring_index('www.pingcap.com', '.', -2)
select substring_index('www.pingcap.com', '.', 0)
select substring_index('www.pingcap.com', '.', 100)
select substring_index('www.pingcap.com', '.', -100)
select substring_index('www.pingcap.com', 'd', 0)
select substring_index('www.pingcap.com', 'd', 1)
select substring_index('www.pingcap.com', 'd', -1)
select substring_index('www.pingcap.com', '', 0)
select substring_index('www.pingcap.com', '', 1)
select substring_index('www.pingcap.com', '', -1)
select substring_index('www.pingcap.com', '', NULL)
select substring_index('', '.', 0)
select substring_index('', '.', 1)
select substring_index('', '.', -1)
select substring_index(NULL, '.', 1)
select substring_index('www.pingcap.com', NULL, 1)
select substring_index('www.pingcap.com', '.', NULL)
select substring_index('www.pingcap.com', '.', '2')
select substring_index('www.pingcap.com', '.', 2.5)
select trim('   bar   ')
select trim('')
select trim(NULL)
select trim('x' from 'xxxbarxxx')
select trim('x' from 'bar')
select trim('' from '   bar   ')
select trim('x' from '')
select trim(NULL from 'bar')
select trim('x' from NULL)
select trim(leading 'x' from 'xxxbarxxx')
select trim(trailing 'xyz' from 'barxxyz')
select trim(both 'x' from 'xxxbarxxx')
select hex(replace(unhex('FF00'), unhex('FF'), 'a'))
select hex(substring_index(unhex('61FF62FF63'), unhex('FF'), -2))
