## Source: tests/integrationtest/t/executor/foreign_key.test:4-11
## (TestForeignKeyOnInsertIgnore). The source's session settings enable FK
## checks; this seed executor enforces declared FKs unconditionally and has no
## global-server configuration surface, so this is the exact DML sub-block.
## Semicolon terminators are omitted by corpus convention.
drop table if exists t1, t2, t3
CREATE TABLE t1 (i INT PRIMARY KEY)
CREATE TABLE t2 (i INT, FOREIGN KEY (i) REFERENCES t1 (i))
INSERT INTO t1 VALUES (1),(3)
INSERT IGNORE INTO t2 VALUES (1), (null), (1), (2),(3),(4)
select * from t2 order by i
