#IDX 0
SELECT GROUP_CONCAT(DISTINCT `name` ORDER BY `name` DESC SEPARATOR ',') FROM `t`
#END
#IDX 1
SELECT GROUP_CONCAT(`name` ORDER BY `name` DESC SEPARATOR '++'),GROUP_CONCAT(`id` ORDER BY `name` DESC,`id` SEPARATOR '--') FROM `t`
#END
#IDX 2
SELECT GROUP_CONCAT(`a`, `b` ORDER BY 1 DESC,`a` SEPARATOR ',') FROM `t`
#END
#IDX 3
SELECT GROUP_CONCAT(`name` ORDER BY `name` SEPARATOR ',') FROM `t`
#END
