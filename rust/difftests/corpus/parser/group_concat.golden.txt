#IDX 167
SELECT GROUP_CONCAT(`a` SEPARATOR ',') FROM `t` GROUP BY `b`
#END
#IDX 168
SELECT GROUP_CONCAT(DISTINCT `a` SEPARATOR ',') FROM `t` GROUP BY `b`
#END
#IDX 169
SELECT GROUP_CONCAT(`a` SEPARATOR '-') FROM `t` GROUP BY `b`
#END
#IDX 170
SELECT GROUP_CONCAT(`a`, `b` SEPARATOR ',') FROM `t` GROUP BY `c`
#END
#IDX 171
SELECT GROUP_CONCAT(`a` SEPARATOR ', ') FROM `t`
#END
