[package]
name = "tidb-dxf-operator"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "operator"

[dependencies]
crossbeam-channel = "0.5"
tidb-resourcemanager = { path = "../tidb-resourcemanager" }

[dev-dependencies]
regex = "1"
