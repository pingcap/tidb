schema = "1"
slice = "server-live-multicolumn-tikv-proof"
status = "ready"
target = "result-tests"
ring = "result"
consumer = "A stock mysql client requests a reordered aliased projection from a real two-column TiKV table and receives exact clustered-key and stored signed-BIGINT values while the retained PD/BatchCommands and cleanup evidence remains nonzero and exact."
test_target = "realtikv_multicolumn_sql_node"
go_sources = ["tests/realtikvtest/testkit.go"]
go_tests = []
depends_on = []
evidence_prerequisites = []
rust_paths = [
  "rust/scripts/run-campaign20-multicolumn-sql-node.sh",
]
