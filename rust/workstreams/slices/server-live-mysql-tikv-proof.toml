schema = "1"
slice = "server-live-mysql-tikv-proof"
status = "partial"
target = "result-tests"
ring = "result"
consumer = "A stock mysql client reads seeded nonempty rows through the Rust SQL node and real PD/TiKV while structured evidence proves nonzero TSO, region discovery, BatchCommands-first transport, connection cleanup, and tag-owned topology cleanup."
test_target = "realtikv_sql_node"
go_sources = ["tests/realtikvtest/testkit.go"]
go_tests = []
depends_on = []
evidence_prerequisites = []
rust_paths = [
  "rust/scripts/run-campaign19-sql-node.sh",
]
