schema = "2"
slice = "server-internal-handshake-package"
status = "ready"
targets = ["tidb-server"]
rings = ["unassigned"]
consumer = "Provides the complete Rust-native Response41 data contract consumed by later handshake parsing and server packages; nil map/slice normalize to empty owned collections and Go strings to Rust UTF-8, while downstream parse/auth/compression/lifecycle behavior is not credited here."
test_target = "handshake_response_package_source"
go_packages = ["pkg/server/internal/handshake"]
module_packages = []
depends_on = []
rust_paths = [
  "rust/crates/tidb-server/src/handshake_response.rs",
  "rust/crates/tidb-server/tests/handshake_response_package_source.rs",
]
