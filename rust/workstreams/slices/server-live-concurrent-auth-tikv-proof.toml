schema = "1"
slice = "server-live-concurrent-auth-tikv-proof"
status = "partial"
target = "tidb-server"
ring = "result"
consumer = "A stock MySQL client opens at least three overlapping non-root password-authenticated sessions to one Rust SQL-node process, all query the configured table through one real PD/TiKV authority, return exact stored rows with distinct connection and snapshot identities, prove at least two simultaneously active sessions, and leave no active connection or tag-owned TiUP process after cleanup."
test_target = "run-campaign21-concurrent-auth-sql-node"
go_sources = [
  "tests/realtikvtest/testkit.go",
]
go_tests = []
module_sources = []
module_tests = []
depends_on = [
  "server-concurrent-authenticated-session-runtime",
]
evidence_prerequisites = []
rust_paths = [
  "rust/scripts/run-campaign21-concurrent-auth-sql-node.sh",
]
