schema = "1"
slice = "planner-read-only-clustered-pk-range-runtime"
status = "ready"
target = "tidb-planner"
ring = "plan"
consumer = "The configured read-only planner detaches clustered-primary-key access conditions into logical ranges, retains stored-column and non-detachable conditions in physical Selection, and exposes both through one physical table-reader plan."
test_target = "read_only_clustered_pk_range_source"
go_sources = [
  "pkg/planner/core/stats.go",
  "pkg/planner/core/operator/logicalop/logical_datasource.go",
  "pkg/planner/core/operator/physicalop/physical_table_scan.go",
]
go_tests = []
module_sources = []
module_tests = []
depends_on = []
evidence_prerequisites = []
support_anchors = [
  "Campaign 24 stage B consumes the stage-A ranger interface after it freezes",
  "Access conditions narrow ranges; residual conditions remain in the exact Campaign 23 TableScan-plus-Selection path",
  "Contradictory PK predicates produce zero ranges and must avoid PD TSO and TiKV transport work",
]
rust_paths = [
  "rust/crates/tidb-planner/src/read_only_scan.rs",
  "rust/crates/tidb-planner/tests/read_only_clustered_pk_range_source.rs",
]
