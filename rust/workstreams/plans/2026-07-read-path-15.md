# Establish bucket, batch-location, and async scheduling authorities

This ExecPlan is a living document. Keep `Progress`, `Surprises & Discoveries`,
`Decision Log`, and `Outcomes & Retrospective` current while implementing it.

Reference: `PLANS.md` at repository root. This plan is self-contained and must
be maintained according to it.

## Purpose / Big Picture

Campaign 15 directly transits the two dependency chains that Campaign 14 made
visible but could not honestly implement: PD bucket wire before RegionCache
bucket/batch behavior, and completion/scheduling before BatchCommands async
requests. The campaign is admitted as 11 exact production sources and 53 exact
original obligations, with pairwise-disjoint Rust write sets and one delayed
12-job integration gate.

The observable result is not a Rust-only approximation. A pinned PD response
can carry exact bucket metadata and ordered batch-scan regions into the sole
RegionCache authority; the cache applies Go's strict TTL, half-open traversal,
bucket version/clamp, gap, and range-limit rules. In parallel, a source-shaped
one-shot completion driver and BatchCommands scheduler preserve cancellation,
priority, grouping, observations, and adaptive policy without pretending a
tonic unary future is client-go `SendRequestAsync`.

## Progress

- [x] (2026-07-19) Audited pinned cache, TiFlash, async, batch, proto, and
  original-test families with three parallel read-only agents.
- [x] (2026-07-19) Rejected synthetic cache buckets and unary-as-async as
  source-incompatible shortcuts.
- [x] (2026-07-19) Froze an 11-source/53-obligation campaign as four disjoint
  first-rotation slices.
- [x] (2026-07-19) Landed stable module contracts and created four checked,
  pairwise-disjoint claims/worktrees.
- [x] (2026-07-19) Transited, cross-reviewed, and statically validated all four
  first-rotation leaves plus async once-ordering, scheduler authority, bucket-
  mismatch publication, PD request-finisher, and RegionCache batch-scan root
  corrections before the workspace gate.
- [x] (2026-07-19) Passed all 53 exact pinned Go obligations with `-p 12`
  across client-go locate/client/async and pd-client router packages.
- [x] (2026-07-19) Promoted the reviewed evidence and transfers for the exact
  original campaign scope: all 11 production sources and all 53 test
  obligations are recorded as honest `PARTIAL` mappings.
- [x] (2026-07-19) Passed focused Rust validation, Ready lint, and the full
  frozen gate with `CARGO_BUILD_JOBS=12 scripts/rewrite-gate.sh integrate`;
  the gate issued `integration_receipt 4`.
- [x] (2026-07-19) Released all four claims as `partial`, froze the campaign
  archive and membership, and returned the queue to zero active claims.

## Surprises & Discoveries

- Observation: Rust already asks PD for buckets but its bounded protobuf omits
  `GetRegionResponse.buckets`; cache-only bucket fixtures would therefore prove
  a path production can never receive.
- Observation: `SendRequestAsync` hard-requires the BatchCommands client. It is
  not asynchronous execution of the ordinary unary RPC.
- Observation: full BatchCommands transport and RegionRequest async policy
  overlap at one queue/callback/close authority. The dependency-closed parallel
  work is completion plus scheduler first, then the wire stream and callback.
- Observation: TiFlash recovery shares the canonical RegionCache TTL, labels,
  bucket metadata, and endpoint validation. It is a second rotation of the
  cache authority, not a disjoint first-wave leaf.
- Observation: background GC and coalesced bucket refresh require owned cache
  snapshots and one shared lock. Attaching threads to the current borrowed,
  `&mut self` cache would create a second authority.
- Observation: cross-review caught three source-shape failures before the first
  Rust build: one cancellation condition variable was shared across unrelated
  mutexes, duplicate completion calls reported an error instead of following
  Go's `sync.Once`, and scheduler observations were detached copies rather than
  the state actually advanced by batching.
- Observation: the handwritten batch-policy JSON parser both accepted invalid
  JSON and rejected valid unknown fields. Campaign 15 now uses the workspace's
  existing `serde_json` authority while preserving Go's field types, zero
  defaults, ignored unknown fields, numeric acceptance, and unknown-version
  behavior instead of inventing a stricter policy language.
- Observation: Go's `sync.Once.Do` covers the complete invoke or append body;
  a losing completion call must wait until the winner publishes or queues its
  result. Claiming the once flag before that body created a deterministic empty
  queue race even though ordinary joined-racer tests passed.
- Observation: batch send start is one absolute group timestamp, but every
  request derives a different offset from its own arrival. The maximum response
  request ID is stream-wide, not group-local. Detached relative copies would
  make timeout diagnostics and TiKV-ack inference disagree across entries and
  successive batches.
- Observation: filtering leaderless regions changes the authoritative progress
  boundary. Advancing from the last raw PD region skipped a filtered suffix;
  pinned Go advances from the last valid region and loads the remainder.
- Observation: epoch-not-match captures the observed region's bucket pointer
  once and gives that exact snapshot to every replacement child. Intersection-
  by-intersection inheritance or conditional version comparison can give split
  children different bucket authorities.
- Observation: evidence review removed superficially adjacent test mappings.
  Exact declared obligations remain visible as UNTRIAGED until a Rust artifact
  directly asserts their source behavior; Campaign 15 does not convert naming
  similarity into progress.

## Decision Log

- Decision: order bucket behavior as exact kvproto/PD projection, retained PD
  worker, loader decode, then canonical cache publication.
  Rationale: direct transition starts at the real source boundary; downstream
  tests cannot inject metadata that the live wire discards.
  Date/Author: 2026-07-19 / root.
- Decision: keep full BatchCommands wire/connection lifecycle and async
  RegionRequest for Campaign 16, behind the contracts established here.
  Rationale: both consumers must share one stream, queue, cancellation, close,
  and completion authority; parallel duplicate runtimes would be a workaround.
  Date/Author: 2026-07-19 / root.
- Decision: freeze `AsyncRequestDispatcher::begin` as exactly one batch attempt;
  async RegionRequest will apply the existing selector transition and use a
  synchronous retry after the completion callback, matching pinned Go.
  Rationale: dispatcher owns completion, RegionCache owns feedback, and
  RequestSelector owns attempts.
  Date/Author: 2026-07-19 / root.
- Decision: keep cache background/concurrency and TiFlash as prepared Campaign
  16 successors rather than hiding them behind conditional test doubles.
  Rationale: owned immutable snapshots plus one shared cache state eliminate
  the current edge case instead of layering locks around borrowed entries.
  Date/Author: 2026-07-19 / root.

## Outcomes & Retrospective

Campaign 15 is integrated and archived. The exact frozen scope was 11 original
production sources and 53 original tests. Every scoped original Go test passed,
the final external evidence records 11 source `PARTIAL` rows and 53 test
`PARTIAL` rows, and the full 12-job rewrite gate completed green with
`integration_receipt 4`. All four matching claims were released as `partial`;
the campaign archive is frozen and the queue has zero active claims.

The result is a dependency-closed foundation, not a live-cluster parity claim.
Campaign 16 retains seven named source semantics without folding them into
Campaign 15's evidence:

1. Exact start-inside-region scan cases.
2. Leaderless scan filtering while preserving the requested limit.
3. Unary load redirection from a follower/router response to the authoritative
   leader.
4. Background liveness resolution of an unreachable store in place.
5. Refresh of changed store labels during that in-place resolution.
6. Public region-by-ID lookup that deliberately does not publish to cache.
7. The contrasting `LocateRegionByID` path that does publish to cache.

Full BatchCommands streaming and connection lifecycle, async RegionRequest
retry policy, shared-lock cache concurrency/background GC, coalesced bucket
refresh, TiFlash recovery, and real PD/TiKV socket validation also remain
successor work. Injected PD services and source-shaped unit tests validate the
bounded contracts here; they do not prove live PD/TiKV behavior.

## Context and Orientation

`rust/crates/tidb-pd-client/src/client.rs` owns one dedicated PD runtime worker
and endpoint-keyed client map. `tidb-txnkv/src/pd_loader.rs` is the only adapter
from PD models to `RegionLocation`. `tidb-txnkv/src/region/cache.rs` is the sole
ordered region/store authority. `tidb-txnkv/src/rpc/unary.rs` owns the existing
TiKV runtime and channel pool; Campaign 15 must not add another runtime.

The exact pinned control-plane fields are `metapb.Buckets` fields 1-5,
`GetRegionResponse.buckets = 7`, `pdpb.Region.buckets = 5`, and ordered
`BatchScanRegionsRequest` ranges. Expiry is strict `now > ttl`; bucket updates
are monotonic; a stale/no-bucket region replacement retains newer cached
buckets; cached traversal stops at expiry, reload, a hole, end, or limit.

## Plan of Work

Root lands only module declarations and narrow contracts. Four feature agents
then work in per-slice branches and worktrees. The wire leaf changes only proto,
PD client/model, loader, and their existing source tests. The cache leaf owns
location/cache plus new TTL/bucket/batch modules and tests. The completion leaf
owns the async contract/driver and its tests. The scheduler leaf owns only the
new batch submodules and tests. Shared Cargo manifests, generated ledgers,
evidence transfers, release status, and final docs remain root-owned.

Agents use exact Go source/tests, `rustfmt --check`, source selectors, TSV checks,
and `git diff --check`. They do not run Cargo workspace builds. Root integrates
all substantial leaves before the first Rust build, then runs exact original Go
tests and one shared Rust/Ready gate.

## Concrete Steps

Run from repository root:

    python3 rust/scripts/work-unit-queue.py check
    python3 rust/scripts/work-unit-queue.py claim-slice --owner txnkv-pd-bucket-batch-wire --slice txnkv-pd-bucket-batch-wire
    python3 rust/scripts/work-unit-queue.py claim-slice --owner txnkv-region-cache-batch-bucket-core --slice txnkv-region-cache-batch-bucket-core
    python3 rust/scripts/work-unit-queue.py claim-slice --owner txnkv-async-completion-core --slice txnkv-async-completion-core
    python3 rust/scripts/work-unit-queue.py claim-slice --owner txnkv-batch-scheduler-core --slice txnkv-batch-scheduler-core

Create `codex/<slice>` worktrees under the sibling
`tidb-rust-worktrees/<slice>` directory. After leaf commits are reviewed and
integrated, run pinned Go package tests with exact parent-qualified regexps and
`-p 12`. Only then run focused Rust package tests, `make -j12 lint`, and:

    cd rust
    CARGO_BUILD_JOBS=12 scripts/rewrite-gate.sh integrate

## Validation and Acceptance

Validation completed for the exact frozen 11-source/53-test campaign. All
scoped original Go tests passed with 12-way package parallelism. Focused Rust
tests and Ready lint passed, followed by:

    cd rust
    CARGO_BUILD_JOBS=12 scripts/rewrite-gate.sh integrate

The command completed green and issued `integration_receipt 4`. Final checked
evidence is 11 production-source `PARTIAL` mappings and 53 original-test
`PARTIAL` mappings. All four claims consumed that receipt and were released as
`partial`; the frozen campaign archive now has zero active claims.

Wire tests cover pinned field numbers, per-request bucket preservation and
clearing, scan ordering, range limits, and response metadata. Cache tests cover
strict TTL, half-open containment, overlap replacement, 128-region batch size,
2048-range cap, bucket monotonicity/clamping, exact-Unimplemented fallback,
holes, and positive-infinity bounds. Completion tests cover reverse injection
and one terminal result across invoke/schedule races. Scheduler tests cover
source priority ordering, raw-layout full `Take`, backing-array reference
clearing, cancellation, terminal observations, grouping, concurrency limits,
and the scoped batch-policy table.

No slice is promoted from an original Go obligation merely because a Rust unit
test has a similar name. The evidence artifact must identify the exact source
rule and all nested subtests. A later live proof must use real PD/TiKV sockets;
this foundation campaign does not call injected buckets or a unary request a
live BatchCommands proof. The seven residual source semantics listed in
`Outcomes & Retrospective` remain explicit Campaign 16 work.

## Idempotence and Recovery

Claims preceded worktrees and feature branches were merged without force. The
frozen archive consumed `integration_receipt 4`, and all four claims were
released as `partial`. Any post-gate implementation, test, script, manifest, or
undeclared Rust edit to this campaign invalidates that receipt and requires a
fresh gate.

## Artifacts and Notes

- Design: `docs/design/2026-07-11-tidb-rust-rewrite.md`
- Current state: `rust/STATUS.md`, `rust/HANDOFF.md`
- Campaign: `rust/workstreams/campaigns/2026-07-read-path-15.toml`
- Slices: the four Campaign 15 manifests under `rust/workstreams/slices/`

## Interfaces and Dependencies

`BucketMetadata` is transport-neutral and immutable. PD owns decoding; cache
owns retention and publication. `AsyncRequestDispatcher::begin` starts exactly
one request and returns a `PendingRequest`; it neither looks up topology nor
mutates feedback. The completion driver owns ready state and once-only terminal
delivery without a second Tokio runtime. Batch scheduler owns ordering and
grouping, not sockets. RegionCache remains the only mutable topology authority.

Revision note (2026-07-19): created from the Campaign 14 postmortem and three
parallel pinned-source/test/write-set audits.

Closeout note (2026-07-19): recorded the green 12-job integration receipt,
exact final evidence counts, released claims, frozen archive, and seven
Campaign 16 residual semantics without claiming live PD/TiKV validation.
