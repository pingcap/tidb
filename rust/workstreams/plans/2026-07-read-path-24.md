# Detach clustered signed-BIGINT ranges and share the live SQL harness

This ExecPlan is a living document and follows `PLANS.md`.

## Purpose / Big Picture

Campaign 24 removes Campaign 23's unconditional full-table range without creating a key-only query path. A source-shaped signed-BIGINT ranger separates clustered-primary-key access conditions from residual conditions. DistSQL converts the logical ranges into exact half-open TiDB record-key ranges. The existing TiKV Selection remains the semantic owner for residual predicates. A shared live harness replaces duplicated Campaign 22/23 orchestration so later vertical slices add scenarios instead of another 1,500-line runner.

## Progress

- [x] (2026-07-19) Audited Go ranger, planner, request-builder, executor, and live-runner boundaries.
- [x] (2026-07-19) Froze six disjoint manifests with more than fifty exact original test obligations and ten production source anchors.
- [ ] Claim and implement the three independent roots: ranger, signed-handle encoding, and shared harness.
- [ ] Compose planner detachment, then the production executor.
- [ ] Run one small shared-harness scenario against real PD/TiKV, then one shared Ready gate and six receipted releases.

## Decision Log

- Range pruning optimizes the Campaign 23 Selection path; it does not replace it.
- Planner owns logical boundaries and access/residual partitioning. DistSQL alone owns physical TiDB key bytes.
- Contradictions normalize to zero ranges and short-circuit before TSO or transport.
- `!=` remains two ordered ranges; MIN/MAX boundaries eliminate empty halves rather than emitting invalid ranges.
- The live harness owns lifecycle and safety, while scenario scripts own SQL expectations and plan evidence.

## Implementation DAG

    A signed ranger --------+--> D planner detachment --+--> E real executor --> F live range proof
    B handle-key encoding --+                           |
    C shared live harness ------------------------------+

Stages A, B, and C run concurrently. D starts after A's interface freezes. E consumes D and B. F consumes E and C. Leaf work uses focused tests and shell self-tests only; no leaf runs a workspace build.

## Validation and Acceptance

The bounded matrix covers all six comparisons, reversed operands, conjunction intersection, signed extremes, contradictions, disjoint `!=`, and a clustered-PK access condition combined with a stored-column residual predicate. Key bytes must match Go's `encodeHandleKey` plus `tablecodec.EncodeRowKey`. The live proof retains one stock MySQL connection through A to B to C to restarted B, blocks a real query before shutdown, and proves tag-owned cleanup.

After all six leaves and evidence freeze, run exactly one batch gate:

    python3 rust/scripts/work-unit-queue.py check
    CARGO_BUILD_JOBS=12 rust/scripts/rewrite-gate.sh integrate
    make -j12 lint

`make bazel_prepare` is not required unless the final diff changes Go or Bazel/module inputs. `make bazel_lint_changed` is not run.
