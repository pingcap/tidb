# MySQL wire-protocol divergence inventory (Go `pkg/server` vs Rust `tidb-protocol`/`tidb-mysql`/`tidb-server`)

Status: in progress. Nothing in this document was executed; the audit machine
cannot run freshly built binaries. Every claim is source-derived, with the Go
file:line and the Rust file:line named.

## Findings

(populated below)

## Verified equal

(populated below)
