# Catalog model JSON parity: `pkg/meta/model` vs `tidb-model` / `tidb-metadef` / `tidb-meta::value`

Status: IN PROGRESS (initial commit; findings below are being filled in).

Nothing in this audit was executed. The machine cannot run freshly built
binaries (`syspolicyd` wedged), so every claim here is read from the Go
source and the Rust source side by side.
