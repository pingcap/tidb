# Complete Go-to-Rust statistics package parity

This ExecPlan is a living document. Keep `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` up to date as work proceeds.

Reference: `PLANS.md` at repository root. The pinned source of truth is Go commit `e2788410d8d696605e8cb002585877a063ccc909`. Repository policy requires one complete upstream Go package, including production sources, variants, tests, fixtures, and validation gates, as the minimum completion unit.

## Purpose / Big Picture

Rust must make the same statistics-loading and planning decisions as the pinned Go implementation. A query should collect the same predicate-column demand at the same logical-rule position, request the same column and index items, apply the same synchronous timeout policy, publish the same loaded state, and expose the same statistics through planning and `SHOW` statements. Rust-only thresholds, narrow cache paths, and behavioral approximations are removed rather than retained as alternatives.

## Progress

- [x] (2026-08-29) Persisted analyze snapshot metadata and matched outdated-statistics pseudo policy.
- [x] (2026-08-29) Added exact secondary-index reads and per-item histogram, bucket, TopN, and CMS loading.
- [x] (2026-08-29) Added logical predicate-column demand collection, including lineage, physical-table visitation, and operator counting.
- [x] (2026-08-29) Wired collection and sync/async requests at Go's logical-rule position, including partition expansion, statement timeout/cache state, interesting-column collection, first-phase access-path pruning, and retained-index filtering.
- [x] (2026-08-29) Split request dispatch from synchronous waiting and added Go's later `SyncWaitStatsLoadPoint` at its pinned logical-rule position.
- [x] (2026-08-29) Replaced eager startup and refresh reads with Go's lite load; preserved resident unchanged payload and fully reloaded only changed resident items.
- [x] (2026-08-29) Corrected wired `SHOW STATS_META` traversal to honor the session partition-prune mode and suppress pseudo cache entries like `GetNonPseudoPhysicalTableStats`.
- [x] (2026-08-29) Wired `SHOW STATS_HEALTHY` to production catalog statistics and made analyzed-row selection ignore metadata-only cache items like Go's `GetAnalyzeRowCount`.
- [x] (2026-08-29) Wired `SHOW STATS_TOPN` through production table/index metadata and the session-aware `ValueToString` equivalent, including hidden-column-capable index type lookup.
- [x] (2026-08-29) Wired `SHOW STATS_BUCKETS` through the same production traversal and decoder, preserving cumulative count, repeat, bounds, and bucket NDV.
- [x] (2026-08-29) Wired `SHOW STATS_HISTOGRAMS` through the shared statistics cache, including initialized-state filtering, average column size, load status, and live histogram/TopN/CMS memory usage.
- [x] (2026-08-29) Wired `SHOW HISTOGRAMS_IN_FLIGHT` to the shared needed-item set and Go's cache-state cleanup semantics.
- [x] (2026-08-29) Completed pinned `pkg/statistics/handle/lockstats` and `pkg/executor/lockstats` behavior across the shared policy, in-process catalog, TiKV internal transaction, session routing, privileges, warnings, errors, and `SHOW STATS_LOCKED`.
- [x] (2026-08-29) Completed pinned `pkg/statistics/asyncload` as the process-wide 128-shard needed-item set, including monotonic full-load upgrades and removal after successful, stale-metadata, or corrupted loads.
- [ ] Complete pinned `pkg/statistics/handle/syncload`. Its load/cache behavior is wired, including per-handle configured queues/workers, global per-item singleflight, timeout demotion, one retry, panic recovery, stale-DDL guards, split request/wait, and joined shutdown. The atomic claim remains blocked on the separate complete `pkg/metrics` owner for five shared collectors.
- [x] (2026-08-31) Re-audited and completed pinned `pkg/statistics/util`: dump/load shares the complete Go object shape, `Sort` has Go's unstable ordering contract, protobuf JSON retains every non-omitempty zero scalar, and `TotalMemoryUsage` uses the pinned generated-message `Size()` rules. The source-absent constructor and four-test carrier were removed.
- [ ] (2026-08-30) Audited pinned `pkg/statistics/handle/metrics`; its complete two-artifact leaf remains unclaimed pending the complete 60-artifact shared `pkg/metrics` collector owner. Existing private collectors are seed integration only, and two source-absent tests were removed.
- [x] (2026-08-30) Completed pinned `pkg/domain/metrics` as a distinct package-shaped module: all seven historical-generation and plan-replayer handles share the process collectors, rebind together, and are wired at their production consumer points.
- [x] (2026-08-30) Completed pinned `pkg/statistics/handle/autoanalyze/priorityqueue/intervaltimezone`: the cluster domain now owns and reuses one capacity-200 advanced statistics-session pool, refreshes its session variables on every checkout, and verifies the persisted analyze-job duration through the ordinary restricted SQL and coprocessor paths.
- [x] (2026-08-30) Re-audited and completed pinned `pkg/statistics/handle/autoanalyze/refresher` as one package: its queue, worker, time-window/rebuild policy, DDL integration, domain tick/shutdown, concrete job execution, all original tests, and build artifacts now map to the dedicated crate and production server path; the pre-port unclaimed audit was retired.
- [x] (2026-08-30) Completed pinned `pkg/statistics/handle/handletest/initstats`: targeted/repeated/all-current physical-ID loads, partition and dropped-table filtering, lite metadata state, non-lite index-only payload loading, skip-init startup, and last-physical-ID completion now have production wiring and executable Rust evidence. Removed the Rust-only eager column-payload startup behavior.
- [x] (2026-08-30) Completed pinned `pkg/statistics/handle/handletest/lockstats`: all 21 original table/partition tests now map to the wired shared policy, SQL and TiKV paths, delta persistence, logical-parent lock inheritance, and common hard-GC lock cleanup; the stale pre-wiring audit was replaced with a package receipt.
- [x] (2026-08-30) Completed pinned `pkg/statistics/handle/handletest/statstest`: all 17 cache/storage/startup/delta tests map to production Rust paths. Non-lite startup now follows Go's histogram/CMS, TopN, and bucket memory stages, and post-ANALYZE cache publication no longer advances the lifecycle version in quota mode.
- [x] (2026-08-30) Closed the root handletest incremental-ANALYZE metadata gap: ordinary Analyze v2 now samples at one snapshot, saves in a later transaction, reconciles `count` and `modify_count` against Go's `BaseCount`/`BaseModifyCnt`, honors `tidb_enable_analyze_snapshot`, and suppresses a stale result when a newer snapshot already won.
- [x] (2026-08-30) Closed pinned root handletest temporary-table cache behavior: LOCAL create/rows and both kinds' ANALYZE sampling use the session overlay; GLOBAL metadata remains shared; ordinary reads do not populate the cache; explicit ANALYZE publishes canonical entries; zero-row analyzed entries retain Go's query-time pseudo rule; and LOCAL cached statistics survive later shared-catalog rebuilds. The root external package remains open pending atomic reconciliation of all 30 original tests.
- [x] (2026-08-30) Reconciled the three pinned root handletest pruned-index async-load cases through ordinary Rust planning: non-partitioned and dynamic demand retain at most ten relevant indexes, static demand expands to every physical partition with Go's weaker no-unrelated-index guarantee, and all three publish through the process-wide async map rather than a test-only carrier.
- [x] (2026-08-30) Reconciled pinned root `TestLoadStatsForBitColumn` with the production cluster loader and the exact four Go cases, preserving decimal bound storage and quoted-input byte values when reconstructing BIT histograms.
- [x] (2026-08-30) Reconciled pinned root `TestIssue39336` end to end: permissive zero-in DATETIME(3) values survive partition Analyze v2, dynamic global-column merge, job persistence, and the finished `SHOW ANALYZE STATUS` surface.
- [x] (2026-08-29) Audited and completed pinned `pkg/statistics/handle/logutil`; the existing crate already matched all four logger routes, category fields, shared sampler state, and 5/10-minute sampling policies.
- [x] (2026-08-29) Completed pinned `pkg/statistics/handle/usage/collector` and `pkg/statistics/handle/usage/indexusage`, including close-aware synchronous delivery and the complete node/session/statement aggregation behavior.
- [x] (2026-08-29) Removed obsolete ignored predicate-collection gap carriers after wiring the real logical rule, retaining the pinned system-schema exclusion as an executable production-unit regression.
- [x] (2026-08-29) Completed pinned `pkg/statistics/handle/cache/metrics`, preserving all six operation labels and both gauge labels on the shared TiDB statistics metric families.
- [x] (2026-08-29) Completed pinned `pkg/statistics/handle/cache/internal/lfu` with buffered TinyLFU admission, cost eviction, the 256-shard secondary table set, metadata-only eviction, dynamic capacity, and wait barriers.
- [x] (2026-08-29) Audited and completed pinned `pkg/statistics/handle/cache/internal/mapcache`, including replacement cost accounting, shared table values, shallow table-copy semantics, and no-op capacity/lifecycle controls.
- [x] (2026-08-29) Audited and completed pinned `pkg/statistics/handle/cache/internal/testutil`, including every table constructor and append helper used by the parent cache and LFU test inventories.
- [x] (2026-08-29) Completed pinned `pkg/statistics/handle/initstats`, preserving concurrency selection, atomic progress, sampled logging, bounded task admission, and multi-worker range processing.
- [x] (2026-08-29) Audited pinned `pkg/statistics/handle/internal`; its sole test-support assertion helper already preserves the complete Go equality contract.
- [x] (2026-08-29) Audited pinned `pkg/statistics/handle/util/test`; its context matcher preserves the exact internal stats foreground request-source contract, panic boundary, and diagnostic text.
- [x] (2026-08-29) Completed pinned `pkg/statistics/handle/util`, including restricted-session variable synchronization, transaction/error/panic lifecycle, request source, worker/session pools, table metadata lookup, lease ownership, auto-analyze process tracking, and all original tests.
- [x] (2026-08-29) Ported the pinned parent cache's full-table core: global LFU/map selection, runtime quota backing, hit/miss/update/delete accounting, lifecycle max version, copy-on-write update, in-place update, capacity, eviction, wait, and close behavior.
- [x] (2026-08-29) Ported the parent cache's atomic wrapper, ordered metadata refresh, batched publication, cancellation, targeted-update version policy, health gauges, and histogram-version-aware reuse; made the cluster storage image construct the canonical full `statistics.Table` before deriving the planner view.
- [x] (2026-09-02) Re-audited the complete root `pkg/statistics/handle/cache` at Go master `c6054025ed4c32ab3672a2a24ea46892714d21ec` (five artifacts, 1,051 lines, two tests, six benchmark shapes, and no fixtures or generated/platform variants). Removed the Rust-only process-wide `StatsTableRowCache` because Go deleted `stats_table_row_cache.go`; statement-local `TableSizeStats` now reads `stats_meta` and conditionally `stats_histograms`, computes the Go size estimates, avoids shared-catalog mutation, and zeroes a failed statement read. Root cache refresh, batching, lifecycle, health, and backend behavior remain aligned. Receipt: `rust/testport/receipts/statistics_handle_cache_audit.md`.
- [x] (2026-08-30) Re-audited the row-cache consumer against pinned `infoschema_reader.go`: replaced the catalog-build planner-snapshot shortcut with the two fresh statement-time restricted storage reads, retained the prior cache on failure, and populated both `information_schema.tables` and per-physical-partition `information_schema.partitions` estimates.
- [x] (2026-08-29) Removed the live cache's reduced `ClusterTableStats` authority: bootstrap, refresh, sync load, planner derivation, and row-cache reads now share canonical full `statistics.Table` objects like Go.
- [x] (2026-08-29) Wired cluster `LOAD STATS` through Go's text-protocol client-local-file transfer, independent restricted TiKV transactions, optional history writes, final metadata publication, and the common shared-cache refresh path.
- [x] (2026-08-29) Wired `SHOW COLUMN_STATS_USAGE` to a fresh shared-storage snapshot, including session-location timestamps and logical/global plus all-partition traversal in both prune modes.
- [x] (2026-08-29) Completed pinned persisted ANALYZE options across table and partition targets, including Go-ordered static targets, final mandatory-column LIST persistence, live persistence gating, and partition FM-sketch storage.
- [x] (2026-08-29) Split dynamic global-stat preparation by Go worker mode: blocking retains its full partition-table load, while async applies kind/item skip classification and reads one histogram item and FM sketch across partitions at a time instead of retaining every payload for every partition.
- [x] (2026-08-29) Reproduced the async global-stat I/O/CPU handoff: FM and CMS use capacity-five channels, histogram+TopN uses an unbuffered channel, payload phases close in Go order, FM memory is released before CMS, worker exits unblock the peer, panics become errors, and simultaneous failures are joined.
- [x] (2026-08-29) Threaded the canonical per-statement SQL killer through routed cluster ANALYZE and matched both sequential and concurrent TopN polling, including Go's concurrent worker-error joining and client error identity.
- [x] (2026-08-29) Recorded non-enforced `source="analyze"` historical metadata after each successful global column/index save, gated by the live switch and pre-save initialized cache exactly like Go.
- [x] (2026-08-29) Removed the Rust-only refusal of stored generated columns from cluster ANALYZE; stored generated values are sampled from row bytes while virtual generated columns remain excluded like Go.
- [x] (2026-08-29) Ported global-stat item slow-save fencing: the live configured stats lease, exact five-lease threshold, independent metadata-version transaction, generic retry diagnostic, and refreshed history version now match Go.
- [x] (2026-08-29) Removed Rust's blanket global-index ANALYZE refusal; ordinary global indexes now participate in each partition's shared column-sampling task and the existing global merge, as pinned Go does.
- [x] (2026-08-30) Completed pinned `pkg/statistics/handle/globalstats` as one atomic package: all four production files, original tests and support helper, both benchmarks, build inventory, async/blocking workers, storage/planner integration, and SQL-binding consumers are mapped. Focused package tests, benchmark compilation, end-to-end cluster receipts, and the repository Ready `make lint` gate pass.
- [x] (2026-08-29) Restored the exported concurrent global-TopN merge boundary with caller-supplied batch sizing and both pinned globalstats benchmark shapes; integration-test inventory closure remains part of the open package claim above.
- [x] (2026-08-29) Restored the exported `MaxPartitionMergeBatchSize` package constant and made the concurrent benchmark consume that boundary rather than duplicating its value.
- [x] (2026-08-29) Restored the pinned session/global validation warning emitted on every assignment to `tidb_enable_async_merge_global_stats`, including exact code 1105 and message text.
- [x] (2026-08-29) Removed Rust-only historical-worker observation APIs and their tests, retained only Go's send/get surface plus package-internal shutdown, restored the full-mailbox warning, and removed stale documentation claiming partitioned ANALYZE is rejected.
- [x] (2026-08-29) Made the cluster statistics cache track logical and every physical partition ID, restored zero-lease eager payload loading, installed physical statistics in session catalogs, and verified the pinned globalstats SHOW counts for static and dynamic pruning end to end.
- [x] (2026-08-29) Restored the pinned `tidb_analyze_version` validation closure so Analyze v1 is rejected with Go's exact error before the v2-only global-statistics pipeline runs.
- [x] (2026-08-29) Added an end-to-end receipt for pinned `globalstats.TestGlobalStatsData`, matching every global and physical column/index bucket count, repeat, bound, and merged bucket NDV.
- [x] (2026-08-30) Added the pinned `globalstats.TestGlobalStatsNDV` sequence and separated Go's cached-table pseudo identity from the planner's empty-table pseudo copy, so analyzed empty global/partition histogram rows remain visible through SHOW while optimization still uses pseudo estimates.
- [x] (2026-08-30) Added the complete five-datum-family sequence from pinned `globalstats.TestGlobalStatsIndexNDV`; the existing FM-sketch/index-key merge already produces the exact global and partition NDVs.
- [x] (2026-08-30) Added pinned `globalstats.TestGlobalStatsVersion` and wired foreground `FLUSH STATS_DELTA` to the node-global usage collector and existing TiKV delta transaction, preserving pending modifications from unanalyzed partitions while refreshing global count/version state.
- [x] (2026-08-30) Added pinned `globalstats.TestDDLPartition4GlobalStats`; cluster `TRUNCATE PARTITION` now replaces only selected physical IDs and runs Go's statistics DDL-subscriber transaction for replacement rows, delayed-GC versions, and logical count/modify deltas.
- [x] (2026-08-30) Added pinned `globalstats.TestGlobalStats`; dynamic plans now retain Go's partition access object, static partition children reload physical statistics, ANALYZE force-flushes target deltas before sampling, and meta-only global statistics preserve their realtime estimate while carrying pseudo distribution/version state.
- [x] (2026-08-30) Added pinned `globalstats.TestGlobalStatsHealthy` and removed per-session bootstrap from shared-catalog construction; cluster sessions no longer publish row deltas from a discarded local `mysql` catalog, and global/partition count, modify-count, and health sequences now match Go exactly.
- [x] (2026-08-30) Added pinned `globalstats.TestBuildGlobalLevelStats`; static and dynamic multi-table ANALYZE, predicate-column demand, named-index ANALYZE, physical/global row counts, and complete histogram inventories match the source sequence.
- [x] (2026-08-30) Added both pinned `globalstats.TestIssues24349` sequences; global TopN candidate recovery from sibling partition histograms and the post-pop remaining buckets match Go exactly at merge concurrency one and two.
- [x] (2026-08-30) Added pinned `globalstats.TestMergeGlobalStatsForCMSketch`; equality estimation consumes the merged global sketch, dynamic pruning retains `p0`, and BRIEF explain now applies Go's statement-wide plan-ID suffix policy to nested table/index reader references. Replaced the remaining Rust-shaped `topn_test.go` fixtures and assertions with the pinned Go key tuples, bounds layout, and observable results.
- [x] (2026-08-30) Added both pinned `globalstats.TestGlobalStatsData2` concurrency variants and `TestGlobalStatsData3`; column/index and composite-index global TopN, buckets, NDV, and null counts now have exact integration coverage across integer, string, double, decimal, and datetime keys. Added the pinned `TestEmptyHists` no-op contract for both async and blocking merge modes.
- [x] (2026-08-30) Added pinned `globalstats.TestGlobalIndexStatistics` across table, named-index, and all-index ANALYZE. Dynamic partition pruning now retains ordinary global-index access paths during initial enumeration, static partition processing still removes them, and index-reader explain identity follows Go's range-resolved `PhysicalIndexScan.ExplainID`.
- [x] (2026-08-30) Added pinned `globalstats.TestGlobalStatsAndSQLBinding` and its concurrency-two twin. Hash, range, and list partition global statistics choose index ranges, session `IGNORE INDEX` bindings switch the same normalized statements to table scans, and dropping the bindings restores the index plans.
- [x] (2026-08-30) Added pinned `history.RecordHistoricalStatsToStorage`'s storage half: canonical JSON is gzip-framed into five-megabyte blocks and upserted by `(table_id, version, seq_no)` with one timestamp; a bootstrapped `mysql.stats_history` round trip verifies the stored bytes and version.
- [x] (2026-08-30) Added pinned `storage.GenJSONTableFromStats`'s missing canonical full-table dump half beside the existing load conversion: typed column bounds become Go BLOB/text bytes, index bounds remain key bytes, and histogram/CMS/TopN/FM metadata uses the shared JSON model.
- [x] (2026-08-30) Wired the historical-data production path: successful ANALYZE physical IDs enter Go's capacity-16 mailbox, the domain worker resolves current table/partition metadata, live dumps preserve Go's per-read restricted-transaction boundaries, and a final independent transaction stores canonical blocks with generate success/failure metrics.
- [x] (2026-08-30) Added the pinned executor receipts for the historical-stats switch and static/dynamic partition targets. Aggregate ANALYZE reports now retain every successful physical ID and separately queue the logical/global ID, matching Go's two post-save target sets instead of collapsing them to one table ID.
- [x] (2026-08-30) Completed pinned `pkg/statistics/handle/history` as one atomic package: `history_stats.go`, `BUILD.bazel`, every production symbol and dependency integration, and the absence of package-local tests, fixtures, generated inputs, and platform variants are inventoried. The original handletest write plus executor table/partition consumers have production receipts, and the repository Ready lint gate passes.
- [x] (2026-08-30) Ported the historical-read slice of pinned storage: metadata and payload versions are selected independently at the requested snapshot, ordered blocks reconstruct canonical JSON, historical counts overlay the payload, and table/partition/global fallback plus feature-gate and dump-metric ordering match Go. This remains seed evidence inside the open ten-file `pkg/statistics/handle/storage` package.
- [x] (2026-08-30) Ported pinned storage's explicit outdated-history cleanup: the global Go-duration retention value determines the cutoff, metadata uses the opening count and independent 1,000-row autocommit batches, and payload cleanup preserves the pinned immediate-return behavior after its first 50-row batch. This remains seed evidence inside the open storage package.
- [x] (2026-08-30) Ported the storage executor plans for pinned `gcHistoryStatsFromKV`, `deleteHistStatsFromKV`, and `DeleteTableStatsFromKV`: dropped-table history uses two independent plans, while item and table deletion preserve Go's wrapped-transaction boundaries, version publication, soft histogram reset, hard deletion, and dependent-row cleanup. These are executor receipts inside the open storage package; `GCStats` scheduling, timestamp progression, schema reconciliation, and two-phase orchestration remain open.
- [x] (2026-08-30) Wired pinned storage `GCStats` through the production restricted-transaction path: wall-clock/lease windowing, persisted half-open `tidb_stats_gc_last_ts`, per-item cleanup, dropped-table two phases, partition-parent schema reconciliation, independent history deletion, warning-only expiry cleanup, and final timestamp publication match Go. The original storage test/support inventory and the parent domain's owner-only `100 * statsLease` scheduler remain open, so the atomic storage package is not claimed complete.
- [x] (2026-08-30) Reconciled pinned `gc_test.go`'s `TestGCStats`, `TestGCPartition`, `TestGCColumnStatsUsage`, and `TestExtremCaseOfGC` through the production embedded-store path, including the retained logical meta-only partition row and the existing-table/empty-histogram guard. `TestDeleteAnalyzeJobs` belongs to the still-open analyze-job storage behavior, and the enclosing storage package remains incomplete.
- [x] (2026-08-30) Ported both pinned corrupted analyze-job cleanup transactions: current-instance selection uses only live `ANALYZE TABLE` process IDs and skips NULL process IDs; dead-instance selection uses the server-info membership set. Both preserve the strict ten-minute cutoff and update only state, failure reason, and process ID.
- [x] (2026-08-30) Wired pinned `SHOW ANALYZE STATUS` through the ordinary SHOW dispatcher and fresh persisted-job snapshot: newest-thirty ordering before privilege filtering, fourteen-column metadata, session-zone timestamps, terminal/global-merge NULL progress, running progress, and the PD-region/exact-count fallback now share Go's process-wide 30-second approximate-count cache.
- [x] (2026-08-30) Reconciled the remaining pinned SHOW statistics executor behavior: `SHOW STATS_EXTENDED` returns Go's exact removed-feature error, production `SHOW STATS_META` uses one shared Go-shaped row builder, and the persisted analyze-status receipt covers physical partition analysis plus global column/index merge jobs.
- [x] (2026-08-30) Completed pinned `pkg/executor/internal/pdhelper` as one atomic package: `pd.go`, `pd_test.go`, `main_test.go`, and `BUILD.bazel` are inventoried; one node-global capacity-1,048,576/30-second TTL cache with a joined cleanup lifecycle now serves both `SHOW ANALYZE STATUS` and ANALYZE auto-sampling. PD region counts, the two-region exact-count fallback, failure caching, cache-hit `hasPD`, source key identity, active resource-group propagation, and Go's `0.001` missing-statistics fallback are covered by focused receipts.
- [x] (2026-08-30) Completed pinned `pkg/statistics/handle/usage` and `pkg/statistics/handle/usage/predicatecolumn` as atomic packages. The inventory covers all four production files, both `BUILD.bazel` files, `export_test.go`, all three original test files, the deliberately skipped concurrent stress case, and the absence of fixtures, generated inputs, or platform variants. Shared session delta/column collectors, index usage, predicate persistence and cleanup, ANALYZE consumers, partition/lock expansion, historical metadata, worker intervals, and shutdown lifecycle now follow the pinned implementation; shutdown stops and joins every usage worker before the final forced delta flush.
- [x] (2026-08-30) Completed pinned `pkg/statistics/handle/autoanalyze/exec` as one atomic package, including its production file, original test file, and build inventory.
- [x] (2026-08-30) Completed pinned `pkg/statistics/handle/autoanalyze/priorityqueue` as one atomic package: all ten production files, complete test/support/build inventory, keyed heap, three job forms, factory and interval policy, source-owned lifecycle, DML/retry refresh, DDL mutations, validation, and ordinary ANALYZE execution are mapped and integrated.
- [x] (2026-08-30) Completed the test-only pinned `pkg/statistics/handle/autoanalyze/priorityqueue/calculatoranalysis` package: its full 690-case matrix runs the production parent calculator, preserves stable descending CSV output, and byte-compares the exact upstream golden fixture.
- [x] (2026-08-30) Completed pinned `pkg/statistics/handle/autoanalyze/refresher` as one atomic package: both production files, all three original test files, build inventory, queue rebuild/window ordering, live concurrency, running-job ownership, panic recovery, shutdown waits, and the domain auto-analyze worker are mapped and integrated.
- [x] (2026-08-30) Completed pinned root `pkg/statistics/handle/autoanalyze` as one atomic composition package: complete production/test/build inventory, all 14 original test dispositions, ownership and worker lifecycle, locks/windows, automatic job identity, skipped-type/vector policy, cleanup, and ordinary ANALYZE/cache effects are mapped across their production owners.
- [x] (2026-08-30) Ported Go Domain's analyze-job cleanup worker and statistics-owner boundary: only a positive stats lease starts the worker, cleanup ticks at exactly 100 leases, every node cleans its own stale processes, only `/tidb/stats/owner` cleans dead instances and seven-day history, and shutdown wakes and joins the worker.
- [x] (2026-08-30) Corrected the wired `storage.UpdateStatsMeta` planner to apply every supplied update like Go, including a row-count delta whose modify-count is zero; the original Rust-only filter silently retained the old count and version. This remains seed evidence inside the open storage package.
- [x] (2026-08-30) Reconciled pinned storage/sync-load lazy item installation: known-but-unanalyzed columns now install Go's empty column without a storage read, analyzed columns and indexes absent from the resident maps are constructed from current schema metadata, and the separate Rust published snapshot receives the exact replacement table instead of briefly republishing an asynchronously superseded LFU value. The schema-less partial update API was removed.
- [x] (2026-08-30) Replaced `DumpStatsDeltaToKV`'s optimistic write with the pinned `FlagWrapTxn` pessimistic transaction boundary. Statement retries now rebuild locked-table reads and delta mutations at the advanced `for_update_ts` while retaining the transaction start timestamp as the statistics version. The common runner is intentionally limited to one Go SQL statement; multi-statement storage wrappers remain open until their original statement boundaries are represented.
- [x] (2026-08-30) Moved the pessimistic rebuild loop onto the ordinary transaction engine and added its read-your-own-writes overlay. Slow-save metadata refresh and LOAD STATS final metadata now use the exact one-statement wrapper; LOAD STATS predicate usage preserves Go's one `REPLACE` statement per nonnil entry inside one pessimistic transaction. Item payload and historical metadata still require their explicit Go statement splits.
- [x] (2026-08-30) Replaced LOAD STATS' flattened item mutation plan with pinned `SaveColOrIdxStatsToStorage`'s statement sequence in one pessimistic transaction: meta, TopN delete and inserts, FM delete, histogram replace, and bucket delete and inserts. TopN and bucket inserts use Go's ten-row cap and exact escaped-SQL 1 MiB split rule through the shared sqlescape port; the legacy all-in-one planner and its test callers were removed. Historical metadata and payload remain open.
- [x] (2026-08-30) Split historical metadata persistence into Go's `SELECT ... FOR UPDATE` and `REPLACE` statements within one pessimistic transaction for ANALYZE, LOAD STATS, and stats-delta flush. The ordinary rebuild path now retains explicit lock-only keys, so the selected `stats_meta` row is actually locked and retried rather than represented by a synthetic write. Historical payload block statements remain open.
- [x] (2026-08-30) Replaced historical payload's flattened optimistic plan with pinned `RecordHistoricalStatsToStorage`: gzip and version selection happen once, every block is one `INSERT ... ON DUPLICATE KEY UPDATE` statement with the same microsecond timestamp, all statements run through one wrapped pessimistic transaction, and the legacy all-block planner was removed.
- [x] (2026-08-30) Replaced delta persistence's flattened mutation plan with pinned `UpdateStatsMeta`'s two locking selects followed by its locked, unlocked-positive, and unlocked-negative multi-row upserts in one pessimistic transaction. Later statements read earlier staged writes, each lock conflict rebuilds only its statement, duplicate negative tuples retain SQL tuple order, and the obsolete one-statement server wrapper and its Rust-only test were removed.
- [x] (2026-08-30) Replaced the flattened optimistic truncate-partition statistics write with pinned `ActionTruncateTablePartition` subscriber behavior: each replacement partition receives `InsertTableStats2KV`'s meta/column/index statement sequence, the logical delta retains `UpdateStatsMeta`'s statement boundaries, retired partitions receive independent version refreshes, schema-change history uses the live switch and initialized-cache gate, and the notifier work commits once through one pessimistic transaction.
- [x] (2026-08-30) Extended the same production subscriber transaction to pinned `ActionAddTablePartition` and `ActionDropTablePartition`: added physical IDs are processed in definition order through `InsertTableStats2KV`; dropped IDs are counted in their old definition order, subtracted from the logical row through `UpdateStatsMeta`, then version-refreshed for delayed GC and optional schema-change history. The immediate post-drop global-count regression fails with the old unsubscriber path (`9` instead of `7`) and passes after the event wiring.
- [x] (2026-08-30) Wired pinned `ActionCreateTable`, `ActionTruncateTable`, and `ActionDropTable` through the same production subscriber transaction, including `CREATE TABLE LIKE`, definition-order partition IDs, the GLOBAL dynamic-prune decision, new-ID placeholder statements before old-ID delayed deletion, schema-change history gates, and cache replacement. The lifecycle regression fails without subscriber dispatch at the absent create metadata row and passes with the complete event path.
- [x] (2026-08-30) Wired every Go-admitted `ActionAddColumn` statistics shape through pinned `InsertColStats2KV`: nullable/defaultless columns use the existing physical row count as `null_count`; declared non-NULL defaults and implicit NOT NULL zero defaults create Go's one-value histogram and bucket with `Datum.GetBytes` size accounting and blob-converted bounds; virtual generated columns persist Go's empty, uninitialized histogram with no bucket. Each GLOBAL-mode physical ID refreshes metadata only when the histogram insert affected a row, attempts schema-change history, and commits the event once pessimistically. Stored generated ADD retains Go's 3106 refusal because ALTER cannot backfill it.
- [x] (2026-08-30) Generalized that storage path to pinned `InsertColStats2KV`'s virtual, NULL-origin-default, and non-NULL-origin-default shapes, including Go `Datum.GetBytes` size accounting, blob-converted default buckets, `INSERT IGNORE`, and conditional metadata refresh. Wired every Rust DDL spelling that publishes pinned `ActionModifyColumn`—MODIFY, CHANGE, RENAME COLUMN, and AUTO_RANDOM-bit changes—through it. The metadata-only MODIFY/RENAME regression fails with subscriber dispatch removed and passes with Go's origin-default distinction: a CREATE TABLE default is not an origin default, so a missing histogram is recreated as `(distinct_count, null_count) = (0, row_count)` without a bucket.
- [x] (2026-08-30) Wired pinned `ActionDropSchema`: Rust snapshots each table's partition IDs in definition order followed by its table ID before catalog deletion, advances every extant stats row in one notifier-like pessimistic transaction, attempts history per ID, logs and continues after per-ID failures, and removes the retired cache entries after commit. The ordinary-plus-partitioned schema regression fails with unchanged versions when dispatch is removed and passes for all four physical/logical IDs when restored.
- [x] (2026-08-30) Applied pinned `asyncNotifyEvent`'s `metadef.IsMemOrSysDB(job.SchemaName)` guard once before every currently wired table, column, and partition statistics projection. The source-shaped system-table regression covers create, add/modify column, truncate, drop, and add/truncate/drop partition; disabling the guard publishes a `mysql.stats_meta` row on CREATE, while the restored path leaves every original and replacement physical ID absent.
- [x] (2026-08-30) Reconciled root handletest `TestStatsCacheShouldNotCacheSystemTable` through the ordinary Unistore SHOW path. The canonical loaded-statistics cache contains the one analyzed user table before the scan and remains byte-for-byte unchanged after `SHOW STATS_META` and `SHOW STATS_HEALTHY`; statement-facing pseudo load-attempt states are not canonical cache objects and were retained.
- [x] (2026-08-30) Reconciled root handletest's collation-load and cache-refresh guards against their production owners. String histogram bounds remain raw collation-key bytes; an equal metadata-version pass retains the exact published snapshot without a reload; and a failed/timeout read preserves the prior cache while recording the failure.
- [x] (2026-08-30) Reconciled root handletest's uninitialized and missing-partition statistics contracts through exact ordinary-session regressions. Delta-only DDL placeholders stay hidden and pseudo under both outdated-stat settings; dynamic analysis of two out of three partitions publishes the six-row/two-modification global meta and initializes all three columns plus the index.
- [x] (2026-08-30) Reconciled root handletest `TestVersion` at the live cluster boundary. Periodic, ANALYZE, and LOAD STATS refreshes now use the canonical parent-cache ordered metadata update instead of Rust's whole-snapshot version probe/rebuild; older per-table versions cannot move cached objects backward, targeted refreshes preserve the global watermark, ANALYZE supplies Go's task-derived logical/partition/index ID list rather than its historical-dump IDs, and the legacy helpers/tests were removed.
- [x] (2026-08-31) Implemented pinned `UpdateStatsVersion` and `ChangeGlobalStatsID` through their owning flashback/add-partitioning/remove-partitioning subscriber events. Their two and six ordered SQL statements now remain distinct retryable statement boundaries inside one real pessimistic notifier transaction; the production notifier uses the ordinary transaction controller, and statistics handler failures are logged and marked processed like Go.
- [ ] Wire all pinned Go `SHOW STATS_*` surfaces to the shared cache and storage semantics.
- [ ] Inventory every production file, platform/generated variant, original test/support artifact, fixture, and validation gate in pinned `pkg/statistics`; close or explicitly retain seed-only gaps until the whole package is complete.
- [ ] Run the Ready validation profile, including `make lint`, only after the complete package inventory is closed.

## Surprises & Discoveries

- Observation: the notifier's internal session adapter sent `BEGIN
  PESSIMISTIC`, `COMMIT`, and `ROLLBACK` through the ordinary write planner,
  which cannot establish the session's explicit transaction. The statistics
  subscriber therefore reached its first staged statement with no active
  transaction even though direct test helpers appeared to work.
  Evidence: the production-notifier regression failed with `DDL notifier
  handler has no active transaction`; routing transaction control through the
  shared session transaction controller makes the same durable event commit
  and clean up successfully.

- Observation: pinned statistics `HandleDDLEvent` deliberately logs a
  subscriber error and returns nil. Propagating that error from Rust's handler
  kept the notifier row pending and retried partial subscriber work, which is
  observably different from Go.
  Evidence: pinned `pkg/statistics/handle/ddl/ddl.go` and the Rust handler
  regression that converts a subscriber error to successful event processing.

- Observation: `UpdateStatsVersion` and `ChangeGlobalStatsID` are one
  transaction but not one statement. Flattening the former's two updates or
  the latter's six updates into one staged plan changes statement retry and
  partial-progress behavior before Go's error-suppressing handler returns.
  Evidence: pinned `pkg/statistics/handle/ddl/subscriber.go` issues the updates
  individually in fixed order; Rust now stages exactly one statement at a
  time in those same orders.

- Observation: the live Rust refresh seam still bypassed the completed parent-cache `Update` implementation. It performed a second all-table metadata scan/whole-snapshot comparison and rebuilt the statement-facing image, so an older metadata row and targeted ANALYZE/LOAD publication were governed by Rust-specific policy rather than the cache's Go rules.
  Evidence: pinned `StatsCacheImpl.Update` queries ordered rows above the five-lease watermark and applies its old-table version/schema guard plus targeted `SkipMoveForward`; the removed Rust helpers instead compared `BTreeMap<table_id, version>` against an entire `StatsSnapshot` and called a separate snapshot loader.

- Observation: Go's cache update reads unsigned `mysql.stats_meta.count` with `chunk.Row.GetInt64`, whose implementation reinterprets the eight-byte slot, while the first Rust adapter draft saturated the value at `i64::MAX`.
  Evidence: pinned `chunk.Column.GetInt64` dereferences the row bytes as `int64`; the Rust boundary regression covers `i64::MAX`, `i64::MAX + 1`, and `u64::MAX` and now produces the same signed values.

- Observation: `TableInfoByID` resolves a partition ID to its unchanged parent table and passes the physical ID separately to `TableStatsFromStorage`; Rust's prior target expansion cloned the parent and overwrote `TableInfo.id` with the partition ID.
  Evidence: pinned `handle/util.TableInfoByID` returns `FindTableByPartitionID`'s parent and pinned cache `Update` passes both `tableInfo` and `physicalID`; Rust now carries separate `StatsTarget.table` and `StatsTarget.physical_id` values, with a regression proving parent ID/update-TS retention.

- Observation: Unistore's default-row response encoded a scan-decoded
  `TIMESTAMP` without the request timezone. The scan had already converted UTC
  to the session location, so the TiDB-side schema-aware decoder applied the
  offset a second time whenever a selection forced coprocessor execution.
  Evidence: the interval-timezone regression returned `-7198` seconds for the
  pushed query while the same row read locally returned `2`; using Go's
  `codec.EncodeValue(sc.TimeZone(), ...)` shape makes both paths return the
  same small positive duration.

- Observation: pinned `InsertTableStats2KV` is not a metadata-only insert. It emits one `INSERT IGNORE` for `stats_meta`, then one independent `INSERT IGNORE` per column and index into `stats_histograms`; later subscriber statements can observe every earlier insert because the notifier wraps the complete handler in one pessimistic transaction.
  Evidence: pinned `pkg/statistics/handle/storage/save.go::InsertTableStats2KV`, `pkg/statistics/handle/ddl/subscriber.go::insertStats4PhysicalID`, and `pkg/ddl/notifier/subscribe.go::processEventForHandler`; the Rust statement-order/placeholder regression and end-to-end truncate receipt pass through the shared staged overlay.

- Observation: `FlagWrapTxn` is selective, not a license to make every statistics write pessimistic. In pinned Go, `CallWithSCtx` opens `BEGIN PESSIMISTIC`, but only callers passing `FlagWrapTxn` receive that boundary; for example, column-usage batches intentionally remain independent unwrapped statements.
  Evidence: pinned `pkg/statistics/handle/util/util.go::CallWithSCtx` and `WrapTxn`, plus `pkg/statistics/handle/usage/session_stats_collect.go::DumpStatsDeltaToKV` and `dumpColStatsUsageToKV`.

- Observation: a wrapped Go transaction may still contain multiple retryable SQL statements. Collapsing its complete mutation set into one Rust pessimistic lock operation changes lock timing and statement-retry behavior, so the ordinary runner must retain each `ExecWithCtx` boundary.
  Evidence: pinned `UpdateStatsMeta` issues up to two locking selects and three multi-row upserts, historical metadata performs `SELECT ... FOR UPDATE` followed by `REPLACE`, and historical payload storage issues one `INSERT` per block.

- Observation: duplicate table IDs in pinned `UpdateStatsMeta` cannot always be pre-aggregated. For a missing unlocked row and negative deltas, the first tuple inserts its absolute delta and later tuples enter the duplicate-key subtraction branch, which differs from inserting the sum of all absolute deltas.
  Evidence: partition expansion may append the same logical parent for multiple physical partitions; the Rust regression for `-5` followed by `-3` requires the resulting missing-row count to be `2`, not `8`.

- Observation: pinned Go's column existence map has three observable states, not a single load-needed bit: absent storage metadata, an unanalyzed histogram row, and analyzed statistics. For the middle state the sync/async worker installs `statistics.EmptyColumn`, which suppresses repeated load requests without claiming analyzed data. Rust retained the existence bits but its cache updater rejected any item without an already-resident column or index, so both this fake-column path and loading an evicted/nonresident analyzed item were ineffective.
  Evidence: pinned `Table.ColumnIsLoadNeeded`, `Table.IndexIsLoadNeeded`, and `statsSyncLoad.handleOneItemTask` branch on the existence map; the Rust regressions now cover empty-column insertion plus full analyzed column and index insertion from current table metadata.

- Observation: Rust's additional statement-facing published statistics map cannot safely re-read an LFU replacement immediately after submission. The LFU admission queue may still return the previous primary-cache value even though its secondary table set already contains the replacement; Go has no separate published map to populate from that stale read.
  Evidence: the new empty-column regression initially reported a successful update but observed no resident column. Publishing the same immutable replacement submitted to the cache preserves Go's immediate update semantics while the LFU completes admission.

- Observation: pinned `UpdateStatsMeta` does not discard a `DeltaUpdate` whose `TableDelta.Count` is zero; `dumpStatsDeltaToKV` skips lock/partition expansion for it but still passes the original update to storage, where its row-count delta and version are applied. Rust repeated the expansion guard inside the storage planner and therefore lost valid row-only updates.
  Evidence: the new executor receipt failed before the filter was removed with metadata `(version=100,count=10)` instead of Go's `(version=101,count=13)`, and passes with the pinned public-storage behavior.


- Observation: the pinned `CollectPredicateColumnsPoint` is not only a collector. It prunes each `DataSource`'s access paths and uses the retained index IDs to avoid loading statistics for indexes the planner just removed.
  Evidence: `pkg/planner/core/rule/rule_collect_plan_stats.go` calls `pruneIndexesForAllDataSources` before `collectSyncIndices`.

- Observation: synchronous load timeout is statement state, not merely a warning. Go marks sync loading failed, prevents plan-cache admission, appends the original load error when pseudo fallback is enabled, and caps the wait by `max_execution_time`.
  Evidence: pinned `RequestLoadStats` in `pkg/planner/core/rule/rule_collect_plan_stats.go`.

- Observation: Go expands requested table items to physical partition IDs only after column and index demand is combined.
  Evidence: pinned `expandStatsNeededColumnsForStaticPruning` appends one item per `tid2pids` entry.

- Observation: Rust's `PossiblePath` is the exact stage used by this pinned pruning rule: like Go before `fillIndexPath`, it has index metadata but no `FullIdxCols` or `IsSingleScan`. Consequently the pinned fallback scoring branch applies and consecutive-prefix/single-scan bonuses are unreachable at this point.
  Evidence: pinned `scoreIndexPath` branches on nil `FullIdxCols`; Rust `access_path::PossiblePath` is documented and populated as the newborn access path.

- Observation: pinned Go dispatches statistics work at `CollectPredicateColumnsPoint` but waits only at the later `SyncWaitStatsLoadPoint`; Rust had combined those phases and therefore blocked intervening logical rules.
  Evidence: pinned `SendLoadRequests` stores statement wait state and returns, while `SyncWaitStatsLoad` consumes that state in the later rule.

- Observation: Go's periodic cache update is not equivalent to replacing the cache with a fresh lite image. A row-count-only `stats_meta.version` change preserves resident histogram payload, while a newer histogram is reloaded in full only when the old item was resident.
  Evidence: pinned `StatsCacheImpl.Update` reuses `MetaOnly` when `LastStatsHistVersion` has not moved, and `TableStatsFromStorage(..., loadAll=false)` branches on each old item's load state and histogram version.

- Observation: the pre-existing Rust `SHOW STATS_META` path hard-coded dynamic pruning and exposed pseudo cache entries, although pinned Go branches on `IsDynamicPartitionPruneEnabled` and calls `GetNonPseudoPhysicalTableStats`.
  Evidence: the regression first returned the empty analyzed table plus the partitioned table's global row under static mode; pinned behavior returns only the two physical partitions.

- Observation: Rust had the `SHOW STATS_HEALTHY` parser and an isolated helper, but no session dispatch, and its reduced `analyze_row_count` selected the first item without checking full-load status.
  Evidence: the end-to-end regression initially failed as unsupported; pinned `GetAnalyzeRowCount` sorts items and returns only the first fully loaded column or index.

- Observation: Rust's TopN row helper retained Go's untyped byte type-code seam even though the production decoder accepts `FieldTypeCode`; no execution path called the helper.
  Evidence: wiring `SHOW STATS_TOPN` required replacing that isolated byte slice with the decoder's native type slice and resolving index offsets against all table columns, including hidden expression-index columns.

- Observation: a one-value Analyze v2 result has no bucket under the default TopN size because the value lives entirely in TopN; pinned bucket tests explicitly use `WITH 0 TOPN` when they require histogram rows.
  Evidence: the initial end-to-end bucket assertion returned no rows; with the pinned setup it produced `count=1`, `repeat=1`, equal bounds, and `ndv=0`.

- Observation: pinned Go still declares `tidb_analyze_version` with the integer range `[1, 2]`, but a second validation closure rejects the otherwise type-valid value `1`. Rust had captured only the declarative range, which made its hard-coded v2 global merge reachable from a session claiming v1.
  Evidence: pinned `sysvar.go` returns `tidb_analyze_version=1 is no longer supported, please set tidb_analyze_version to 2`; the Rust SQL receipt failed before the closure was added and now returns the same 1105 message without changing the session value.

- Observation: `SHOW ANALYZE STATUS` limits persisted jobs before checking table privileges, and its running-row estimate does not stop at the loaded statistics count. A zero count or processed count above that estimate asks PD for record-range region statistics, uses `storage_keys` only above two regions, and otherwise performs an exact `COUNT(*)`; errors become a cached zero because the caller ignores `hasPD`.
  Evidence: pinned `dataForAnalyzeStatusHelper`, `getRemainDurationForAnalyzeStatusHelper`, and `pkg/executor/internal/pdhelper/pd.go`; the Rust end-to-end receipt forces `processed_rows > RealtimeCount` on a backend without PD and observes the cached zero fallback.

- Observation: Rust already aggregated column/index memory like Go, but histogram bytes were a caller-injected field that every production construction boundary discarded.
  Evidence: the full `Column`/`Index` types had `MemoryUsage`, while cluster-loaded and in-process planner statistics retained only the histogram, TopN, and CMS payloads. Moving measurement onto `Histogram` makes all ordinary construction paths report the resident payload without a SHOW-specific cache.

- Observation: `SHOW HISTOGRAMS_IN_FLIGHT` is not a thread counter. Go sweeps its global needed-item set against current cache load state and counts only entries that still require loading.
  Evidence: pinned `CleanFakeItemsForShowHistInFlights` deletes missing tables and already-satisfied column/index items before returning the count.

- Observation: statistics locking is an internal statistics-handle transaction, not part of the caller's user transaction. Its executor first checks INSERT and SELECT on every target, and warnings are statement-local.
  Evidence: pinned `LockExec`/`UnlockExec` call the domain statistics handle, while `statsLockImpl` uses `util.CallWithSCtx(..., util.FlagWrapTxn)`; pinned plan building records INSERT then SELECT visit-info for each target.

- Observation: pinned table-level unlock deliberately skips a target whose logical table row is unlocked, even if a partition row remains locked; partition unlock also refuses every partition while the logical table is locked.
  Evidence: pinned `RemoveLockedTables` continues before visiting partitions when the logical ID is absent, and `RemoveLockedPartitions` returns the whole-table warning before its partition loop.

- Observation: pinned asynchronous-load demand is one process-wide, 128-shard map, not a session or catalog-local queue. Re-inserting an item may upgrade metadata-only demand to full load but never downgrade it.
  Evidence: the complete pinned `pkg/statistics/asyncload/async_load.go` owns the exported singleton; its shard key is the absolute column/index ID modulo 128 and each shard has its own RW mutex.

- Observation: pinned synchronous loading has two independent time boundaries: each singleflight operation times queue admission plus worker response, while the later planner wait starts one common statement timer over all returned result channels. A loader error is an item result and is logged, whereas only the outer common wait error triggers pseudo fallback.
  Evidence: pinned `SendLoadRequests` creates its timer inside `singleflight.DoChan`; pinned `SyncWaitStatsLoad` creates a separate timer and distinguishes `singleflight.Result.Err` from `stmtctx.StatsLoadResult.Error`.

- Observation: zero-wait statistics demand is not processed by the synchronous worker pool. It remains in the process-global asynchronous map until the domain refresh tick invokes `storage.LoadNeededHistograms`.
  Evidence: pinned `CollectPredicateColumnsPoint` inserts directly into `asyncload.AsyncLoadHistogramNeededItems` when `syncWait == 0`; the domain `loadStatsWorker` drains it separately.

- Observation: pinned `pkg/statistics/util` owns the full statistics JSON object model, while Rust had split it into a predicate-only `tidb-stats` type and a separate executor-only load type that omitted FM sketch, usage timestamps, and historical state.
  Evidence: pinned `json_objects.go` defines `JSONTable`, `JSONColumn`, and `JSONPredicateColumn` together; the executor consumes that package rather than declaring a second representation.

- Observation: pinned handle metrics do not define a second health calculation. They only fix bucket identities/configuration and bind child handles to the shared `tidb_statistics_stats_healthy` and `tidb_statistics_historical_stats` metric families.
  Evidence: pinned `pkg/statistics/handle/metrics/metrics.go` contains only constants, `HealthyBucketConfigs`, three metric variables, and `InitMetricsVars`; bucket population remains in `pkg/statistics/handle/cache`.

- Observation: pinned `pkg/statistics/handle/types` is an interface-only closure package whose composite `StatsHandle` embeds concrete cache, usage, history, analyze, storage, global-stats, lock, and DDL owners. A standalone Rust umbrella crate before those owners are complete would duplicate the existing narrow integration seams without adding Go behavior.
  Evidence: the complete pinned `interfaces.go` contains declarations only; `StatsHandle` embeds the owner interfaces at the end of the file.

- Observation: Go's high-priority collector send observes `closeCh` and returns `false` after close. Rust previously enqueued after close, so index-usage `Flush` could report success to a worker that no longer existed or block on a full abandoned queue.
  Evidence: pinned `sessionCollector.SendDeltaSync` selects between `highPriorityDataCh` and `closeCh`; the regression `synchronous_send_stops_after_close` failed before the close-state check and passes after it.

- Observation: two ignored planner integration files still described `CollectColumnStatsUsage` and its logical-rule carrier as unported after both had been wired. Keeping those empty tests made the test inventory contradict production behavior.
  Evidence: both files contained only ignored empty functions; `logical::rule_collect_plan_stats` now owns the collector, rule positions, partition expansion, and request/wait tests.

- Observation: the Rust `StatsCacheInner` and map backend were not shareable across threads, although Go publishes one cache to every session. The LFU backend was thread-safe, but the common trait could not form the parent cache's process-wide ownership boundary.
  Evidence: `StatsCacheInner` lacked `Send + Sync` and `MapCache` stored its map in `RefCell`; a compile-time shared-cache boundary rejected both before the parent package could be wired.

- Observation: Rust registered `tidb_stats_cache_mem_quota` but did not publish its validated global value to a process-wide backing store, so Go's cache constructor could not observe runtime quota changes.

- Observation: the cluster loader constructed the reduced planner statistics directly from storage rows, bypassing Go's full cached `statistics.Table` ownership boundary. That lost the existence map, load-state payload, eviction cost, schema update timestamp, and histogram-refresh version as one coherent object.
  Evidence: `ClusterTableStats` fed `TableStatistics` directly; it now first produces a canonical `tidb_stats::Table`, and the planner view is derived from that table.

- Observation: Rust's information-schema transport must emulate Go's statement-local `TableSizeStats`: `TABLE_ROWS` needs only `mysql.stats_meta`, while `AVG_ROW_LENGTH`, `DATA_LENGTH`, and `INDEX_LENGTH` additionally read non-index `mysql.stats_histograms`. The values are overlaid on a scratch catalog for one statement and never published as a process-wide row cache.
  Evidence: pinned `infoschema_reader.go` calls `UpdateByID` for table and partition IDs, including the logical table for global-index size, before calling `EstimateDataLength`.
  Evidence: the session global publisher updated the two ANALYZE atomics only; the new `vardef::STATS_CACHE_MEM_QUOTA` is populated from the same resolved global image before `StatsCache::new` reads it.

- Observation: the live Rust cache still published decoded `ClusterTableStats` storage rows after the parent cache crate had been taught to own full tables. That made sync loading replace storage DTOs and forced every planner/catalog consumer to reconstruct another full table.
  Evidence: pinned `StatsCacheImpl.Update` and `statsSyncLoad.updateCachedItem` both publish `*statistics.Table`; `TableStatsState::Loaded` now carries that same canonical Rust object and `ClusterTableStats` remains only at storage decode/write boundaries.

- Observation: Rust collapsed `LastAnalyzeVersion` and `LastStatsHistVersion` into the one `stats_meta.last_stats_histograms_version` value. Go initializes the former from the analyze snapshot and analyzed item versions, but uses the latter only as the independent table-level histogram refresh marker.
  Evidence: pinned bootstrap initializes both from `snapshot` and then advances them independently; pinned cache refresh assigns `LastStatsHistVersion` from the metadata row while changing `LastAnalyzeVersion` only from analyzed histograms or the zero-value snapshot fallback.

- Observation: the initial Rust `initstats` worker wrapped a single-consumer standard channel receiver in a mutex. Go's buffered channel permits every worker goroutine to wait independently, while the wrapper admitted only one waiting Rust receiver at a time.
  Evidence: pinned `RangeWorker.LoadStats` starts `concurrency` goroutines ranging directly over one channel; Rust now uses a bounded multi-consumer channel with the same capacity of one.

- Observation: Rust's load-stat JSON path had no equivalent of storage's gzip block framing, so `pkg/statistics/handle/history` could not persist or reconstruct the same `mysql.stats_history.stats_data` rows.
  Evidence: pinned `JSONTableToBlocks` marshals once, gzip-compresses once, and slices the compressed byte stream; `BlocksToJSONTable` concatenates ordered blocks before one gzip decode and JSON unmarshal.

- Observation: Rust's LOAD STATS converter returned only the planner's reduced statistics map and walked only visible columns. Pinned Go returns a canonical full `statistics.Table`, walks every `TableInfo.Columns` entry (including hidden expression-index columns), retains FM sketches and full-load state, and populates the column/index existence map.
  Evidence: pinned `TableStatsFromJSON` constructs `statistics.Column`/`statistics.Index` objects in a new `HistColl`; Rust now constructs the same full table before the durable cluster writer refreshes the shared planner cache. The former standalone cache-only LOAD STATS path was removed because Go has no server-filesystem/in-memory alternative.

- Observation: pinned LOAD STATS does not use the ANALYZE replacement policy. It commits each named column/index independently, leaves unmentioned histograms intact, clears only that item's prior TopN/buckets/FM rows, persists v1 CMSketch bytes, then performs a final meta upsert that preserves columns it does not name.
  Evidence: `loadStatsFromJSON` calls the restricted-session `SaveColOrIdxStatsToStorage` wrapper once per object and `SaveMetaToStorage` last; the Rust cluster writer now has separate item and final-meta plans, with regressions proving unrelated payload retention, stale-tail removal, CMS round-trip, per-item snapshot reset, and final-meta snapshot preservation.

- Observation: LOAD STATS data belongs to the MySQL connection, not the server filesystem or a cache-only executor. Go's text `handleStmt` parks a request, sends `0xfb + path`, concatenates client packets through the empty terminator, and resumes the same statement. The pinned prepared path does not call `handleFileTransInConn`, so adding a prepared-only transfer would exceed Go behavior.
  Evidence: pinned `clientConn.getDataFromPath`, `handleStmt`, and `handleStmtExecute`; the Rust wire regression proves the text request/data/terminator/OK sequence.

- Observation: the Rust statistics worker pool exposed an `is_closed` observer solely for its own tests. Pinned `handle/util` returns the external `gp.Pool`, whose API has `Go` and `Close` but no closed-state accessor.
  Evidence: pinned `pool.go` and pinned external module `github.com/tiancaiamao/gp` at `4025bc8a4d4a`; tests now verify post-close submission behavior directly.

- Observation: the existing Rust `SHOW COLUMN_STATS_USAGE` code was only two disconnected row helpers. Pinned Go reads the complete persisted usage map through the statistics handle on every SHOW, then visits the logical table ID and every partition ID regardless of prune mode.
  Evidence: pinned `pkg/executor/show_stats.go::fetchShowColumnStatsUsage`; the production cluster session now installs a storage-backed provider and the SQL regression exercises the global and partition rows under static pruning.

- Observation: pinned persisted ANALYZE options are raw planner input, not effective executor knobs. Statement values merge over stored values before the current global defaults are filled; `TOPN = 0` is present while stored `topn = -1` is absent, and LIST IDs are resolved through current table metadata so dropped columns disappear.
  Evidence: pinned `genV2AnalyzeOptions`/`getSavedAnalyzeOpts` in `pkg/planner/core/planbuilder.go` and `saveAnalyzeOptions` in `pkg/executor/analyze.go`; the Rust storage regression replaces a row and proves raw sentinel and stale-column behavior.

- Observation: legacy CMS width/depth remain validator-visible in Analyze v2, and their upper bound is the process-initial transaction entry-size limit divided by Go's maximum 32-bit varint length; accepting larger values even though the builder ignores them is not compatible behavior.
  Evidence: pinned `CMSketchSizeLimit` and `analyzeOptionLimit` in `pkg/planner/core/planbuilder.go`; Rust boundary tests now accept the limit and reject limit-plus-one for both spellings.

- Observation: pinned Go persists the final schema-ordered LIST after mandatory primary-key/index columns are added, not the user's literal `COLUMNS` list. A later statement with persistence disabled neither reads nor replaces that saved row.
  Evidence: pinned `getFullAnalyzeColumnsInfo` returns `colList` after `combineColumnSets`, `saveAnalyzeOptions` stores those IDs, and `genV2AnalyzeOptions` returns empty saved-option maps immediately when persistence is off; the in-process regressions cover all three effects.

- Observation: pinned global-statistics storage is a sequence of independent item transactions. It logs a failed item, continues with later histograms, and returns the final attempted write's result; a later success therefore clears an earlier error. Rust first stopped at the first error and then incorrectly retained the last failure seen.
  Evidence: pinned `WriteGlobalStatsToStorage` assigns the named `err` in every loop iteration without returning early; the Rust regression proves every item is attempted, successful receipts remain visible, a final success returns success, and a final failure is returned.

- Observation: Go schedules one global merge job for the complete column group and one separate job for each index, and `AnalyzeExec.handleGlobalStats` never returns those job errors to the ANALYZE statement. Missing-stat errors still append 8243/8244 warnings before returning, so multiple groups may add multiple warnings.
  Evidence: pinned `handleGlobalStats` groups by `(tableID,indexID)`, calls `MergePartitionStats2GlobalStatsByTableID` once per map entry, finishes each job with its error, and returns nil. Rust previously combined columns and indexes into one merge, failed ANALYZE on ordinary merge/storage errors, and retained only one missing-stat warning.

- Observation: `tidb_merge_partition_stats_concurrency` controls only TopN candidate probing, with a maximum batch of 256 partitions; the histogram merge remains a later single merge over the mutated partition histograms.
  Evidence: pinned `mergeGlobalStatsTopN` and `MergeGlobalStatsTopNByConcurrency`; Rust now snapshots the session value and produces the same TopN and histogram receipt at concurrency one and two.

- Observation: the blocking and async global-statistics workers deliberately classify missing data differently. Blocking treats an unanalyzed item as 8243 and a nonempty partition whose histogram plus TopN are empty as 8244; async always skips a partition with no histogram rows for that item kind, reports an absent target histogram row as 8244 when sibling rows exist, and does not reject an existing-but-empty payload.
  Evidence: pinned `blockingMergePartitionStats2GlobalStats` versus async `prepare`, `CheckSkipPartition`, and `CheckSkipColumnPartiion`; focused Rust regressions now cover every branch instead of applying the blocking predicate to both modes.

- Observation: a missing FM sketch is not a merge error. Go deliberately calls the nil-safe `(*FMSketch).NDV`, which returns zero, and then continues to CMS, TopN, and histogram merging.
  Evidence: pinned `FMSketch.NDV`, blocking `globalStats.Fms[i].NDV()`, and async `dealFMSketch`; Rust removed its extra `MissingFmSketch` error and now performs the same FM, CMS, and histogram/TopN phase reductions over exact per-item storage reads.

- Observation: global TopN cancellation is checked at partition and cross-partition loop boundaries, and the concurrent coordinator deliberately erases individual error identity by joining worker messages into a new error.
  Evidence: pinned `MergePartTopN2GlobalTopN`, `topnStatsMergeWorker.Run`, and `MergeGlobalStatsTopNByConcurrency`; Rust now preserves error 1317 on the sequential path and returns the joined generic merge error on the concurrent path.

- Observation: global-statistics history is recorded per successful item, not once after the whole merge, and `enforce=false` suppresses it until the logical table already exists as initialized statistics in the handle cache.
  Evidence: pinned `WriteGlobalStatsToStorage`, `statsReadWriter.SaveColOrIdxStatsToStorage`, and `RecordHistoricalStatsMeta`; Rust now performs the same independent, nonfatal `stats_meta_history` transaction with source `analyze`.

- Observation: Go excludes only virtual generated columns from ANALYZE sampling; a stored generated column is decoded from the ordinary persisted row bytes.
  Evidence: pinned `analyze_col_sampling.go` and both global-stat target builders test `IsVirtualGenerated`/`GeneratedStored`; Rust's scan already reads stored columns by name, so its additional all-generated-column rejection had no Go equivalent and was removed.

- Observation: a successful global-stat item save can still require a second transaction when its duration reaches five statistics leases; this prevents another node's cache watermark from passing the original version while the item is still being stored.
  Evidence: pinned `statsReadWriter.handleSlowStatsSaving`, `cache.LeaseOffset`, and `UpdateStatsMetaVerAndLastHistUpdateVer`; Rust now preserves all metadata except `version` and `last_stats_histograms_version`, and uses the refreshed version for history just like Go.

- Observation: Go's ordinary global indexes are part of the per-partition column-sampling task and are then merged into logical-table statistics; only a global prefix/expression index is split into an independent special-index task.
  Evidence: pinned `getModifiedIndexesInfoForAnalyze`, `IsSpecialGlobalIndex`, and `TestGlobalIndexStatistics`; Rust now admits the ordinary shape instead of rejecting every `IndexInfo.Global` value.

- Observation: global-index admission is governed by the static partition processor, not by partitioned-table metadata alone. Dynamic pruning must retain the path before physical optimization begins; clearing the processor flag after logical construction is too late.
  Evidence: pinned `DataSource.getPossibleAccessPaths` receives `removeGlobalIndexes=hasFlagPartitionProcessor`, while the Rust regression initially fell back to `TableFullScan` until the session prune mode was supplied to `PlanBuilder` before access-path enumeration.

- Observation: Go's `PhysicalIndexScan` overrides both `TP` and `ExplainID`, so an `IndexReader` names its pushed child `IndexRangeScan` or `IndexFullScan` according to the resolved ranges. Rust rendered the child row correctly but used the generic construction type `IndexScan` in the reader's operator info.
  Evidence: pinned `physical_index_scan.go` and `physical_index_reader.go`; the exact BRIEF regression initially returned `index:IndexScan` while its child was `IndexRangeScan`.

- Observation: Go exposes its concurrent global-TopN worker boundary independently of the session-variable selector, and its benchmark calls that boundary with an already bounded batch size.
  Evidence: pinned `MergeGlobalStatsTopNByConcurrency` and `topn_bench_test.go`; Rust previously kept the function private and recomputed its batch size internally, so it could not represent the original package API or benchmark artifact.

- Observation: the same benchmark also consumes Go's exported `MaxPartitionMergeBatchSize`; keeping `256` private in Rust left a package API gap and forced the benchmark to duplicate policy.
  Evidence: pinned `global_stats.go` exports the constant and `topn_bench_test.go` clamps with it; Rust now exports and tests the same value and imports it from the benchmark.

- Observation: the async global-statistics switch is not a silent boolean assignment. Pinned Go's validation closure appends the same warning for every session or global assignment, regardless of the assigned value.
  Evidence: pinned `pkg/sessionctx/variable/sysvar.go` registers the closure, and the first two globalstats tests assert warning 1105; Rust now exercises both scopes through the ordinary session SET path.

- Observation: Go's empty-TopN shortcut belongs to the private session-aware selector, not either exported TopN merge function. Direct callers therefore still observe SQL-killer polling for an empty TopN, while the ordinary global merge returns before entering a worker.
  Evidence: pinned `mergeGlobalStatsTopN` calls `statistics.CheckEmptyTopNs` before dispatch; `MergePartTopN2GlobalTopN` and `MergeGlobalStatsTopNByConcurrency` do not. Rust moved the shortcut to the same boundary and has regressions for both direct workers and selector behavior.

- Observation: the async coordinator must preserve both failures when its IO and CPU workers fail together. Rust's early `?` on a CPU thread panic discarded an already captured IO error, and its panic wrappers added diagnostics Go does not return.
  Evidence: pinned `MergePartitionStats2GlobalStats` waits for both errgroups and uses `errors.Join`; each worker's recovery converts the panic payload with `fmt.Sprint` only. Rust now flattens both worker results before the join, retains IO-before-CPU ordering, and returns the raw panic payload.

- Observation: the historical-stats worker exposed Rust-only result and inspection APIs solely to make its local tests easier, while Go deliberately makes disabled and full-mailbox sends indistinguishable to callers.
  Evidence: pinned `SendTblToDumpHistoricalStats` returns nothing and `GetOneHistoricalStatsTable` returns the `-1`/`0` sentinels; Rust removed `SendOutcome`, the outcome-returning send, the optional receive, the public session accessor, and the public queue-length accessor.

- Observation: the cluster refresh target set contained only logical table IDs, so partition ANALYZE persisted physical statistics that the shared cache and every later session catalog could never expose. Rust also applied lite loading under a zero stats lease, although Go disables the lazy-load shortcut when `lease == 0`.
  Evidence: pinned `StatsCacheImpl.Update` resolves every physical ID through `TableInfoByID`, while `TableStatsFromStorage`'s `notNeedLoad` predicate begins with `lease > 0`; the end-to-end regression failed first with zero META rows, then with zero bucket rows, and now matches the pinned static/dynamic counts across META, BUCKETS, HISTOGRAMS, and HEALTHY.

- Observation: Go's dynamic partition access text and pseudo marker are retained planner state, not EXPLAIN-time reconstruction. Static partition children also receive statistics only after `PartitionProcessor` assigns their physical IDs, and ANALYZE flushes each target's delta before sampling.
  Evidence: pinned `PhysPlanPartInfo`, `initStats`, `AnalyzeExec.Next`, and `GetStatsTable`; the source regression initially omitted `partition:all`, summed global statistics across static children, lost the logical realtime count, and omitted `stats:pseudo` before those four ownership boundaries were matched.

- Observation: `Session::with_catalog` bootstrapped a fresh local catalog through `Session::default` and then discarded that catalog, but retained the bootstrap DML's table-delta map. The first harmless autocommit statement published those fake IDs, which could later collide with newly allocated user tables.
  Evidence: pinned Go `createSessionWithOpt` only constructs session state over the domain infoschema; bootstrap runs separately in `bootstrapSessionImpl`. The failing health sequence observed local deltas for IDs 1 and 2 before any user DML, and both disappeared when shared-catalog construction stopped invoking the standalone-store bootstrap.

## Decision Log

- Decision: reconstruct and test each pinned Go branch before editing Rust; do not preserve Rust-only fallback paths.
  Rationale: the requested acceptance condition is behavioral parity, and local convenience policies are explicitly out of scope.
  Date/Author: 2026-08-29 / Codex

- Decision: keep statistics storage shared across sessions while making request/wait outcome statement-local.
  Rationale: this matches Go's domain statistics handle plus statement context split.
  Date/Author: 2026-08-29 / Codex

- Decision: do not claim `pkg/statistics` complete from individual files or functions.
  Rationale: repository policy makes the whole pinned Go package the atomic completion unit.
  Date/Author: 2026-08-29 / Codex

- Decision: make periodic, ANALYZE, and LOAD STATS refreshes consume the one canonical parent-cache update rather than preserving a cache-specific snapshot runner.
  Rationale: pinned Go has one `StatsCacheImpl.Update` path for full and targeted refreshes; retaining a second Rust path would duplicate its watermark, version, payload-reuse, batching, and deletion behavior and allow them to drift.
  Date/Author: 2026-08-30 / Codex

- Decision: make histogram memory an owned histogram operation rather than carrying a manually supplied measurement beside it.
  Rationale: Go computes `Histogram.MemoryUsage` from the live object. Rust's bounds representation differs, so its method measures the equivalent live ownership—histogram value, reserved bucket storage, and variable bound payloads—while preserving Go's empty-histogram zero and component aggregation behavior.
  Date/Author: 2026-08-29 / Codex

- Decision: implement lockstats policy once over a narrow transaction interface, with separate catalog and TiKV adapters.
  Rationale: pinned Go centralizes branching, warnings, and delta merging in `pkg/statistics/handle/lockstats` while its restricted SQL session is only the storage boundary. Sharing the policy prevents the two Rust runtime modes from diverging without inventing a Go-visible path.
  Date/Author: 2026-08-29 / Codex

- Decision: make `tidb-stats-handle-cache` own full `tidb_stats::Table` objects and keep reduced planner statistics as a derived consumer view.
  Rationale: pinned Go caches `statistics.Table`; wrapping the executor's reduced planner view would lose eviction payload, existence-map, histogram-version, and refresh semantics and create a second source of truth.
  Date/Author: 2026-08-29 / Codex

- Decision: keep LOAD STATS on the ordinary text/prepared connection path and use a dedicated restricted statistics writer underneath it.
  Rationale: reading the path with `std::fs` would replace Go's client-local-file behavior, while reusing ANALYZE's whole-table transaction would change item independence, history, and preservation of unmentioned histograms.
  Date/Author: 2026-08-29 / Codex

- Decision: represent explicit ANALYZE options separately from effective options and persist them through the shared `mysql.analyze_options` row codec after the statistics commit.
  Rationale: storing filled defaults would freeze a value Go deliberately re-reads live, and folding the row into the statistics transaction would change Go's warning-only failure contract.
  Date/Author: 2026-08-29 / Codex

## Outcomes & Retrospective

The exact storage item reader, lite bootstrap/refresh lifecycle, logical demand collector, split request/wait rule positions, partition expansion, newborn access-path pruning, synchronous worker concurrency/retry behavior, several SHOW surfaces, and cluster LOAD STATS path are integrated. The current milestone is not complete: remaining SHOW and whole-package inventory work stays open.

## Context and Orientation

`rust/crates/tidb-planner/src/logical/rule_collect_plan_stats.rs` corresponds to Go's logical statistics-usage collector and rule. `rust/crates/tidb-executor/src/driver/catalog.rs` owns the session-visible catalog and planner statistics cache. `rust/crates/tidb-exec/src/cluster_stats_load.rs` reads individual statistics objects from TiDB system tables. `rust/crates/tidb-server/src/cluster_session_node/mod.rs` connects production cluster snapshots to the executor catalog. `rust/crates/tidb-session/src/stmt_ctx.rs` snapshots system variables into `tidb_executor::StmtContext`.

A full load includes histogram payload needed for estimation; a metadata load preserves existence/load-state information without fetching all buckets and TopN data. Static partition pruning plans against physical partition table IDs, so a logical table request must be copied to those IDs. Plan-cache admission must be denied after sync-load fallback because a pseudo-derived plan must not be reused as though loaded statistics produced it.

## Plan of Work

First, finish the logical rule as one behavior: collect usage, mark a determinate full item per analyzed table, prune access paths, collect retained index demand, expand to physical partitions, request async or sync loading, and record Go's statement outcome. Keep planner-only traversal in `tidb-planner`; pass only the resulting item demand across the executor boundary.

Second, change startup and periodic refresh to load Go's lite table state. Prove a later item request adds exactly the requested payload and atomically republishes the planner view.

Third, map every pinned Go statistics `SHOW` producer and filter to Rust. Remove placeholder documentation and ignored/stub tests only after their behavior is either implemented or shown absent in the pinned package.

Finally, build a complete pinned-package inventory and run the required validation gates. A file/function match is evidence, not a package completion claim.

## Concrete Steps

Run commands from `rust/` unless stated otherwise:

    cargo check --locked -p tidb-planner -p tidb-executor -p tidb-session -p tidb-server
    cargo test --locked -p tidb-planner rule_collect_plan_stats::tests --lib
    cargo test --locked -p tidb-executor <focused_statistics_test>
    cargo fmt -p tidb-planner -p tidb-executor -p tidb-session -p tidb-server -- --check

From repository root, use `git diff --check` after every slice. Before a completion claim, follow `.agents/skills/tidb-verify-profile/SKILL.md` Ready profile and run `make lint` because code changed.

## Validation and Acceptance

Focused tests must prove metadata versus full demand, determinate first-column selection, exclusion of already-full and pruned indexes, virtual generated-column dependency discovery, static partition expansion, async publication, sync success, sync error, timeout fallback, wait capping, and plan-cache refusal. Production cluster tests must prove a fresh snapshot is used per requested item and the shared cache publishes the loaded object.

Package acceptance additionally requires a complete inventory of pinned `pkg/statistics` and its required tests/fixtures. No package-complete statement is permitted while an inventory row is absent or marked incomplete.

## Idempotence and Recovery

Checks and focused tests are safe to rerun. Statistics workers publish immutable table snapshots into shared locks, so a retry may replace a table with an equivalent or newer view. Do not reset or discard unrelated working-tree changes. If a test reveals a mismatch, return to the pinned Go function and update this plan's discovery or decision log before changing behavior.

## Artifacts and Notes

Pushed evidence includes commits `e0d5b4c1f0` (individual item loading), `00179562d2` (logical demand collection), `cb732f4908` (logical request integration), and `9cd431bd02` (newborn access-path pruning). The current wait-point slice compiles and has focused tests proving request dispatch and waiting occur at their separate pinned-Go rule positions, including timeout fallback only at the wait point.

    cargo check --locked -p tidb-session -p tidb-server

## Interfaces and Dependencies

`tidb_planner::logical::rule_collect_plan_stats::StatisticsLoadRequester` is the rule-to-runtime request interface. `tidb_executor::driver::StatisticsItemLoader` is the runtime-to-storage interface. `Catalog::request_statistics_load` owns session cache publication and wait semantics. `ClusterStatisticsItemLoader` owns production snapshot acquisition and `SharedStats` publication. These interfaces must remain narrow enough that the planner does not depend on cluster storage types, while their observable behavior remains identical to pinned Go.

Revision note (2026-08-29): created the living plan after completing exact item loading and logical usage collection; recorded the three parity gaps found while reviewing the first request-seam implementation.

Revision note (2026-08-29): completed the pinned first-phase interesting-column/access-path pruning behavior over Rust's newborn path representation and recorded why Go's later path-growth scoring fields are unreachable at this phase.

Revision note (2026-08-29): added the complete predicate-column storage primitives (load all/table, replacement save, cleanup-and-get planning), reused them from LOAD STATS, and wired the SHOW consumer through a fresh cluster snapshot. The parent usage package's ANALYZE PREDICATE COLUMNS consumer remains an explicit integration gap, so no package-complete claim is made yet.

Revision note (2026-08-29): moved synchronous waiting out of initial request dispatch and registered `SyncWaitStatsLoadPoint` at the pinned later logical-rule position.

Revision note (2026-08-29): made production bootstrap and refresh consume the existing lite table loader, removed the unused snapshot wrapper, and added Go's resident-payload-preserving update behavior.

Revision note (2026-08-29): restored the pinned globalstats package's exported concurrent TopN boundary and both benchmark workload shapes without closing the package-level claim; the original SQL integration-test inventory is still being reconciled.

Revision note (2026-08-29): moved the empty-TopN fast path from Rust's exported workers to the private global merge selector, preserving direct SQL-killer behavior and the ordinary no-worker shortcut from pinned Go.

Revision note (2026-08-29): corrected async global worker failure propagation so simultaneous IO and CPU failures are joined in pinned Go order, including CPU panics, and removed Rust-only panic-message prefixes.

Revision note (2026-08-29): corrected global item persistence return semantics after re-reading the Go loop: every item is still attempted independently, but the result is the final attempted write rather than the last failure retained across later successes.

Revision note (2026-08-29): split dynamic global merging into Go's complete-column group plus one group per index, made ordinary group failures job-local instead of statement-fatal, and accumulated every missing-stat warning for session delivery.

Revision note (2026-08-29): fixed the existing `SHOW STATS_META` production path before expanding the SHOW family; the new regression was observed failing with pseudo/global rows before the fix and passing afterward.

Revision note (2026-08-29): wired `SHOW STATS_HEALTHY` through the production cache, reused the session prune-mode traversal, and aligned analyzed-row selection with full-load status.

Revision note (2026-08-29): wired `SHOW STATS_TOPN` and verified the pinned integration result for a repeated column and its secondary index; the unique column/index correctly contributes no TopN rows.

Revision note (2026-08-29): wired `SHOW STATS_BUCKETS` and verified the pinned single-value row under `WITH 0 TOPN`.

Revision note (2026-08-29): wired `SHOW STATS_HISTOGRAMS`, removed caller-injected histogram-memory fields, and verified initialized column/index rows plus memory-component totals through the SQL path.

Revision note (2026-08-29): added the shared needed-item lifecycle and wired `SHOW HISTOGRAMS_IN_FLIGHT`; delayed-load coverage observes one live item and then zero after worker cleanup.

Revision note (2026-08-29): began the complete persisted ANALYZE-options behavior by separating raw and effective options, loading and replacing table-level `mysql.analyze_options` rows, resolving live defaults after merge, and saving through Go's later warning-only transaction. Static/dynamic partition option inheritance and persistence remain open, so this is not a completion claim.

Revision note (2026-08-29): centralized the pinned `genV2AnalyzeOptions` merge order and added direct regressions for static partition-over-table-over-statement inheritance and dynamic named-partition override suppression. The table-level cluster path now consumes that common policy. Partition execution/storage wiring remains open.

Revision note (2026-08-29): wired the shared option resolution into in-process execution, including named static partitions, table/partition inheritance, targeted statistics refresh, the live persistence gate, exact missing-mandatory-column warning, and final schema-ordered LIST persistence. Cluster partition execution/storage and dropped-object option GC remain open, so no package-complete claim is made.

Revision note (2026-08-29): moved the cluster explicit-column normalization to the planner/execution seam before sampling and option save. The resolved statement now carries Go's final schema-ordered LIST after mandatory PK/index columns are added, leaving the storage writer as a pure serializer.

Revision note (2026-08-29): replaced the cluster tier's blanket partition-table rejection with pinned static-mode target construction. Partition names resolve in definition/request order, each physical partition is scanned and committed with its own merged options and current physical row count, and the later option transaction saves the unchanged logical row plus each selected static-partition row. Dynamic mode remains refused until the existing Go histogram/TopN/FM merge primitives are connected to stored partition sketches; no rescan-based substitute is used.

Revision note (2026-08-29): connected Go's partition-only FM-sketch lifecycle end to end. Row and index analysis retain their sketches, physical-partition writes encode them into `mysql.stats_fm_sketch`, ordinary cache loads omit them, the full merge-input loader decodes them, and logical/global or LOAD STATS writes retract stale rows. A storage round-trip regression covers the partition, ordinary-load, merge-load, and global-delete boundaries. Dynamic global histogram/TopN/FM merging remains the next incomplete behavior.

Revision note (2026-08-29): completed the atomic inventory for pinned `pkg/statistics/handle/lockstats` (`lock_stats.go`, `query_lock.go`, `unlock_stats.go`; all four original test/support files; `BUILD.bazel`) and `pkg/executor/lockstats` (both executors, executor tests, `BUILD.bazel`). The Rust mapping is `tidb-stats::lock_stats` for policy/query/delta behavior, `tidb-executor::stats_lock` plus `tidb-session::stats_lock_arm` for the in-process restricted-session equivalent, and `tidb-exec::{cluster_stats_lock,real_tikv_stats_lock}` plus the server seam for one independent TiKV transaction. Focused tests cover stable skip messages, table/partition gates, delta propagation and clamping, real persisted system-row mutations, duplicate no-write warnings, INSERT-before-SELECT privilege admission, statement warning publication, and preservation of the caller's transaction.

Revision note (2026-08-31): re-reading every pinned lockstats production and test artifact exposed that the cluster adapter's “one independent TiKV transaction” was still an optimistic, whole-package mutation plan, unlike Go's `BEGIN PESSIMISTIC` wrapper and separate restricted SQL statements. Production now resolves targets once and drives the shared policy through the ordinary pessimistic statement engine: the initial lock query retains its transaction-snapshot decision, insert/version/select/update/delete run as distinct statements, staged writes are visible to later statements, and only a conflicting statement is rebuilt at its advanced `for_update_ts`. Errors roll back and duplicate `ON DUPLICATE KEY UPDATE` remains lock-carrying. The source-absent flattened planner and its test file were removed. A statement-boundary regression and an embedded-Unistore table/partition delta receipt cover the corrected path; the atomic package receipt now matches the already-complete checklist entry.

Revision note (2026-08-30): closed the complete pinned `pkg/statistics/asyncload` inventory (`async_load.go`, `async_load_test.go`, `BUILD.bazel`). `tidb-stats::async_load` owns the process-global 128-shard map and only Go's four operations; Rust-only construction, `Default`, `is_empty`, root re-export, and substitute concurrency tests were removed. Planner demand, explicit handle draining, `SHOW HISTOGRAMS_IN_FLIGHT`, and the positive-lease-only independent production ticker share that singleton; pinned Go's zero lease retains only the ordinary three-second loader and does not start `asyncLoadHistogram`. The loader uses Go's full-load eligibility check for columns while preserving metadata-only storage requests, always fully loads indexes, deletes every attempted item, preserves the index metadata/schema/payload read order, handles stale table/column/index metadata, and publishes through the shared statistics cache. Four DDL regressions mirror the dropped-object tests, the exact corrupt integer bound proves Go's warning conversion, and a fail-before/pass-after regression covers the previously discarded metadata-only request.

Revision note (2026-08-30): re-audited all 1,120 lines of pinned `pkg/statistics/handle/syncload` (`stats_syncload.go`, `stats_syncload_test.go`, `BUILD.bazel`). Removed the Rust-only process-global worker-pool policy: each shared statistics handle now owns its configured urgent/expired queues and worker group, while the source-global singleflight remains shared. Singleflight broadcasts under its group lock, a task dropped from a full expired queue waits for the original request timer instead of reporting a source-absent closed channel, and pool destruction joins active workers like domain shutdown. Three fail-before/pass-after regressions cover those differences. The load/cache and all six original test contracts are wired through `tidb-executor`, `tidb-exec`, and `tidb-server`, but the package is not atomically complete because its five shared Prometheus collectors belong to the separate 60-artifact `pkg/metrics` package, which has no complete Rust owner.

Revision note (2026-08-31): re-read the complete pinned `pkg/statistics/util` inventory (`json_objects.go`, `BUILD.bazel`; no package-local tests/support/fixtures/generated/platform variants) and the pinned tipb generated structs it embeds. The earlier mapping incorrectly used stable sorting, omitted non-omitempty zero protobuf scalars from JSON and `Size()`, and retained a source-absent predicate constructor plus four tests. Production now follows `slices.SortFunc`'s unstable contract and the generated protobuf tags/size methods exactly; the extras are removed, and the existing downstream `storage_test.TestJSONTableToBlocks` mapping carries the regression. The complete receipt is `rust/testport/receipts/statistics_util.md`.

Revision note (2026-08-30): audited the complete pinned `pkg/statistics/handle/metrics` inventory (`metrics.go`, `BUILD.bazel`; no tests/support/fixtures/generated/platform variants). Its ten bucket identities and twelve child handles depend on parent collectors owned, recreated, and registered by the separate 60-artifact `pkg/metrics` package. Rust currently has only private collector fragments, so this leaf remains unclaimed; two source-absent tests of that representation were removed. Inventory and exact blocker are in `rust/testport/receipts/statistics_handle_metrics_audit.md`.

Revision note (2026-08-30): completed the pinned `pkg/domain/metrics` inventory (`metrics.go`, `BUILD.bazel`; no tests, fixtures, generated inputs, or platform variants). Its package-shaped module binds all seven handles in one initializer: historical generation success/failure, plan-replayer dump success/failure, capture send/discard, and registered-task gauge. The live historical generator, plan-replayer collector, nonblocking channel, and dump result paths now consume those handles at the same points as Go. The domain and statistics-handle packages share collector identity but cannot rebind one another's package variables.

Revision note (2026-08-29, audited 2026-08-30): completed the atomic inventory for pinned `pkg/statistics/handle/logutil` (`logutil.go`, `BUILD.bazel`; no package-local Go tests or fixtures). Existing crate `tidb-stats-handle-logutil` maps `StatsLogger`, `StatsErrVerboseLogger`, `StatsSampleLogger`, and `StatsErrVerboseSampleLogger` onto the pinned background/error-verbose bases, `category=stats`, `sampled=""`, one shared sampler per factory, five-minute ordinary sampling, and ten-minute verbose-error sampling. The Rust-only emitted-file composition test contradicted the source-free test inventory and was removed; the shared factory's original tests remain with the separate `pkg/util/logutil` owner.

Revision note (2026-08-29): inventoried all 537 lines of pinned `pkg/statistics/handle/types/interfaces.go` plus `BUILD.bazel`. This remains a closure receipt rather than a new Rust crate: its declarations compose concrete packages that must be completed first, and equivalent Rust behavior is intentionally integrated through narrow owner-specific seams. Completed the concrete dependency packages `pkg/statistics/handle/usage/collector` (`collector.go`, `collector_test.go`, `BUILD.bazel`) and `pkg/statistics/handle/usage/indexusage` (`collector.go`, `collector_test.go`, `BUILD.bazel`). The Rust crates preserve the two ten-entry channels, five-minute priority escalation, close/drain lifecycle, node/session/statement aggregation, exact percentage buckets, modulo counters, latest-use timestamp, metadata GC, duplicate-query suppression, and benchmark support. A new regression proves the previously missing close branch of synchronous delivery.

Revision note (2026-08-30): completed the declaration-by-declaration reread of pinned `pkg/statistics/handle/types`. The shared cache payload now preserves Go's nested `CacheUpdate.Options.SkipMoveForward` contract rather than a Rust-only flattened field. `AnalysisJobJSON` now follows `encoding/json` for declaration order, decimal-text ordering of integer map keys, integral/negative-zero/exponent float rendering, and rejection of NaN/infinities; randomized-map and integral-float regressions failed before their respective corrections. The package remains a closure receipt, not a package-complete claim, until every interface embedded by `StatsHandle` has a completed concrete owner.

Revision note (2026-08-29): removed the ignored empty `rule_collect_column_stats_usage_source` and `collect_column_stats_usage_skip_system_tables_source` legacy gap carriers. The real collector is already exercised in its production module; a new executable test pins Go's `mysql.*` exclusion directly on that implementation instead of retaining documentation that says the feature is unported.

Revision note (2026-08-29): completed the atomic inventory for pinned `pkg/statistics/handle/cache/metrics` (`metrics.go`, `BUILD.bazel`; no package-local Go tests or fixtures). New crate `tidb-stats-handle-cache-metrics` binds `miss`, `hit`, `update`, `del`, `evict`, and `reject` counter children plus the `track` and `capacity` gauges to the exact `tidb_statistics_stats_cache_op` and `tidb_statistics_stats_cache_val` families. Focused tests cover independent children, gauge identity, and `InitMetricsVars` rebinding.

Revision note (2026-09-02): re-audited `pkg/statistics/handle/cache/metrics` at Go master `c6054025ed4c32ab3672a2a24ea46892714d21ec` (BUILD plus `metrics.go`, 67 lines, no tests/fixtures/generated/platform variants/benchmarks). Go binds eight children to shared `pkg/metrics` vectors; the Rust leaf's private vectors remain seed evidence and are explicitly not parity because collector registration and identity differ. The receipt records the exact blocker and Ready checks; completion stays open until the atomic shared `pkg/metrics` owner is available.

Revision note (2026-08-29): completed the atomic inventory for pinned `pkg/statistics/handle/cache/internal/lfu` (`key_set.go`, `key_set_shard.go`, `lfu_cache.go`, `lfu_cache_test.go`, `BUILD.bazel`). New crate `tidb-stats-handle-cache-internal-lfu` uses the Ristretto-compatible Stretto TinyLFU implementation with transparent integer keys, Go's counter/capacity/buffer settings, production internal-cost accounting, all three callback routes, 256 secondary shards, full-to-metadata eviction copies, cost tracking, dynamic capacity, close-once semantics, shared `Copy`, and buffered-write barriers. Focused tests consolidate all ten original Go cases across put/get/delete, replacement accounting, oversized rejection, length/value retention, concurrent access, metadata eviction, capacity reduction, and shared-copy behavior. Live selection remains assigned to the parent cache package and is the next integration slice.

Revision note (2026-09-02): re-audited the complete `pkg/statistics/handle/cache/internal/lfu` unit against Go master `c6054025ed4c32ab3672a2a24ea46892714d21ec` (five artifacts, 782 lines, ten race-enabled source tests, and no fixtures or generated/platform variants). The native LFU owner had two concrete source gaps: negative table IDs were routed by Rust's unsigned modulo instead of Go's invalid negative array index, and the test-mode zero-quota override was absent. Focused regressions failed before each fix and pass after; tagged current/detached Go tests (including detached race validation), eight native Rust owner tests, formatting, clippy, and Ready lint pass. Full package completion remains open because the Go owner depends on external `github.com/dgraph-io/ristretto` semantics; the exact inventory, hashes, and validation boundary are recorded in `rust/testport/receipts/statistics_handle_cache_internal_lfu_audit.md` and `rust/docs/operations/statistics-handle-cache-internal-lfu-audit-execplan.md`.

Revision note (2026-08-29): completed the atomic inventories for pinned `pkg/statistics/handle/cache/internal/mapcache` (`map_cache.go`, `BUILD.bazel`; no package-local Go tests or fixtures) and `pkg/statistics/handle/cache/internal/testutil` (`testutil.go`, `BUILD.bazel`; support library only). Existing crates `tidb-stats-handle-cache-internal-mapcache` and `tidb-stats-handle-cache-internal-testutil` cover every production/support symbol. Map replacement, deletion, memory cost, unordered keys/values, shared-value copy, and no-op controls match Go; the lock is solely the Rust ownership mechanism required by the common shared cache. Test tables preserve full-load construction and optional CMS/TopN/histogram components, while append helpers add the next numeric ID with only the Go CMS payload. Focused Rust tests cover the map's production behaviors; the support package has no independent upstream test gate.

Revision note (2026-09-02): re-audited `pkg/statistics/handle/cache/internal/testutil` against Go master `c6054025ed4c32ab3672a2a24ea46892714d21ec` (two artifacts, 109 lines, no tests, fixtures, generated/platform variants, or benchmarks). The complete constructor and append-helper mapping remains source-parity clean: negative counts create no entries, initial items are full-load while appended items retain zero status, optional CMS/TopN/histogram payloads and one-based IDs match, and native memory accounting exercises the same table path. Current/detached Go probes, owner tests/check, clippy, formatting, pinned lint, and diff checks pass; the refreshed receipt and living audit plan record the exact hashes.

Revision note (2026-08-29): completed the atomic inventory for pinned `pkg/statistics/handle/util/test` (`ctx_matcher.go`, `BUILD.bazel`; test-support package with no package-local tests or fixtures). Existing crate `tidb-stats-handle-util-test` checks the actual TiKV trace request source against `internal_{stats_foreground}`, panics on a non-context input like Go's direct type assertion, and exposes the exact matcher description through `Display`. No production integration is required because pinned visibility and use are test-only.

Revision note (2026-08-29): completed the atomic inventory for pinned `pkg/statistics/handle/util` (`auto_analyze_proc_id_generator.go`, `lease_getter.go`, `pool.go`, `table_info.go`, `util.go`, `util_test.go`, and `BUILD.bazel`; no fixtures, generated inputs, or platform variants). Existing crate `tidb-stats-handle-util` preserves every exported behavior and the pinned external `gp.Pool` size/recycle/close contract. The audit removed the Rust-only public closed-state observer and converted its tests to Go-observable post-close submission. Focused tests cover generator/tracker ordering, lease atomics, process-set semantics, worker and session pool ownership, V1/V2 partition lookup/cache invalidation, source-order partial session-variable updates, timezone synchronization, restricted SQL context/options, transaction commit/rollback/panic behavior, request timestamp composition, and special global indexes, including all four original Go integration cases through native seams.

Revision note (2026-08-29): wired cluster LOAD STATS end to end. The connection now advertises and enforces `CLIENT_LOCAL_FILES`, transfers text-protocol payloads with Go's packet sequencing, parses empty/null inputs as no-ops, executes one restricted TiKV transaction per named item plus usage and final metadata transactions, emits best-effort history under the pinned gates, and refreshes the same process-wide full-table cache used by ANALYZE while propagating LOAD STATS refresh failures. A first prepared-transfer draft was removed after direct inspection showed that pinned `handleStmtExecute` does not enter `handleFileTransInConn`.

Revision note (2026-08-29): corrected the common `cache/internal` ownership boundary before parent-cache integration. `StatsCacheInner` is now `Send + Sync`, and `mapcache` uses a poisoned-lock-tolerant `RwLock` instead of `RefCell`, preserving Go's shared-read behavior and COW copy independence. Focused tests cover cost replacement, deletion, copy isolation, and concurrent reads; the LFU suite proves the stronger trait does not change its behavior.

Revision note (2026-08-29): ported the pinned parent cache's `StatsCacheImpl`, `Update`, batch-update, healthy-distribution, and version-offset behavior. Refresh now preserves resident payload only when the latest histogram version permits it, deletes removed physical IDs, skips per-table load failures, observes cancellation before each row, publishes ten-row batches, and prevents targeted refreshes from advancing the lifecycle maximum. The production cluster storage image now converts to the full shared `tidb_stats::Table`, and the reduced planner statistics are derived from that object. Live `StatsSnapshot` ownership still needs consolidation onto this cache, and `stats_table_row_cache.go` remains outstanding, so the parent Go package is not yet claimed complete.

Revision note (2026-08-29): ported the complete pinned `stats_table_row_cache.go` behavior into the parent cache crate. Focused tests cover all-or-nothing two-read updates, negative-size clamping, missing-key zeros, exact ID SQL predicate text, fixed and variable columns, partition row aggregation, and the split between table-level global indexes and partition-level local indexes. The cache's executor-facing restricted-read adapter and `information_schema` consumption remain integration work, so this source completion does not yet close the parent package claim.

Revision note (2026-08-29): replaced LOAD STATS' reduced JSON conversion with pinned `storage.TableStatsFromJSON`'s canonical full-table shape, including hidden columns, FM sketches, physical IDs, full-load state, stats-version propagation, handle metadata, and the existence map. Removed the server's duplicate full-table-to-planner conversion and routed both Go `TableInfo` and executor `KvTable` metadata through one shared derived planner view. Cluster persistence, history rows, predicate-column usage, and the remaining storage package files are still outstanding, so this is explicit seed evidence rather than a package completion claim.

Revision note (2026-08-29): added the cluster mutation planners for pinned LOAD STATS' per-object `SaveColOrIdxStatsToStorage` and final `SaveMetaToStorage` policies instead of reusing ANALYZE's whole-table replacement. The production statement route, predicate-column usage writes, historical-meta recording, and cache refresh remain to be connected; this writer slice is not a storage-package completion claim.

Revision note (2026-08-29): completed the row-cache consumer path. A cluster catalog image now refreshes the process-wide cache from the same loaded `stats_meta` and column-histogram image, includes logical and physical partition IDs, computes the four Go size values from the original `TableInfo`, and carries them into `information_schema.tables`. The production regression observes `TABLE_ROWS=3000065`, `AVG_ROW_LENGTH=24`, `DATA_LENGTH=72001560`, and `INDEX_LENGTH=0` instead of the previous documented zeros. Parent-package closure still depends on replacing the parallel `StatsSnapshot` full-table ownership path with `StatsCacheImpl`.

Revision note (2026-08-29): replaced `StatsSnapshot`'s decoded storage-row payload with canonical `tidb_stats::Table` objects. Lite bootstrap converts once at the storage boundary; periodic refresh copies the canonical column/index maps and preserves or reloads resident payload according to pinned `TableStatsFromStorage`; sync load uses `ColumnMapWritable`/`IndexMapWritable`; planner and `information_schema` consumers read the same table object. The remaining parent-cache closure is to make the live owner itself `StatsCacheImpl` rather than the snapshot publisher.

Revision note (2026-08-29): made `StatsCacheImpl` the live table authority. Bootstrap, periodic refresh, deletion, and sync load now publish through the parent cache; the statement-facing snapshot indexes the exact cache-owned `Arc<Table>` objects and no longer mutates an independent table image. Cache construction errors propagate through node startup like pinned `NewHandle`, and focused tests prove pointer identity, deletion, held-reader immutability, and sync-load replacement. The parent-cache source inventory still requires final integration review before a package-complete claim.

Revision note (2026-08-29): continued the parent-cache integration audit and restored the pinned `StatsCacheGetNil` failpoint through the server feature graph. Corrected the storage boundary to carry `LastAnalyzeVersion` and `LastStatsHistVersion` separately; bootstrap, refresh of an existing table, refresh of a missing table, and analyze-produced tables now follow their distinct pinned update rules. Regressions use deliberately different snapshot, analyzed-histogram, and histogram-refresh versions so a future collapse cannot pass accidentally.

Revision note (2026-08-29): completed the atomic inventory for pinned `pkg/statistics/handle/cache` (`statscache.go`, `statscacheinner.go`, `stats_table_row_cache.go`, `statscache_test.go`, `bench_test.go`, and `BUILD.bazel`). The parent cache is the live Rust statistics authority, all production readers share its immutable table objects, the failpoint is reachable through the server feature graph, and the six original benchmark shapes are represented by a compiling benchmark target. Focused tests cover cache selection and updates, refresh and deletion, row-cache SQL and aggregation, live-owner identity, version-field separation, and feature-gated failpoint behavior. This closes only the parent cache package; the enclosing pinned `pkg/statistics` inventory remains incomplete.

Revision note (2026-08-30): re-opened the completed `pkg/statistics/handle/cache` claim after its receipt contradicted the plan and direct source comparison exposed an integration shortcut. Rust had refreshed `StatsTableRowCache` from the already-loaded planner snapshot during catalog construction, embedded one static logical-table tuple, and returned zero storage sizes for every `information_schema.PARTITIONS` row. The live node now performs Go's two fresh statement-time storage reads from the columns retained by the resolved physical memory-table scan, publishes both process-global maps atomically, retains prior estimates on a read failure, computes logical/global-index and physical-partition values separately, and passes the active resource group. This preserves Go's distinction: `TABLES` is pruned and refreshes only for a retained size column, while unpruned `PARTITIONS` refreshes on every scan. The audit also corrected Rust's case-sensitive memory-table pruning allow-list so ordinary lowercase SQL reaches the canonical Go rule. Targeted reads use deduplicated clustered-key prefixes instead of scanning unrelated statistics rows. The quota update watermark now uses pinned Go's one compare-and-swap attempt, and the unused public SQL-string helper was removed. Planner, storage, session, partition, cache, and server regressions close the corrected package receipt.

Revision note (2026-09-02): re-audited the same package against Go master `c6054025ed4c32ab3672a2a24ea46892714d21ec`. Go removed `stats_table_row_cache.go` and moved the behavior to statement-local `pkg/statistics/handle/storage/table_size_stats.go`. Rust now removes the stale process-wide row cache and its owner tests, computes the replacement `TableSizeStats` in the cluster statistics loader, passes Go's `needColLength` decision through the planner/session boundary, overlays results only on a statement scratch catalog, and returns zero size columns when either restricted read fails. The focused server regression failed before the overlay-order fix and passes after it. The refreshed root receipt records the five c605 artifacts and exact hashes. The historical 2026-08-30 note describes the pre-c605 behavior and is superseded for the rolling Go-master comparison.

Revision note (2026-08-29): completed the atomic inventory for pinned `pkg/statistics/handle/initstats` (`load_stats.go`, `load_stats_page.go`, and `BUILD.bazel`; no package-local Go tests, fixtures, variants, or generated inputs). `tidb-stats-handle-initstats` owns Go's force/default CPU formulas and `[2,16]` clamp, process-global atomic percentage, one-minute/one-message statistics sampler, one-slot task channel, worker lifecycle, error logging, and completion progress formula. Replacing the mutex-wrapped standard receiver with a bounded multi-consumer channel restores Go's independent receiver waiters without adding another execution path. The caller-side four-phase bootstrap remains part of the parent `pkg/statistics/handle` inventory rather than this leaf package.

Revision note (2026-08-30): completed the atomic inventory for pinned external package `pkg/statistics/handle/handletest/lockstats` (`lock_partition_stats_test.go`, `lock_table_stats_test.go`, `main_test.go`, and `BUILD.bazel`; 997 lines, 21 tests, no benchmark or fixture). The production mapping was already distributed across the shared lock policy, session and TiKV transaction adapters, delta dump, and DDL statistics GC. This batch added executable evidence for logical-parent lock inheritance, negative partition deltas merged into both partition and global metadata on whole-table unlock, and stale lock-row removal by hard GC. The old unclaimed audit was replaced rather than preserving obsolete ignored gap carriers or introducing an eager DDL-specific lock path absent from Go.

Revision note (2026-08-30): completed the atomic inventory for pinned external package `pkg/statistics/handle/handletest/statstest` (`stats_test.go`, `main_test.go`, and `BUILD.bazel`; 934 lines, 17 tests, no benchmark or fixture). Direct comparison found that Rust collapsed Go's non-lite histogram/CMS, TopN, and bucket startup stages, bypassing the live one-quarter-system-memory/statistics-quota admission checks, and that the real post-ANALYZE publication moved the cache lifecycle version. Startup now admits each stage separately, retains metadata after capacity is reached, preserves TopN when buckets are blocked, and marks no-bucket indexes full at the TopN stage. ANALYZE now uses the targeted skip-move-forward cache option in quota mode. Focused regressions also cover missing DDL histogram metadata, TopN without buckets, and one transaction version across a delta batch. The stale unclaimed audit was replaced with the complete package receipt.

Revision note (2026-08-29): completed the atomic inventory for pinned `pkg/statistics/handle/internal` (`testutil.go` and `BUILD.bazel`; no package-local Go tests, fixtures, variants, or generated inputs). Existing `tidb-stats-handle-internal::assert_table_equal` preserves realtime/modify counts, column/index cardinality and presence, Go `HistogramEqual(..., false)` through the same textual form, CMS equality, nil-or-empty TopN equality and ordered entries, and the complete column/index existence maps. No new code or tests were added because the pinned package contains no other behavior or original test artifact.

Revision note (2026-08-29): connected `ANALYZE ... PREDICATE COLUMNS` and explicit column targets to the shared predicate-usage store for both execution tiers. Selection now unions public index/PK columns and recursively closes generated-column dependencies. Partial ANALYZE persistence and in-process publication replace only selected items, preserving older unselected histograms as pinned `SaveAnalyzeResultToStorage` does. Both execution tiers publish the exact pinned empty-predicate warning. Persisted analyze-option storage remains outstanding, so the parent usage/storage packages are not claimed complete.

Revision note (2026-08-29): added pinned `SaveColumnStatsUsageToStorage` seed evidence for LOAD STATS. One planned transaction REPLACEs each `(table_id,column_id)` row, parses dump timestamps as UTC TIMESTAMP(6), converts them to the cluster table's declared precision, and preserves explicit NULL replacement. System-row reconciliation now supplies a timezone when a full system-table row contains TIMESTAMP columns. `TableStatsFromJSON` also consumes one schema-only metadata contract, allowing the forthcoming cluster route to use canonical `model.TableInfo` without constructing an executor table or duplicating JSON conversion. The production statement route, historical-meta recording, transaction execution, and cache refresh remain outstanding, so the storage package is not claimed complete.

Revision note (2026-08-29): lowered LOAD STATS against the cluster's canonical `model.TableInfo`, including the pinned nil-versus-present partition rule, selective partition-name matching, logical `global` target, all schema columns/indexes, signed counts, and stable column-before-index item order. Corrected two edge mismatches found by reading the pinned code: negative per-item counts now update only existing version markers instead of REPLACE-resetting meta, and nullable `[]*JSONPredicateColumn` entries are represented and skipped during persistence. Transaction execution, historical-meta recording, cache refresh, and server routing remain outstanding; this is still storage-package seed evidence.

Revision note (2026-08-29): added the real-TiKV LOAD STATS storage coordinator with pinned per-column, per-index, predicate-usage, and final-meta transaction boundaries. Historical meta uses a later best-effort transaction, requires the exact current stats version, preserves Go's `enforce=false` initialized-cache gate for items and `enforce=true` behavior for final meta, and writes source `load stats`. Shared-cache refresh and server/file-transfer routing remain outstanding, so this remains explicit storage-package seed evidence rather than a package-complete claim.

Revision note (2026-08-29): removed the blanket `ANALYZE TABLE ... INDEX` refusal for the pinned stats-v2 ordinary-index branch. Both execution tiers now retain the requested index names until table metadata is available, validate public names with Go's exact 8109 message text, run the ordinary full-sampling path, and publish Go's exact “collect all statistics” warning. The all-special-global-index branch remains an explicit gap because pinned Go constructs an independent ordered index scan; no row-sampling substitute was added, and no executor/statistics package completion is claimed.

Revision note (2026-08-29): replaced the schema-lease-driven statistics follower with pinned Go's independent `Performance.StatsLease` lifecycle. The configuration retains negative, zero, and positive values: negative publishes an empty cache and starts no initialization/reload worker, zero uses Go's three-second fallback, and positive uses the exact duration. Nonnegative initialization now runs as the worker's immediate first pass rather than blocking node startup, while only a positive lease reaches the slow-save version fence. This closes the lease scheduling mismatch but is not a parent-domain package completion claim.

Revision note (2026-08-29): added the shared statistics core for pinned `analyzeIndexProcessor`, `updateIndexResult`, and `AnalyzeIndexExec.buildStatsFromResult`. Each region consumes ordered encoded index-column slices, excludes one-column NULLs into `NullCount`, retains NULL-bearing composite tuples, inserts every progressively longer prefix into the default 5-by-2048 CMS, and returns its unstandardized histogram plus CMS/TopN/FM fragment. The TiDB-side reducer then uses Go's `MergeHistograms`, CMS/FM merge, and `MergeTopNAndUpdateCMSketch` before removing only the final TopN and applying stats-v2 index standardization. A rejected integration draft used one globally ordered snapshot stream; direct comparison showed that this loses Go's region-boundary-dependent TopN and bucket behavior, so it was removed rather than retained as an approximation. Exact region-fragment transport, independent-save policy, and planner routing remain outstanding, so this is explicit seed evidence rather than an executor or statistics-package completion claim.

Revision note (2026-08-29): added exact serving-region snapshot fragments to the transaction coordinator and connected them to the independent global-index analyzer. Multi-page reads from one epoch remain one fragment, while an epoch change before completion discards and retries the partial fragment, matching Go's successful-coprocessor-response boundary. The analyzer now cuts declared index columns from each region key, runs one pinned processor per fragment, and feeds the existing Go-shaped reducer. A regression preserves the region-dependent TopN approximation that a globally flattened scan would lose. Independent-save policy and special-global planner routing remain outstanding, so no package-complete claim is made.

Revision note (2026-08-29): connected pinned special-global-index task selection, execution, and persistence. The planner now separates global prefix/virtual-expression indexes from ordinary column sampling, preserves Go's named-index validation, partition error, collect-all branch, and empty-name edge behavior, then executes each independent logical-index scan in its own transaction. Its `ForMVIndexOrGlobalIndex`-shaped write replaces only that index, preserves existing count/modify-count/snapshot metadata, creates missing metadata with count and snapshot zero, and removes stale logical-table FM rows. Dynamic global merging runs before these logical-only index reports are appended, so it does not incorrectly request partition copies of independently analyzed indexes. This closes the previously listed transport/routing/save gaps but remains part of the incomplete enclosing executor/statistics package inventories.

Revision note (2026-08-29): restored the common pinned post-save lifecycle for ordinary and independent ANALYZE results. Every normal physical result now measures the complete save, applies the positive-lease five-lease metadata-version fence, and best-effort records enforced historical metadata when the live switch is enabled. An independent special-index result does so only when its save inserted a missing `stats_meta` row; the existing-row update deliberately reports no storage version in Go and therefore skips both steps. Global item history keeps its separate non-enforced initialized-cache gate. This removes the Rust-only restriction of slow-save fencing/history to global-stat item writes without claiming the enclosing storage package complete.

Revision note (2026-08-30): added pinned `storage.GenJSONTableFromStats` as the inverse of the existing canonical `TableStatsFromJSON` path. The dump walks stable full-table column/index objects, lowercases their schema names, converts typed column bounds to the BLOB bytes Go writes while retaining raw index-key bounds, and preserves CMS, TopN, FM, counts, versions, and predicate usage in the shared JSON model. A regression was recorded failing on the absent conversion and now verifies both bound domains. Physical-table storage loading, partition/global assembly, and production history-worker routing remain open, so the enclosing history package is not claimed complete.

Revision note (2026-08-30): transcreated the pinned executor receipts for historical generation after ANALYZE. The disabled global switch leaves the mailbox empty; enabling it queues a successful physical target and the production domain dump writes canonical history blocks. Reading pinned `analyze.go` and `analyze_global_stats.go` exposed that Rust's aggregate report had discarded physical partition IDs: it now preserves every successfully saved physical ID and adds the logical ID for dynamic global work, so static and dynamic partition ANALYZE queue exactly Go's target sets. The broader cross-package history-reader, fallback, and GC inventory remains open, so no enclosing package-completion claim is made.

Revision note (2026-08-30): closed pinned `pkg/statistics/handle/history` atomically. Its complete upstream inventory is `history_stats.go` and `BUILD.bazel`; there are no package-local tests, support files, fixtures, generated inputs, or platform variants. Rust maps constructor integration, the live enable check, enforced and initialized-cache-filtered metadata recording, exact-version metadata locking, independent best-effort transactions, full table/partition JSON generation, canonical gzip block storage, and Go's version selection. The pinned handletest storage case and executor table/static-partition/dynamic-global consumers have end-to-end receipts, focused crate checks pass, and `make lint` passes. Historical reading, fallback, and garbage collection belong to the still-open pinned `pkg/statistics/handle/storage` package rather than this completed package.

Revision note (2026-08-30): re-audited the complete history package against both pinned artifacts and corrected its last timestamp mismatch. Go's metadata writer deliberately uses `NOW(6)` and its payload writer formats `time.Now()` to six fractional digits; Rust had declared FSP 6 while zeroing the microsecond field. All production history-meta sources and the payload writer now share a local `DATETIME(6)` clock, while the separate column-usage path retains Go's second-precision `TimeFormat`; the fixed-fraction history regression fails before and passes after the correction.

Revision note (2026-08-30): inventoried pinned `pkg/statistics/handle/storage` as `BUILD.bazel`, six production files (`gc.go`, `json.go`, `read.go`, `save.go`, `stats_read_writer.go`, `update.go`), and four original test files (`dump_test.go`, `gc_test.go`, `read_test.go`, `stats_read_writer_test.go`), with no generated inputs, fixtures, or platform variants. Added `TableHistoricalStatsToJSON` and the common `DumpHistoricalStatsBySnapshot` production path: the two historical version searches remain independent, blocks are ordered by sequence, metadata counters overlay the restored payload, feature-gate failures precede metric accounting, and missing table/partition/global history falls back through the ordinary two-transaction live dump while returning Go's exact fallback names. Version-skew, disabled-feature, table history, partition/global assembly, and latest-stats fallback receipts pass. The rest of the ten-file storage inventory, including GC, remains open, so this is seed evidence rather than a package-completion claim.

Revision note (2026-08-30): added pinned `storage.ClearOutdatedHistoryStats` without normalizing its quirks. Rust reads the existing `tidb_historical_stats_duration` global definition, computes the same wall-clock cutoff, performs the initial metadata count, commits each 1,000-row metadata delete as an independent restricted transaction, and executes exactly one 50-row payload delete because the pinned Go function returns from inside that loop. A planner receipt proves cutoff selection and retention of newer rows; the production unistore receipt matches `TestGCOutdatedHistoryStats` and proves both system tables are cleared. The remaining `gc.go` table/histogram GC and the rest of the atomic storage inventory remain open.

Revision note (2026-08-30): added the pinned storage GC deletion plans after re-reading `gc.go`. `gcHistoryStatsFromKV` remains two separately executable plans in statement order; `deleteHistStatsFromKV` advances both metadata version fields and removes exactly one histogram item plus column usage in one plan; `DeleteTableStatsFromKV` handles all physical IDs in one plan, retaining zeroed histogram metadata for soft deletion and removing it for hard deletion while clearing every pinned dependent table. Focused storage-image receipts cover history removal, one-column preservation, and both table-deletion phases. The public `GCStats` loop, persisted `tidb_stats_gc_last_ts`, lease/TSO cutoff, live schema lookup, and production two-phase invocation are still absent, so the atomic storage package remains open.

Revision note (2026-08-30): wired the pinned `storage.GCStats` loop to the production cluster transaction seam. Each Go restricted statement remains an independent snapshot/commit, the metadata scan is `[lastGC,gcVer)`, stale schema items are removed one transaction at a time, dropped tables retain `stats_meta` for one pass, and the successful pass upserts the next timestamp after warning-only historical expiry. Re-reading `TableInfoByID` and `InfoSchema.TableByID` exposed their intentional partition difference: a partition resolves to its parent for item reconciliation but still triggers physical-history deletion. A new timestamp receipt failed before the fix because a VARCHAR identity was written as a byte datum and duplicated the `mysql.tidb` row; it now uses the column collation and proves two successive upserts. The pinned nonpartitioned end-to-end GC sequence passes. Remaining original partition/usage/support receipts and the parent domain scheduler keep the atomic storage package open.

Revision note (2026-08-30): reconciled the remaining GC-specific cases in pinned `gc_test.go`. The embedded-store receipts now preserve exact nonpartitioned and partitioned histogram/bucket counts across stale index and column removal, the two dropped-table phases, Go's retained logical partition metadata quirk, column-usage cleanup, and the existing-table empty-histogram guard. The test obtains physical identity from the live catalog, matching Go's InfoSchema lookup and avoiding an unrelated unsupported bare covering-index query. `TestDeleteAnalyzeJobs` and the other production/test files in the storage inventory remain open, so this is not an atomic package-completion claim.

Revision note (2026-08-30): reconciled pinned storage `TestDeleteAnalyzeJobs` for the ordinary one-task table ANALYZE path. The shared writer now persists Go's pending, running, finished/failed transitions, 65,535-byte truncation, processed-row accumulation, cleared process ID, and strict update-time cleanup predicate through independent restricted transactions; bookkeeping failures remain non-fatal. A planner receipt covers the full row lifecycle and cutoff boundary, and the embedded-store receipt proves ordinary ANALYZE creates the observable finished row before cleanup removes it. Partition, independent-index, progress-dump, global-merge, and corrupted-job cleanup task lifecycles remain part of the open autoanalyze/executor inventory, so neither the enclosing storage nor autoanalyze package is claimed complete.

Revision note (2026-08-30): ported pinned `CleanupCorruptedAnalyzeJobsOnCurrentInstance` and `CleanupCorruptedAnalyzeJobsOnDeadInstances` through the shared cluster storage writer and restricted transaction boundary. The source-shaped receipt covers stale pending/running selection, live analyze versus ordinary process IDs, the deliberate NULL-process skip, terminal/recent jobs, dead versus alive server addresses, exact failure text, and unchanged `update_time`. Exercising the timestamp result also closed the unistore coprocessor's existing `newRowDecoder` gap by resolving the DAG request's fixed, system, or named time zone and passing it to row decoding like Go. No enclosing package is claimed complete.

Revision note (2026-08-30): closed the surrounding pinned Domain lifecycle for analyze-job cleanup. Real-cluster nodes campaign an etcd `OwnerManager` and unistore uses Go's `MockManager`, both under the exact `/tidb/stats/owner` key and `stats` prompt/config gate. The worker exists only for a positive statistics lease, uses independent one-hour GC and `100 * statsLease` cleanup deadlines, applies the seven-day and ten-minute cutoffs at tick time, runs current-instance cleanup on every node, gates history GC and dead-instance cleanup on live ownership, and is synchronously stopped on factory teardown. The lifecycle receipt covers the zero/negative gate, positive startup, and prompt shutdown; transaction behavior remains covered by the source-shaped embedded-store receipt.

Revision note (2026-08-30): replaced the temporary session-level one-job ANALYZE wrapper with the pinned executor task lifecycle. The resolved physical plan now retains partition names, the exact pre-dispatch column selection, and the adjusted sample rate used by both sampling and `job_info`; every partition and independent-index task is inserted pending before dispatch, then started and finished at its execution boundary. Scan responses feed Go's strict `>10,000,000` row and `>5s` progress gate, finish flushes the residual only for table jobs, failures and panics fail the current and every undispatched job, and independent index work precedes dynamic global merges. Global column/index merge jobs are inserted later and never accumulate processed rows. The old shape guard and duplicate session execution pipeline are gone. Planner, embedded-store, partition/global, progress, and failure-order receipts pass. Corrupted-job cleanup and the remainder of the executor/autoanalyze inventories remain open, so this is not an enclosing-package completion claim.

Revision note (2026-08-30): completed a whole-file source/test audit of pinned `pkg/executor/show_stats.go` and `show_stats_test.go`. The audit exposed one missing dispatch behavior outside the SHOW-STATS helper file: pinned `show.go` retains `SHOW STATS_EXTENDED` only to return `Extended statistics feature has been removed`, while Rust returned its generic unsupported-statement message. The exact source test now fails before and passes after that dispatch. Production `SHOW STATS_META` also consumes the shared `appendTableForStatsMeta`-shaped helper instead of maintaining a second session-local row construction, and the end-to-end analyze-status receipt now includes the source partition/global-column/global-index job sets. This is executor integration evidence and does not claim the enclosing package complete.

Revision note (2026-08-30): completed the atomic inventory for pinned `pkg/executor/internal/pdhelper`. The preexisting cache value object covered the direct underscore key, capacity, TTL, LRU, and hit/miss contract, but its production consumer was duplicated around SHOW and ANALYZE passed no approximate count at all. One node-global helper now owns PD loading, the exact small-table count fallback, failure caching, and a Start/Stop-shaped expiry worker; SHOW and ANALYZE share it without holding the cache lock during storage reads. ANALYZE passes the active statement resource group and partition identity, and the auto-rate helper now returns pinned Go's `0.001` when both statistics and PD counts are unavailable instead of the old Rust-only full-scan policy. Focused tests cover the original TTL sequence, cleanup deadline, source identity, stale-count `150000/approximate` rule, missing-statistics fallback, and resource-group propagation.

Revision note (2026-08-30): completed the atomic inventory for pinned `pkg/statistics/handle/usage` (`index_usage.go`, `predicate_column.go`, `session_stats_collect.go`, `BUILD.bazel`, `export_test.go`, `index_usage_integration_test.go`, `predicate_column_test.go`, and `session_stats_collect_test.go`) and its `predicatecolumn` child (`predicate_column.go` and `BUILD.bazel`; no package-local tests). Existing Rust production paths already covered the source test sequences, including the skipped Go stress test's batching/requeue contract. Re-reading Go Domain shutdown found one lifecycle gap: Rust forced the final delta flush while periodic usage threads were still live, whereas Go first exits the usage workers and only then runs delta-worker exit preprocessing. The new regression failed before `stop_before_shutdown_flush` existed and passes after the factory takes, drops, and joins the worker set before flushing.

Revision note (2026-08-30): completed pinned `pkg/statistics/handle/autoanalyze/exec` atomically. The complete upstream inventory is `exec.go`, `exec_test.go`, and `BUILD.bazel`, with no fixtures, generated inputs, support files, or platform variants. The Rust package preserves v2 option rewriting and escaped warning SQL, snapshot/prune/current-session options, process tracking and release, panic recovery with zero return values, success/failure metrics and logging, parameter reads, ratio parsing, and UTC analysis windows. The source-shaped tests cover the execution option set, legacy rewrite, interruption, parameter parsing, and the deferred panic boundary. A fail-before receipt exposed that Rust floating-point `max` suppresses `NaN` while Go `math.Max` propagates it; the parser now preserves Go's result explicitly. The package consumes the existing restricted-executor/session boundary; scheduling and job selection remain owned by the still-open parent `autoanalyze` package.

Revision note (2026-08-31): re-auditing the complete pinned exec call graph exposed that production priority-queue ANALYZE still bypassed the completed package through a server-only identifier binder and direct executor shortcut. The queue now calls the shared `auto_analyze` entry point, and the live restricted executor consumes the same v2, snapshot, prune-mode, current-session, and process-tracking options as Go. Drop guards release and untrack the process on ordinary errors and recovered panics. The post-GC domain hook now checks the configured window and kills IDs from the shared auto-analyze process list through the live process registry. Embedded-store receipts cover successful current-session execution, tracking cleanup, and inside/outside-window interruption. An older priority-queue integration test still fails at its pre-execution dropped-table queue assertion; that separate asynchronous DDL lifecycle failure is not hidden or attributed to the exec path.

Revision note (2026-08-30): completed pinned `pkg/statistics/handle/autoanalyze/priorityqueue` atomically after reading its ten production files and complete original test/support/build inventory. Source comparison corrected four subtle divergences before integration: the DML watermark is captured before the cache scan, static retry recreates the whole logical table, concurrent close callers wait for the one worker reset, and duration strings use Go's nanosecond/microsecond/millisecond units. The production server source reads the shared catalog/statistics/lock/global-variable state, receives admitted DDL events after publication, and runs generated ANALYZE through the ordinary session executor. The end-to-end receipt initially failed because the unchanged Go validation SQL selected `mysql.analyze_jobs`' index and unistore explicitly refused bare index DAGs; replacing the legacy aggregate-only branch with Go's general index-entry decode and Selection/Limit/Aggregation composition fixed the root execution gap without changing the SQL or forcing an access path. Seventeen package tests, the targeted server receipt, server test compilation, formatting, and the Ready lint gate form the package validation set.

Revision note (2026-08-30): completed the separate test-only `pkg/statistics/handle/autoanalyze/priorityqueue/calculatoranalysis` package after reading its README, both Go test files, build target, and 691-line fixture. The Rust test uses the production `AnalysisJob` and `PriorityCalculator`, reproduces all 11 table sizes, 13 elapsed durations, and six change factors, preserves Go's stable descending CSV order and formatting, and byte-compares all 690 generated rows against the pinned fixture consumed directly from the source tree. The parent priority-queue receipt was reconciled from its obsolete pre-integration audit state; the separate `intervaltimezone` package remains open.

Revision note (2026-08-30): completed pinned `pkg/statistics/handle/autoanalyze/refresher` atomically after reading `refresher.go`, `worker.go`, all three original test files, and `BUILD.bazel`. The Rust refresher initializes before checking the time window, rebuilds only after publishing changed ratio/prune settings, reads concurrency and partition batch size live, excludes already-running table IDs, validates before submission, recovers worker panics, and waits for running work before close. The domain worker now ticks only for a positive statistics lease, checks the process switch and statistics ownership on every tick, invokes the shared refresher through the production path, and closes the queue when disabled or ownership is lost so re-acquisition rebuilds it. Pinned Go's now-unconditional priority scheduler also exposed Rust accepting `SET GLOBAL tidb_enable_auto_analyze_priority_queue=OFF`; the exact validation refusal is wired. Five refresher tests, seventeen priority-queue tests, the sysvar regression, two embedded-server lifecycle receipts, server test compilation, formatting, and the Ready lint gate form the package validation set.

Revision note (2026-08-30): completed pinned root `pkg/statistics/handle/autoanalyze` after rereading all 920 production lines, 615 test lines, and `BUILD.bazel`. The package remains a facade over its live owners rather than becoming a duplicate Rust crate. Auto-generated SQL is reparsed and executed by the ordinary physical ANALYZE path with only Go's observable automatic-job marker. Exact root tests exposed and fixed the shared TopN inline-projection gap in the unchanged duration query and the missing GLOBAL skipped-column/vector filtering in ANALYZE planning. Predicate selection, locked-table rebuild, skipped types, automatic job text, missing-partition fill, current/dead-instance cleanup, windows, ratio/index rules, and permanent queue enablement now have executable owners; the obsolete ignored missing-partition planner body was removed. The parent-package receipt maps all 14 upstream tests and the complete build inventory.

Revision note (2026-08-29): closed the first pinned globalstats SQL-integration pair as an end-to-end receipt. Cluster cache target construction now expands each logical table to its global and physical partition IDs, session catalogs install every loaded physical entry, and zero-lease boot/refresh loads complete payloads as Go does. The exact pinned SHOW row counts pass for both static and dynamic pruning. This does not yet close the whole `pkg/statistics/handle/globalstats` claim; the remaining original tests/support artifacts are still being reconciled.

Revision note (2026-08-29): replaced the Rust-only GLOBAL-index refusal while reconciling pinned `globalstats.TestGlobalIndexStatistics`. GLOBAL entries now use the logical table ID, retain the physical partition ID in their value, preserve the stored `GlobalIndexVersion`, use V1 partition-handle key suffixes where pinned `setGlobalIndexVersion` selects them, filter pruned reads by the value's partition ID, and enforce uniqueness across physical partitions. The exact pinned six-row/index-range result and a cross-partition duplicate regression pass. This fills the storage/execution dependency of the globalstats test but does not claim the larger DDL or globalstats packages complete.

Revision note (2026-08-30): reconciled pinned `globalstats.TestGlobalStatsNDV` against the real embedded-store path. The failing source sequence exposed that Rust had reused one `TableStatistics::pseudo` flag for Go's cached statistics table and the separate pseudo table returned by planner `GetStatsTable` when realtime count is zero. SHOW consequently hid analyzed empty column/index rows. Rust now preserves the cached `HistColl.Pseudo` identity separately and SHOW suppresses only that synthetic cache object; the optimizer keeps its existing zero-row pseudo behavior. The full empty/populated/duplicate NDV sequence passes for the global index and all four partitions. This closes one original test behavior but does not complete the remaining globalstats test/support inventory.

Revision note (2026-08-30): reconciled pinned `globalstats.TestGlobalStatsIndexNDV` and `TestGlobalStatsVersion` against the embedded-store path. The five-type index-NDV sequence passed without a production change. The version sequence failed before the fix with global `(modify_count, count) = (0, 7)` instead of `(2, 9)`: Rust parsed `FLUSH STATS_DELTA` but refused execution, leaving DML deltas only in the node collector while global merge read stale `mysql.stats_meta` partition rows. The cluster session now publishes its statement delta and invokes the existing forced delta transaction with Go's global/database/table target expansion, including every partition ID and missing-object warnings. Global scope is represented separately from an empty valid-target list so a missing object cannot accidentally flush every table. The source sequence now preserves two un-analyzed p1 modifications after analyzing p2, clears them after analyzing p1, and recomputes count seven after dropping p2. These receipts close two original test behaviors but do not complete the remaining globalstats test/support inventory.

Revision note (2026-08-30): reconciled pinned `globalstats.TestDDLPartition4GlobalStats`. The first source run failed at DDL admission because the cluster route refused `ALTER TABLE ... TRUNCATE PARTITION`; after the shared partition executor was wired into cluster DDL, the next run reached the statistics assertion but retained global count 15 instead of 11. Pinned Go's DDL subscriber creates empty metadata and histogram-placeholder rows for replacement partition IDs, applies `(Count=count, Delta=-count)` to the logical row, version-marks retired rows for delayed GC, and preserves every helper's SQL-statement boundary inside the notifier's one pessimistic transaction. Rust now performs that same ordered follow-up and republishes the affected cache entries. The source sequence passes with fresh IDs for p2/p4, immediate logical count 11, seven visible metadata rows after re-analysis, and final count 11. This closes one original globalstats test behavior but does not complete the remaining package inventory.

Revision note (2026-08-30): wired the pinned table-lifecycle DDL subscriber actions after re-reading `subscriber.go` before implementation. Rust captures dropped metadata before DDL, resolves created metadata after DDL, calls the GLOBAL prune-mode accessor independently for each new and old partitioned `TableInfo`, and runs `InsertTableStats2KV` plus delayed version refreshes in Go order inside one pessimistic transaction. The common partition transaction now reads retired counts only for the drop/truncate-partition global adjustment; ordinary truncate/drop do not perform that extra read. A raw-system-table receipt covers ordinary CREATE, CREATE LIKE, analyzed-table TRUNCATE, and DROP, and its dispatch mutation fails before the fix. The pinned GC receipts were also corrected to use `WithPruneMode`'s GLOBAL-plus-SESSION static setting and to respect the strict wall-clock GC version window after a fresh delayed-delete TSO. This remains DDL/storage integration evidence rather than a completion claim for either open package.

Revision note (2026-08-30): wired the pinned add-column subscriber and reconciled every Go-admitted ADD statistics shape. Nullable/defaultless columns use the current physical row count as `null_count`; declared string defaults and implicit NOT NULL integer zero defaults take the non-NULL `InsertColStats2KV` branch, including exact `Datum.GetBytes` total size and TypeBlob bucket bounds. Virtual generated ADD now reuses the ordinary executor's generated-expression validation, persists Go's canonical expression/dependency metadata, rebuilds the cluster executor table from it, and records the pinned empty histogram without a bucket; stored generated ADD retains Go's own 3106 refusal. The coprocessor scan no longer asks TiKV for a value it cannot store: like Go, it falls back to root-side dependency decoding and statement-context evaluation. The first source run reached the new metadata but returned NULL from the remote stored-column projection; the root boundary fixes the exact three-row `a + 1` result. This entry does not claim the enclosing DDL or storage package complete.

Revision note (2026-08-30): completed `InsertColStats2KV`'s three pinned column branches and connected the admitted `ActionModifyColumn` producers after rereading both `subscriber.go` and `storage/save.go`. The first regression expectation incorrectly treated a CREATE TABLE declared default as an origin default; its passing implementation exposed Go's deliberate distinction, and the receipt now proves the correct NULL placeholder and absent bucket after both metadata-only MODIFY and RENAME. The non-NULL branch uses `Datum.GetBytes` rather than display bytes for `tot_col_size`, converts both bucket bounds through TypeBlob, and only stages the bucket and metadata-version refresh if the histogram INSERT affected a row. This remains DDL/storage integration evidence rather than an enclosing-package completion claim.

Revision note (2026-08-30): added the pinned best-effort DROP SCHEMA subscriber after reading its mini-event construction and handler together. Unlike ordinary DROP TABLE, this event does not consult partition-prune mode: it always visits every partition definition and then the logical table ID, even when the logical UPDATE would be a no-op in static mode. Rust captures that exact ID sequence before the database disappears, retains one event transaction/start TS, suppresses only individual refresh/history failures, and still surfaces transaction-open/commit failures from the notifier boundary. This remains seed evidence inside the open DDL/storage package inventory.

Revision note (2026-08-30): restored pinned `asyncNotifyEvent`'s system/memory-schema boundary before all currently wired DDL statistics projections, reusing the shared `tidb-metadef` predicate instead of duplicating schema names. A source-shaped `mysql.*` regression covers table, column, and partition lifecycle events and was observed failing at the first CREATE with the guard disabled. This remains subscriber integration evidence rather than an enclosing-package completion claim.

Revision note (2026-08-30): connected Rust's admitted multi-schema ALTER executor to pinned Go's ordinary per-sub-job ADD COLUMN notifications. Applied additions are projected in SQL order and each runs through the existing independent subscriber transaction; `IF NOT EXISTS` no-ops publish nothing, and the common `IsMemOrSysDB` boundary now also covers the parent ActionMultiSchemaChange. The pinned two-default-column histogram sequence was observed failing with both rows absent before the dispatch fix and now proves both histogram and bucket payloads, no-op suppression, and system-schema suppression. Advanced subscriber actions whose owning DDL is not admitted remain outside this integration slice, so no enclosing-package completion claim is made.

Revision note (2026-08-30): reconciled pinned `globalstats.TestGlobalStats` through the ordinary planner and embedded-store execution paths. The source sequence exposed four connected gaps: dynamic pruning discarded Go's physical-plan partition access object; static children retained the logical table's statistics because Rust initialized before `PartitionProcessor`; static ANALYZE sampled before force-flushing target deltas; and the reduced planner view inferred initialization from placeholder maps instead of canonical `Table.IsInitialized`, then retained a real statistics version for a pseudo planner copy. Rust now preserves the pruning result on physical scans, reruns the existing statistics initializer after static partition expansion, invokes the existing forced-delta transaction for ANALYZE targets, and maps canonical pseudo state to `PseudoVersion`. The exact dynamic/static estimates, physical/global META rows, pseudo fallback, re-analysis, and table/index access paths pass. This closes one original test behavior but does not complete the remaining globalstats package inventory.

Revision note (2026-08-29): removed the in-process Rust refusal for pinned special-global-index ANALYZE tasks. The ordinary row-sampling plan now excludes GLOBAL prefix and virtual-generated indexes, while a separate ordered logical-index-key scan builds their stats-v2 histogram/TopN/FM result. Task selection matches pinned `checkIsAllSpecialGlobalIndex`, including its partition-scoped error, ordinary-index warning boundary, and empty-name branch; publishing an independent result replaces only that index and preserves existing table row-count metadata. This closes the in-process execution gap without claiming the enclosing executor or globalstats package complete.

Revision note (2026-08-29): wired pinned Go's static `PartitionProcessor` into the ordinary logical-rule and physical-executor path. Logical tables now retain their complete partition definition inventory, pruning shallow-copies one `DataSource` per surviving physical ID, and zero/one/many survivors become `TableDual`, the child, or `PartitionUnion` respectively. Physical lookup resolves partition IDs, statistics use the physical ID, point reads honor Go's partition restrictions, task memoization follows plan-object identity despite Go-retained numeric IDs, and column pruning inserts Go's repair projection when a child keeps condition-only columns. Focused session regressions cover negated pruning, empty pruning, join leaves, row-ID point reads, and multi-partition batch point reads. This is planner/executor integration evidence, not a completion claim for either enclosing Go package.
